# Чемодан (спрайт проходит через ноги и одежду унатха)

## Metadata
Thread ID: 1535027750082056404 / Date: 2026-08-06 / Author: 9Kvac_ / Tags: Разработка / Archive state: NOT archived / Messages: 2 (ответ — стикер-эмодзи от ᑕᗝᗩᒪᑕᑌᗷᗴᗪ) / Attachments: 2 (PNG/webp — не отрисовались в субагенте; presumed BUG_EVIDENCE)

## Discord evidence
- «Спрайт чемодана с бока проходит через ноги и одежду унатха» — в боковых направлениях (E/W) inhand-спрайт чемодана пересекает ноги/одежду унатха.
- Expected: чемодан держится сбоку, не пересекая тело. Actual: явное пересечение спрайтов.

## Search vocabulary
User terms: чемодан, унатх, с бока, ноги / Domain terms: inhand-left/inhand-right states, held item layer, Reptilian body sprite / Prototype-entity candidates: BriefcaseBase, BriefcaseBrown / Code symbols: ItemSystem.OnGetVisuals, TryGetDefaultVisuals

## Repository investigation
FILE: Resources/Prototypes/Entities/Objects/Misc/briefcases.yml
SYMBOL: `BriefcaseBase` (parent: BaseStorageItem; Item size: Ginormous; MeleeWeapon 8 blunt), `BriefcaseBrown` (Item sprite `_Erida/Objects/Storage/Briefcases/briefcase_brown.rsi`)
WHY RELEVANT: прототип чемодана из репорта.
CURRENT BEHAVIOR: inhand-визуалы берутся из RSI по умолчанию (явных InhandVisuals в yml нет).

FILE: Resources/Textures/_Erida/Objects/Storage/Briefcases/briefcase_brown.rsi/meta.json
SYMBOL: states `inhand-left` / `inhand-right` (directions: 4), size 32x32; copyright: inhand — Baystation, «Inhands resprited by MureixloL»
WHY RELEVANT: крупный полноразмерный боковой inhand-арт без смещений; рисуется поверх спрайта носителя.

FILE: Content.Client/Items/Systems/ItemSystem.cs
SYMBOL: OnGetVisuals (GetInhandVisualsEvent) → `defaultKey = $"inhand-{args.Location...}"`; TryGetDefaultVisuals (state = `inhand-left`/`inhand-right`, MapKeys = state)
WHY RELEVANT: доказывает механизм: боковой спрайт предмета добавляется слоем на спрайт носителя (выше слоёв тела), позиция — дефолтная, силуэт расы не учитывается.
CURRENT BEHAVIOR: при виде сбоку 32x32 inhand-спрайт чемодана перекрывает тело; у унатха (Reptilian) корпус/ноги длиннее, пересечение бросается в глаза.

## History
- briefcases.yml: 1f9241fca2 2026-08-11 "hello (#10)" — через 5 дней после репорта; менялся ли RSI/inhand, не проверено (git show упёрся в таймаут в рамках лимита вызовов).
- 06b00b9795 2026-07-07 "Nuke SPDX" — косметический; b1a4922dc2 2025-11-03 "Remove unused tags".
- Коммита, чинящего пересечение inhand с телом, не найдено.

## Upstream comparison
Inhand-спрайты родом из Baystation; рендер held-слоёв в SS14 идентичен — проблема в сочетании «большой боковой inhand + крупный спрайт унатха»; upstream-фикс не искался (локальных улик достаточно).

## Verdict
LIKELY_CURRENT_BUG — причинная цепочка по коду подтверждена (большой боковой inhand без оффсета, рендер поверх тела носителя); пиксельная проверка спрайтов/скриншотов недоступна в субагенте.

## Confidence
Medium

## Root cause
HYPOTHESIS: состояния `inhand-left`/`inhand-right` в _Erida/Objects/Storage/Briefcases/briefcase_brown.rsi (32x32, resprite MureixloL) занимают почти весь тайл и рендерятся на held-слое носителя без оффсетов → в боковых направлениях пересекают ноги/одежду унатха. (Medium)

## Proposed fix (do NOT implement!)
- Файлы: briefcase_brown.rsi — уменьшить/сместить `inhand-left`/`inhand-right` для E/W-направлений; альтернатива — per-species/per-direction offsets для held-слоёв (attachment points). Контент-фикс, код не требуется.

## Regression risk / Validation plan / Links-Related threads
Risk: меняется вид чемодана в руках у всех рас; проверить 4 направления × обе руки. Validation: унатх с чемоданом в левой/правой руке, скриншоты N/E/S/W; сверить с human. Related: 1449885909653651496 (inhand-состояния оружия) — общая тема per-hand состояний.

## Severity & complexity (if actionable)
Severity: S4 COSMETIC / Complexity: XS

## Verdict JSON (also write to batch file)
См. .ai-triage/data/verdicts/batch_B05.json
