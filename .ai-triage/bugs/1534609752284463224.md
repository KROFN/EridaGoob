# Ядерный Фабрикатор

## Metadata
Thread ID: 1534609752284463224 / Date: 2026-08-05 / Author: argun77 / Tags: "Разработка" / Archive state: NOT archived / Messages: 2 / Attachments: 1 (image.webp — not examined, media budget)

## Discord evidence
- Root: «Отсутствует в хранилище материалов» — Nuclear Fabricator is absent from the material storage (screenshot attached; [MEDIA] not examined this pass).
- Reply (Пряники, 2026-08-05, dev): «Знаем» — the team acknowledges it as a known issue.

## Search vocabulary
User terms: ядерный фабрикатор, хранилище материалов / Domain terms: Nuclear Fabricator, machine prototype, machine board, lathe recipe, material storage / Prototype-entity candidates: ent-NuclearFabricator, NuclearFabricatorMachineCircuitboard / Code symbols: FissionGenerator content set

## Repository investigation
1. [SRC] rg 'NuclearFabricator' across the whole repo → exactly ONE hit:
   FILE: Resources/Locale/ru-RU/_CorvaxGoob/fission-generator/entities.ftl (L247-248): `ent-NuclearFabricator = ядерный фабрикатор / .desc = Производит детали для ядерного реактора.`
   WHY RELEVANT: locale key exists but NO entity prototype with id NuclearFabricator exists anywhere in Resources/Prototypes → the machine itself was never ported. An unresolvable locale key alone cannot spawn or list a machine.
2. [SRC] FILE: Resources/Prototypes/_CorvaxGoob/Entities/Structures/Power/Generation/FissionGenerator/ (nuclear_reactor.yml, nuclear_centrifuge.yml, turbine.yml, gasTurbine_parts.yml, reactor_components.yml)
   WHY RELEVANT: the sibling fission-generator machines WERE ported; the fabricator is the lone machine of the set with no prototype → incomplete content port.
3. [SRC] rg 'Fabricator' in Resources/Prototypes/Recipes/Lathes/machine_boards.yml + Entities/Structures/Machines/lathe.yml → only `ExosuitFabricatorMachineCircuitboard` / `ExosuitFabricator`. No NuclearFabricator machine board, no lathe recipe, no flatpack, no cargo/storage entry → machine unobtainable by any route (build, print, order, spawn menu), which matches "absent from material storage" wherever the user looked. [INFER] the exact in-game "хранилище материалов" surface the reporter means (storage room/silo UI/cargo crate) cannot be pinned without the image, but with zero prototypes all routes are closed simultaneously — consistent either way.

## History
[HIST] `git log --all -S'NuclearFabricator'` → single commit 4cf391efb1 "The beginning of success (#4)" (added the locale string; never a prototype).
[HIST] Fission generator content: aa7019dedd "[Port] Reactor (#20)" — ported reactor/centrifuge/turbine; no subsequent commit adds the fabricator. Report date 2026-08-05 postdates all of this; state unchanged at HEAD.

## Upstream comparison
[UP] Upstream CorvaxGoob ships `ent-NuclearFabricator` (its locale was copied verbatim into ru-RU/_CorvaxGoob), so the machine definition should be fetchable from the CorvaxGoob fission-generator module. Not diffed further — local evidence sufficient for the verdict.

## Verdict
CONFIRMED_CURRENT_BUG
(prototype-absence is proven by exhaustive search; dev reply «Знаем» confirms it is a known content gap, not user error.)

## Confidence
High (for "machine is missing entirely") / Medium (for the specific storage-UI interpretation)

## Root cause
CONFIRMED: Nuclear Fabricator machine prototype does not exist in the fork — `NuclearFabricator` appears only as a dead locale key (Resources/Locale/ru-RU/_CorvaxGoob/fission-generator/entities.ftl:247) introduced by commit 4cf391efb1, with no entity YAML, no machine board (machine_boards.yml), no lathe recipe, no cargo/material-storage entry. The [Port] Reactor (#20) aa7019dedd brought the rest of the FissionGenerator set but omitted this machine.

## Proposed fix (do NOT implement!)
Port from CorvaxGoob upstream: entity prototype (machine, FissionGenerator dir), `NuclearFabricatorMachineCircuitboard` + lathe recipe in Recipes/Lathes/machine_boards.yml, research unlock (Research/industrial.yml already references the fission set), and its storage/cargo placement; verify `ent-NuclearFabricator` locale resolves. Why it fixes: every acquisition route (storage, printing, ordering) depends on the prototype existing.

## Regression risk / Validation plan / Links-Related threads
- Risk: new machine needs balance check (fuel rod production vs centrifuge economy).
- Validation: spawn/print machine, verify it lists and produces reactor parts; check storage UI shows it where expected.
- Related: B24 fission/reactor threads (Amber printers cluster unrelated).

## Severity & complexity (if actionable)
Severity: S3 MINOR (feature gap, dev-acknowledged) / Complexity: M (full prototype + board + recipes port)

## Verdict JSON (also written to batch file)
CONFIRMED_CURRENT_BUG, High, S3, M
