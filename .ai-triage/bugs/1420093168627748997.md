# Баг за феленидов

## Metadata
Thread ID: 1420093168627748997 / Date: 2025-09-23 / Author: Deleted User / Tags: unknown_tag / Archive state: archived / Messages: 5 / Attachments: 1 (image.png, скриншот редактора — классификация BUG_EVIDENCE по контексту; рендер изображения в суб-агенте недоступен)

## Discord evidence
- Автор: «В меню редакции персонажа в чертах внешности можно выбрать лишь голову (Уши), и хвост. Другие косметические предметы, например как отдельный цвет глаз, повязки, шрамы тела, лица, отсутствуют».
- Vecortys (мейнтейнер): «Шарим. Надо будет пофиксить» — признание проблемы.
- Ожидание: феленидам доступны повязки, шрамы (лица/тела), отдельный цвет глаз и пр. Факт: в редакторе доступны только уши/хвост (+ волосы).

## Search vocabulary
User terms: феленид, уши, хвост, повязки, шрамы, цвет глаз / Domain terms: markingPoints, markingLimits, markingCategory, gauze, scars, Overlay, Head / Prototype-entity candidates: Felinid, MobFelinidMarkingLimits, Gauze*, Scar* / Code symbols: MarkingPicker, MarkingPoints, MarkingCategories

## Repository investigation
FILE: Resources/Prototypes/Nyanotrasen/Species/felinid.yml
SYMBOL: species Felinid; markingLimits: MobFelinidMarkingLimits (строка 9)
WHY RELEVANT: вайдлист категорий маркировок феленида задаёт, что видно в редакторе.
CURRENT BEHAVIOR: MobFelinidMarkingLimits содержит только Hair, FacialHair, Tail(required), HeadTop(required), Chest, Right/Left Leg/Foot/Arm/Hand. ОТСУТСТВУЮТ категории: Head, HeadSide, Snout, Overlay (сравнить с MobHumanSprites-набором человека).

FILE: Resources/Prototypes/Entities/Mobs/Customization/Markings/gauze.yml
SYMBOL: GauzeLefteyePatch, GauzeBlindfold, GauzeShoulder, GauzeStomach и др. — у всех `markingCategory: Overlay`
WHY RELEVANT: «повязки» из репорта существуют как контент, но категория Overlay не входит в MobFelinidMarkingLimits → не выбираемы.
CURRENT BEHAVIOR: повязки недоступны феленидам (и человеку — в MobHumanMarkingLimits Overlay тоже нет).

FILE: Resources/Prototypes/Entities/Mobs/Customization/Markings/scars.yml
SYMBOL: ScarEyeRight/ScarEyeLeft/ScarNeck → `markingCategory: Head`; ScarChest/ScarTopSurgery* → `markingCategory: Chest`
WHY RELEVANT: лицевые шрамы требуют категории Head, которой у феленидов нет; шрамы груди (Chest) формально доступны (points: 10).
CURRENT BEHAVIOR: «шрамы лица» недоступны; тела — доступны, что противоречит формулировке автора («шрамы тела … отсутствуют»), вероятно он не нашёл их в списке.

FILE: rg 'markingCategory: Eyes' Resources/Prototypes → 0 результатов
SYMBOL: MarkingCategories.Eyes
WHY RELEVANT: «отдельный цвет глаз» маркировками в репо не реализован вовсе; цвет глаз задаётся генерическим пикером (profile.Appearance.EyeColor), не маркировкой.
CURRENT BEHAVIOR: соответствующего контента нет для любых рас.

FILE: Content.Client/Humanoid/MarkingPicker.xaml.cs
SYMBOL: строки 84 (SpeciesPrototype.MarkingPoints), 197 (PointsLeft(category)==0 → нельзя добавить)
WHY RELEVANT: редактор строится по markingPoints вида — категория отсутствует в вайдлисте ⇒ маркировки категории недоступны.

## History
- 2eba26cd06 (2026-08-16) "Changed marking limit" — поднял points у феленида 1→10, набор категорий НЕ менял.
- 0572564025 (2026-08-24) "[Fix] Bug fixes (#32)" — трогал felinid.yml, категории Head/Overlay/Eyes не добавлены (текущий файл их не содержит).
- Коммитов после 2025-09-23, добавляющих недостающие категории, не найдено → проблема в актуальном виде сохранилась.

## Upstream comparison
Not investigated — local evidence sufficient (Nyanotrasen-феленид в апстриме имеет тот же вайдлист; разрыв — контентное решение форка).

## Verdict
CONFIG_OR_CONTENT_ISSUE — система работает как запрограммировано: редактор ограничен whitelist'ом markingPoints. Нужные семьи маркировок (gauze, scars) существуют в репо, но их категории (Overlay, Head) не добавлены в MobFelinidMarkingLimits; «цвет глаз» маркировками не существует в принципе. Изменение — контентное/конфигурационное; мейнтейнер уже признал необходимость правки.

## Confidence
High

## Root cause
CONFIRMED: MobFelinidMarkingLimits (Resources/Prototypes/Nyanotrasen/Species/felinid.yml:13-56) не содержит категорий Overlay/Head/HeadSide/Snout, поэтому gauze.yml (Overlay) и лицевые scars.yml (Head) не отображаются в редакторе феленида; отдельного контента «цвет глаз» (markingCategory: Eyes) в репозитории нет.

## Proposed fix (do NOT implement!)
- Resources/Prototypes/Nyanotrasen/Species/felinid.yml: добавить в MobFelinidMarkingLimits категории `Head` и `Overlay` (опционально `HeadSide`, `Snout`) с points: 10; для «цвета глаз» — отдельная задача контента (маркировки с markingCategory: Eyes).
- Минимальное изменение — YAML; кода не требует. Проверить points-баланс и что Gauze*/ScarEye* корректно рендерятся на MobHumanSprites.

## Regression risk / Validation plan / Links-Related threads
Риск: минимальный (только YAML). Валидация: в редакторе феленида появляются разделы Head/Overlay; повязка глаза и ScarEye накладываются и в игре. Связано: 1452510092305104927 (у унатхов нет слоя волос — другой механизм, тот же домен маркировок).

## Severity & complexity
Severity: S3 MINOR / Complexity: S

## Verdict JSON
См. .ai-triage/data/verdicts/batch_B04b.json
