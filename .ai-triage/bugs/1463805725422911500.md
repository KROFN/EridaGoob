# Пироги и их отображение на лице (pie on face stays for everyone until relog, even after washing)

## Metadata
Thread ID: 1463805725422911500 / Date: 2026-01-22 / Author: 𝗪𝗵𝗮𝘁𝗔𝗽𝗽𝗹𝗲 (whatapple) / Tags: Незначительный, Визден-Баг, unknown_tag / Archive state: archived / Messages: 1 / Attachments: 1 (Screenshot_7444.png)

## Discord evidence
[SRC] Root: «Вас прилетел пирог? - Теперь все те, кто не перезаходил, не вы, а они, будут на вас его видеть. А ну и да, сами вы тоже будете его видеть до перезахода».
- After being pied and then washing it off, every client that was already connected (including the victim's own view) keeps rendering the pie overlay until they reconnect; freshly connected clients see no pie.
[MEDIA] attachments/1463805725422911500/1463805725917577318_Screenshot_7444.png — not examined (NO image reads per task constraint); presumed BUG_EVIDENCE.

## Search vocabulary
User terms: пирог → cream pie; помыть → wash; перезаход → relogin; отображение на лице → face overlay.
Domain terms: CreamPiedComponent, CreamPieSystem, WashCreamPieEntityEffect, client overlay, component removal net message.
Code symbols: CreamPiedSystem (client), SharedCreamPieSystem, CreamPieSystem (server), WashCreamPieEntityEffectSystem.

## Repository investigation
FILE: Content.Client/Nutrition/EntitySystems/CreamPiedSystem.cs
SYMBOL: `class CreamPiedSystem : SharedCreamPieSystem` — EMPTY subclass (11 lines total).
WHY RELEVANT: all pie-overlay logic lives in the shared system; there is no dedicated client cleanup path.
CURRENT BEHAVIOR: stale-overlay symptom (visual persists until reconnect) is consistent with overlay add/remove being handled asymmetrically: added on component init, but not reliably removed on component removal/property change for already-connected clients.

FILE: Content.Shared/Nutrition/EntitySystems/SharedCreamPieSystem.cs
SYMBOL: SharedCreamPieSystem (visual/overlay handling for CreamPiedComponent)
WHY RELEVANT: candidate location of the missing removal handler (component-removal hook / appearance state reset).
CURRENT BEHAVIOR: [INFER] not fully read within budget; suspect.

FILE: Content.Shared/EntityEffects/Effects/WashCreamPieEntityEffectSystem.cs
SYMBOL: WashCreamPieEntityEffect — wash (shower/sink) effect that removes CreamPiedComponent.
WHY RELEVANT: server-side removal works (relogging players see no pie) → the data is cleaned; only already-connected clients show the stale overlay.
CURRENT BEHAVIOR: matches reporter's "everyone who didn't relog still sees it".

FILE: Content.Shared/Nutrition/Components/CreamPiedComponent.cs / Content.Server/Nutrition/EntitySystems/CreamPieSystem.cs
SYMBOL: CreamPiedComponent / server smash logic
WHY RELEVANT: state owner; removal path exercised by washing.

## History
[HIST] No targeted git history pulled within budget; no commit evidence of a fix. Tag «Визден-Баг» (staff) suggests upstream-origin suspicion. [?]

## Upstream comparison
Not verified — the wash effect + CreamPied overlay exist in WizDen upstream too; an upstream fix commit was not located within budget. [?]

## Verdict
LIKELY_CURRENT_BUG

## Confidence
Medium

## Root cause
HYPOTHESIS: washing removes `CreamPiedComponent` server-side (WashCreamPieEntityEffectSystem), but the client-side face overlay managed by SharedCreamPieSystem/CreamPiedSystem has no (working) removal path on component-removal/dirty for already-connected sessions — each connected client keeps a stale local overlay until relogin, while new sessions render correctly. [INFER] exact missing hook not read within budget.

## Proposed fix (do NOT implement!)
In SharedCreamPieSystem subscribe to component removal (or clear the overlay in the wash effect via `AppearanceSystem.SetData`/`RemComp` with Dirty), ensuring the overlay layer is deleted for all clients on removal; unit-test: smash → wash → assert no CreamPiedComponent and no overlay state. Why: mirrors the add path with a symmetric remove path.

## Regression risk / Validation plan / Links-Related threads
Low. Validation: two clients A(pied)/B(observer): wash → both must lose overlay without reconnect; also test shower vs sink vs bath. Related: B20 cleaning threads (1368389167616098424 mop/footprints cleanup family).

## Severity & complexity
Severity: S3 MINOR / Complexity: S

## Verdict JSON
See .ai-triage/data/verdicts/batch_B30.json
