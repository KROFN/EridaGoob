# Окно консоли (окно РнД-консоли «шлёт куда подальше», всё «в откатике»)

## Metadata
Thread ID: 1367643283735576606 / Date: 2025-05-01T23:26Z / Author: 🐰8LilApte4ka8🐇 / Tags: «На рассмотрении» / Archive: archived / Messages: 10 / Attachments: 1 (Base_Profile_2025.05.02_DVR.mp4 — BUG_EVIDENCE, видео НЕ анализировалось: лимит/запрет батча)

## Discord evidence
- «вот хочется поработать в рнд, поизчать его на фулл, а в итоге окно шлет куда подальше.... все в откатике» — работа с РнД-консолью; окно «отправляет» игрока/пропадает; итог «откатик» (потеря прогресса) [MEDIA: видео, не разбирали].
- 2025-05-02 no_mad: «В настройках есть штука которая отвечает за перемещение окон не за края окна. Переключи его» [HIST].
- Автор: не нашёл такой настройки («я все настройки игры облазил, ее нет»), 2025-06-01 повторно спрашивает — тред завис [HIST].
- Интерпретацияclaim [INFER]: (а) клики/драг из окна проваливаются в мир и персонажа «утаскивает» от консоли, либо (б) окно улетает за экран, и «откатик» — потеря наигранного прогресса (возможно, вообще серверный откат, а не клиентский баг).

## Search vocabulary
User terms: окно, рнд, откатик / Domain terms: ResearchConsole, UI window drag, options menu, BoundUserInterface / Prototype candidates: ComputerResearchAndDevelopment / Code symbols: ResearchConsoleBoundUserInterface, ResearchConsoleMenu, CVar (ui.*).

## Repository investigation
FILE: Content.Client/Research/UI/ResearchConsoleBoundUserInterface.cs, ResearchConsoleMenu.xaml.cs; Content.Server/Research/Systems/ResearchSystem.Console.cs
SYMBOL: ResearchConsoleBoundUserInterface (UpdateState, OnOpen)
WHY RELEVANT: UI РнД-консоли, о которой репорт.
CURRENT BEHAVIOR: стандартный BoundUserInterface; логики «выталкивания» игрока или перемещения окна в коде консоли не видно (rg Close|UserInterface|SetServer — только ожидаемые хуки). Консоль закрывается стандартно при выходе из радиуса — как и везде.

FILE: Content.Client/Preferences/** + Resources/Locale/en-US/preferences/ui/options-menu.ftl
SYMBOL: настройки клиента
WHY RELEVANT: no_mad советовал настройку «перемещение окон не за края окна».
CURRENT BEHAVIOR: `rg -i 'drag' Content.Client/Preferences` → пусто; в en-US локали совпадения только по нерелевантным файлам (tips, dragon и т.п.). Настройки drag-окон в клиенте форка НЕ найдено — совет был неверен, переключить нечего.

FILE: RobustToolbox/
SYMBOL: — 
WHY RELEVANT: где живёт код перетаскивания окон/движка.
CURRENT BEHAVIOR: каталог RobustToolbox ПУСТ (сабмодуль не выкачен; `ls | wc -l` → 0) — исходники движка недоступны для проверки; искать CVar вида ui.drag/window-drag нельзя [?].

## History
`git log --since=2025-04-01 -i --grep='drag'` — только нерелевантные коммиты (drag сущностей, card_drag спрайты) [HIST]. Изменений Research UI после 2025-05-01 не зафиксировано. Upstream-статус не проверяем из-за пустого сабмодуля.

## Upstream comparison
Не исследовано: RobustToolbox (движок, где реализовано перетаскивание UI-окон) не выкачен в этом чекауте; Content-часть изменений не имеет. [?]

## Verdict
CANNOT_VERIFY — ключевое доказательство (видео) не проанализировано из-за ограничений батча; формулировка репорта двусмысленна («окно шлёт куда подальше» — утаскивает персонажа или улетает само окно; «откатик» может быть серверным роллбэком, т.е. не багом клиента). Рекомендованная в треде настройка в форке отсутствует — подсказка no_mad неверна.

## Confidence
Low

## Root cause
HYPOTHESIS (Low): неопределено. Кандидаты: (1) клик-сквозь/драг UI-окна приводит к движению персонажа и разрыву взаимодействия с консолью (провал кликов в мир при перетаскивании окна); (2) окно улетает за пределы экрана при драге (движковый баг Robust, код недоступен); (3) «откатик» — серверный откат прогресса, не воспроизводимый баг. Локальных доказательств для любого из вариантов недостаточно.

## Proposed fix (do NOT implement!)
Ничего не менять до верификации. План: (1) ffprobe/ffmpeg видео 1367643288705695804, извлечь кадры и классифицировать поведение окна; (2) выкачать RobustToolbox, проверить CVar-ы перетаскивания окон (Robust.Client/UserInterface) и наличие опции в OptionsMenu; (3) воспроизвести: открыть РнД-консоль, драгать окно за различные зоны, наблюдать движение персонажа/улетание окна.

## Regression risk / Validation plan / Links-Related
Риск: без видео любое «починенное» поведение может не совпасть с жалобой. Валидация — по шагам из Proposed fix. Related: batch B25 (research-консоли: 1365481390363115561, 1502364788683046953) — при подтверждении склеить в кластер UI-окон.

## Severity & complexity
Severity: S3 MINOR (потенциально S2, если подтверждается потеря прогресса у всех игроков) / Complexity: M (верификация)

## Verdict JSON
См. .ai-triage/data/verdicts/batch_B24a.json
