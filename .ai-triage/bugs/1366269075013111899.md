# Бесплатная тпешка шадоукинов

## Metadata
Thread ID: 1366269075013111899 / Date: 2025-04-28 / Author: xkttty / Tags: none / Archive state: archived / Messages: 2 / Attachments: 2 (image.png ×2 — screenshots, not renderable in triage environment; per author description = BUG_EVIDENCE, classification UNVERIFIED)

## Discord evidence
- «если нести за собой какой то предмет или ящик и нажать телепорт то стамина на тп не потратится и звука не будет, может быть очень сильный багоюз»
- Expected: teleport (shadowkin тпешка) always spends stamina and plays its sound.
- Actual: while carrying/dragging an item or crate, teleport executes with no stamina cost and no sound.
- Author calls it a serious exploit ("очень сильный багюз"). Only reply is an emoji (🤫). No admin/dev response.
- Media: 2 screenshots; cannot be rendered in this environment → treated as UNVERIFIED BUG_EVIDENCE.

## Search vocabulary
User terms: тпешка (teleport), стамина, звук, нести предмет/ящик (carry/drag).
Domain terms: shadowkin teleport = EE "Shadeskip"; stamina cost (StaminaComponent); pulling/carrying state; sound emission.
Prototype-entity candidates: MobShadowkinBase, EffectFlashShadowkinShadeskip, ShadowkinShadow.
Code symbols: ShadowkinComponent / ShadeskipSystem (Einstein Engines, expected names) — NOT PRESENT in fork.

## Repository investigation
- FILE: Resources/Prototypes/_EinsteinEngines/Entities/Mobs/Species/shadowkin.yml
  SYMBOL: MobShadowkinBase
  WHY RELEVANT: playable shadowkin species root prototype.
  CURRENT BEHAVIOR: contains NO ability component and NO action grants (no ShadowkinComponent, no teleport/Shadeskip action) — the teleport ability reported here is not wired anywhere in current fork data.
- FILE: Resources/Prototypes/_EinsteinEngines/Entities/Effects/shadow.yml
  SYMBOL: EffectFlashShadowkinShadeskip
  WHY RELEVANT: the EE "shadeskip" visual/sound flash entity survives as data; in current tree it is only spawned by the Destructible gib threshold of MobShadowkinBase — no teleport system references it.
- FILE: Content.Server/**, Content.Shared/** (rg -i "shadowkin" in *.cs)
  SYMBOL: none — only hit is Content.Shared/_EinsteinEngines/Language/LanguagePrototype.cs (language mention).
  WHY RELEVANT: proves the shadowkin ability C# (teleport logic, stamina cost, sound emission) is absent from the current fork; the cost/sound bug cannot be located in this codebase.
- FILE: Resources/Audio/_EinsteinEngines/Effects/Shadowkin/shadeskip.ogg (+ darkswapon/off.ogg, futuristic-teleport.ogg)
  WHY RELEVANT: sound assets for the EE teleport were ported — the feature existed data-side only.

## History
- fa46b517e0 2025-05-22 "[Port] [Bounty] Shadowkins from EE (#2675)" — data-only port (audio/locale/body/markings/species yml + FlashSystem/FlashbangSystem tweaks). No ability system files. Dated AFTER this report (2025-04-28).
- PR #342 (changelog id 44, EridaChangelog.yml) "Shadowkins are no longer breathing" — later shadowkin tweak, unrelated.
- No commit adds or removes shadowkin ability C# anywhere in repo history (pathspec search for deleted *hadowkin*/*ampire* files: empty).

## Upstream comparison
Erida upstream (dead-space-server/EridaGoob, upstream/master): `git ls-tree -r upstream/master` contains no shadowkin C# either — same data-only state. Goob-Station: no shadowkin teleport system in its history within this repo. Original EE (Einstein Engines) shadowkin ability code was never merged here.

## Verdict
CANNOT_VERIFY — the shadowkin teleport ("Shadeskip") ability does not exist in the current fork (no C#, no action YAML, species prototype grants no abilities). The reported bug is against a server build whose ability code is not in this git tree; root cause cannot be located or reproduced here.

## Confidence
Medium (absence in this tree is verified; the live server demonstrably had the ability, so the real server build exceeds this repository's contents).

## Root cause
HYPOTHESIS (Low-Medium): in the EE Shadeskip implementation the stamina cost + sound are applied on a code path that is skipped when the user is pulling/carrying an entity (e.g., early-return on pulled/contained state after or instead of cost application), yielding a free, silent teleport. Cannot be proven: system code absent from this fork.

## Proposed fix (do NOT implement!)
- Locate ShadeskipSystem/ShadowkinComponent in the actual server build (or re-port from Einstein Engines).
- Apply stamina cost and sound unconditionally at the start of the teleport handler; reject teleport while pulling/carrying if intended.
- Add tests: teleport cost with empty hands vs. pulling a crate must be identical.

## Regression risk / Validation plan / Links-Related threads
- Risk: over-blocking teleports during legit pulls; ensure intangible-state transitions still cost stamina.
- Validation: pull a crate → teleport → assert stamina reduced and sound played.
- Related: 1375435479725375538 (same bug, cluster shadowkin-free-teleport).

## Severity & complexity (if actionable)
Severity: S2 MAJOR (balance exploit: free mobility/escape) / Complexity: M (feature must be re-located/re-ported first).

## Verdict JSON (also written to batch file)
see .ai-triage/data/verdicts/batch_B15.json
