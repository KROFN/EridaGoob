# Экспедиции сломаны

## Metadata
Thread ID: 1414517676541349909 / Date: 2025-09-08 / Author: FreshInk / Tags: unknown_tag / Archive state: unresolved ("Полагаю баг оффов") / Messages: 13 / Attachments: none

## Discord evidence
- At expedition start the announcement shows only the timer ("Осталось до конца экспедиции 11 минут"), no destination/direction.
- Expedition generation is broken: no enemies, no loot, only "fixed loot" (wrappers, papers, drill + pickaxe = shuttle loadout), no ores.
- Reply (Vecortys) suspected a broken localization file; reply (Vecortys) suspected an upstream ("оффы") bug.

## Search vocabulary
User terms: экспедиции, куда идти, генерация, лут, руда / Domain terms: salvage expeditions, dungeon generation, announcement, FTLDestination / Prototype candidates: SalvageExpeditionComponent, SalvageDungeonModPrototype, dungeon configs / Code symbols: SpawnSalvageMissionJob, DungeonLocation, Announce, ExpeditionStage.

## Repository investigation
FILE: Content.Server/Salvage/SpawnSalvageMissionJob.cs
SYMBOL: SpawnSalvageMissionJob.ExecuteAsync (~lines 95-186)
WHY RELEVANT: order of operations — `FTLDestinationComponent` enabled (~95), `SalvageExpeditionComponent` + EndTime added (~148), THEN dungeon generation and `expedition.DungeonLocation = dungeonOffset` (184) only AFTER `dungeon.Rooms.Count == 0 → return false` (~179-186).
CURRENT BEHAVIOR: if dungeon generation yields no rooms, the job returns false AFTER the destination and expedition component are live; DungeonLocation stays Vector2.Zero and all budgeted mob/loot spawning (code after the check) never runs.

FILE: Content.Server/Salvage/SalvageSystem.Expeditions.cs
SYMBOL: UpdateExpeditions (lines ~105-120)
WHY RELEVANT: `case JobStatus.Finished: _salvageJobs.Remove(...)` — job result/success is NOT checked; a failed mission leaves the half-created map, its FTL destination and the countdown in place.
CURRENT BEHAVIOR: players can FTL to an empty planet: countdown announces work, no dungeon, no mobs/loot.

FILE: Content.Server/Salvage/SalvageSystem.Runner.cs
SYMBOL: OnFTLCompleted (lines ~101-118)
WHY RELEVANT: announces countdown always, but `salvage-expedition-announcement-dungeon` only `if (component.DungeonLocation != Vector2.Zero)` — explains "не показывается куда идти, а только окончание".

FILE: Resources/Locale/ru-RU/procedural/expeditions.ftl
SYMBOL: salvage-expedition-announcement-dungeon (line 39)
WHY RELEVANT: locale key EXISTS and is complete → Vecortys' "сломанный файл локализации" hypothesis disproven.

## History
git log since 2025-08-01 for Content.Server/Salvage/: only minor upstream commits (SPDX nuke, station method moves, SalvageStructureComponent atoms, 20 july upstream sync). No relevant fix after the report date.

## Upstream comparison
Erida upstream: code identical to upstream SS14 salvage/expeditions layout; the failure-handling gap exists upstream too. Not investigated further — local evidence sufficient (matches Vecortys' "баг оффов" suspicion).

## Verdict
LIKELY_CURRENT_BUG — failure-handling gap confirmed in code; the trigger making dungeon generation return zero rooms is not reproducible from code alone.

## Confidence
Medium

## Root cause
HYPOTHESIS (Medium): dungeon generation for the mission returns an empty dungeon (`SpawnSalvageMissionJob`, `Rooms.Count == 0 → return false`) or otherwise fails after `FTLDestinationComponent`/`SalvageExpeditionComponent` are already created; `UpdateExpeditions` ignores job failure → a live expedition map without dungeon/mobs/loot and with `DungeonLocation == Vector2.Zero` (no direction announcement). Exact trigger (async generation failure, seed/config) unproven.

## Proposed fix (do NOT implement!)
- Content.Server/Salvage/SalvageSystem.Expeditions.cs: in `UpdateExpeditions`, on `JobStatus.Finished` check `job.Result`; on failure delete the mission map (`QueueDel(mapUid)`) and reset the station's claim/offer state.
- Content.Server/Salvage/SpawnSalvageMissionJob.cs: create the FTL destination and expedition component only AFTER the dungeon succeeds; log dungeon-generation failures.
- Investigate `DungeonSystem.GenerateDungeonAsync` empty-room causes (dungeon config for the rolled `SalvageDungeonModPrototype`).

## Regression risk / Validation plan / Links-Related threads
Deleting maps on failure risks canceling valid in-progress missions — gate on `Stage == Added` / no players on map. Validation: roll many mission seeds (offer 4+ missions across rounds), FTL to each, verify direction announcement + dungeon content; add automated spawn-job smoke test. Related: 1432041966081671240 (Box lacks expeditions console per thread chatter — separate content matter).

## Severity & complexity
Severity: S2 MAJOR / Complexity: M

## Verdict JSON
See .ai-triage/data/verdicts/batch_B31.json
