# Диагональные стены (diagonal walls always place in one direction, ignore intended orientation)

## Metadata
Thread ID: 1438072028904030238 / Date: 2025-11-12 / Author: Звезда лисы (jickson900)
Tags: На рассмотрении, unknown_tag / Archive: archived / Messages: 1 / Attachments: 0 (reactions ✅:1)

## Discord evidence
- "Когда ставишь диагональные стены - они ставятся не так, как ты хочешь и размещаешь призрак конструкции, а строго в одном направлении" — when building diagonal walls, the construction ghost orientation is ignored: the wall always snaps to one fixed diagonal direction, not the one the player intends.
- "Я не знаю баг ли это визардов или нет (на фронтирах этого бага нет), но вы все его знаете" — author believes Frontiers fork does not have this; states it is well known to the community. Media: none.

## Search vocabulary
User terms: "диагональные стены/призрак конструкции/ставить" / Domain terms: diagonal wall, construction ghost, placement rotation, construction graph / Prototype candidates: WallDiagonal / GirderDiagonal / WindowDiagonal graphs / Code symbols: Diagonal tag, ConstructionSystem.Placement, FixRotationsCommand, TileWallsCommand.

## Repository investigation
FILE: Resources/Prototypes/Recipes/Construction/structures.yml:173, 252, 264, 290, 318, 360, 416
SYMBOL: `DiagonalShuttleWall`, `GrilleDiagonal`, `ClockworkGrilleDiagonal`, `WindowDiagonal`, `ReinforcedWindowDiagonal`, `ClockworkWindowDiagonal`, `PlasmaWindowDiagonal` (graphs GrilleDiagonal / WindowDiagonal etc.)
WHY RELEVANT: [SRC] diagonal construction family exists in the fork as WizDen-style construction graphs; diagonal walls ride the same graph/placement machinery.

FILE: Content.Server/Construction/Commands/TileWallsCommand.cs:28,89 and Content.Server/Construction/Commands/FixRotationsCommand.cs:21,99-100
SYMBOL: `ProtoId<TagPrototype> DiagonalTag = "Diagonal"`; diagonal entities explicitly skipped/excluded in wall-tiling and rotation-fixing admin commands.
WHY RELEVANT: [SRC] diagonal structures are special-cased as rotation-anomalies in the codebase (their rotation is managed differently from ordinary walls).

FILE: Content.Shared/Construction + Content.Server/Construction (grep `iagonal` in `*.cs`)
SYMBOL: **no diagonal-specific placement/orientation code found** in the construction systems (only the two admin commands above).
WHY RELEVANT: [INFER] nothing in the fork's construction code re-orients the ghost based on player facing for diagonal variants — orientation appears to be decided inside the generic WizDen placement path, which for diagonal variants resolves to a fixed direction, matching the report.

## History
No relevant commits found within budget (git log not run on construction placement code — budget exhausted). [HIST] report from 2025-11-12, still "На рассмотрении".

## Upstream comparison
SS14/WizDen: diagonal placement is a known upstream limitation/quirk (author suspects "баг визардов"). Frontiers: reported as not affected — likely carries a local fix; not verified. Goob: not investigated — local evidence sufficient to record symptom.

## Verdict
LIKELY_CURRENT_BUG (placement orientation of diagonal walls ignores player intent; responsible code path not located within budget)

## Confidence
Low

## Root cause
HYPOTHESIS (Low): construction ghost placement for diagonal wall graphs resolves the entity rotation to a constant (or derives it from something other than user facing) in the generic construction placement system, since no diagonal-orientation handling exists in Content.Shared/Construction or Content.Server/Construction — the diagonal structures are only special-cased as "Diagonal" tag exceptions in admin commands (TileWallsCommand.cs:28, FixRotationsCommand.cs:99).

## Proposed fix (do NOT implement!)
Investigate `Content.Shared/Construction/ConstructionSystem.Placement.cs` (ghost rotation computation at StartBuild/TryStartBuild) and make diagonal graph variants (two opposite diagonals) selected from the player's LocalRotation, as reportedly done in Frontiers; compare Frontiers' implementation. Files: Content.Shared/Construction/*.cs (+ possibly construction graph YAML for diagonal walls).

## Regression risk / Validation plan / Links-Related
Risk: medium — placement math is shared by all construction; test normal walls/windows/grilles. Validate: in-game, face four different directions and build a diagonal wall/grille/window — ghost must follow the intended diagonal. Links: B19 window batch (adjacent directional-window work).

## Severity & complexity
Severity: S3 MINOR (construction UX, widely known annoyance) / Complexity: M
