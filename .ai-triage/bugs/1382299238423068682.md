# Хвост кицунэ.

## Metadata
Thread ID: 1382299238423068682 / Date: 2025-06-11 / Author: Звезда лисы (jickson900) / Tags: "В процессе" / Archive state: archived / Messages: 3 / Attachments: 4 (image/png ×4 — ракурсы персонажа с хвостом кицунэ)

## Discord evidence
- Автор: хвост кицунэ «накладывается поверх одежды, должно быть наоборот. А так же на него накладывается предметы из рук, когда смотришь на спину персонажа, хотя тут тоже должно быть наоборот».
- 4 скриншота (BUG_EVIDENCE, разобраны через VLM по ракурсам):
  - front: хвост за телом, одежда корректна;
  - вид справа: хвост за одеждой — приемлемо;
  - вид слева: хвост ПОВЕРХ одежды (held item при этом над хвостом);
  - back: хвост полностью поверх одежды.
- cybertrash (2025-06-12): «Уже занимаються» — признание валидности и обещание работы; коммита с фиксом не найдено.

## Search vocabulary
User terms: хвост кицунэ, поверх одежды, предметы из рук, спина
Domain terms: kitsune tail marking, HumanoidVisualLayers.Tail, in-hand visuals layering, held item layers, species layers
Prototype-entity candidates: AnthropomorphTailKisune (mam_tails.rsi state tail_kitsune)
Code symbols: HumanoidAppearanceSystem.ApplyMarking, HandsSystem.UpdateHandVisuals, LayerMapReserve

## Repository investigation
FILE: Resources/Prototypes/_Erida/Customization/Markings/anthropomorph.yml
SYMBOL: AnthropomorphTailKisune (1246): bodyPart: Tail, markingCategory: Tail, sprite `_Orion/Mobs/Customization/Anthropomorph/mam_tails.rsi#tail_kitsune` (4 направления), без `layering:`
WHY RELEVANT: сам marking из репорта.
CURRENT BEHAVIOR: рисуется на слое Tail вида (выше одежды) на side-left и back ракурсах.

FILE: Resources/Prototypes/_Erida/Entities/Mobs/Species/anthropomorph.yml
SYMBOL: Sprite layers: `LHand`/`RHand` (86–87) … `enum.HumanoidVisualLayers.Tail` (109)
WHY RELEVANT: базовые слои рук стоят РАНЬШЕ слоя Tail, но предметы в руках добавляются динамически.
CURRENT BEHAVIOR: см. следующий пункт — динамические hand-слои всплывают наверх, выше хвоста.

FILE: Content.Client/Hands/Systems/HandsSystem.cs
SYMBOL: UpdateHandVisuals (~281–382): `var index = _sprite.LayerMapReserve((ent, sprite), key)` (строка 342)
WHY RELEVANT: `LayerMapReserve` с новым ключом добавляет слой в КОНЕЦ списка спрайта → визуал предметов в руках всегда выше статических слоёв, включая Tail.
CURRENT BEHAVIOR: на виде со спины предметы в руках перекрывают хвост — ровно как описал автор (вторая часть репорта). Кодовое подтверждение.

FILE: Content.Client/Humanoid/HumanoidAppearanceSystem.cs
SYMBOL: ApplyMarking — вставка marking-спрайта на `targetLayer+1` от слоя Tail
WHY RELEVANT: объясняет «хвост поверх одежды» на back/side-left.
CURRENT BEHAVIOR: совпадает со скриншотами (VLM-разбор).

## History
- Репорт 2025-06-11; ответ «Уже занимаються» 2025-06-12.
- `git log -- _Erida/Customization/Markings/anthropomorph.yml` → только 72a45ae642 (Species #12) и 4d254ce565 (Fix #31, без изменений хвостов). `git log -i --grep='kitsune\|tail layer\|TailBehind'` — фикс-коммитов нет.
- Механизм `MarkingPrototype.Layering` (позволяющий отметить состояния хвоста на TailBehind) портирован только 2026-08-01 (a2ba0ebe00) — после репорта, но для mam_tails не применён.

## Upstream comparison
Erida upstream (upstream/master @ 511cc23b7c): marking AnthropomorphTailKisune и порядок слоёв идентичны → в апстриме не исправлено. Goob-Station: dogTail из `_Goobstation/.../tails.yml` тоже без layering (но слои base.yml ставят Tail после одежды — поведение одинаковое). WizDen — не применимо.

## Verdict
CONFIRMED_CURRENT_BUG — (1) хвост кицунэ над одеждой: слой Tail вида после jumpsuit/shoes + `ApplyMarking` (вставка на Tail+1); (2) предметы в руках над хвостом: hand-визуалы добавляются `LayerMapReserve` в конец спрайта, выше Tail. Обе части подтверждены кодом и скриншотами. Член кластера marking-layer-order (первичный: 1379120791253418124).

## Confidence
High

## Root cause
CONFIRMED: `Resources/Prototypes/_Erida/Entities/Mobs/Species/anthropomorph.yml:109` (Tail над одеждой) + `Content.Client/Humanoid/HumanoidAppearanceSystem.cs:ApplyMarking`; вторичная часть — `Content.Client/Hands/Systems/HandsSystem.cs:342` (LayerMapReserve добавляет hand-слои поверх всех).

## Proposed fix (do NOT implement!)
- Часть 1 — общий фикс кластера: `layering:` → `TailBehind`/`TailBehindBackpack` для хвостов (образец `_Imp/Mobs/Customization/moth.yml:646`, LunaWings) или перенос слоя Tail в species-файлах.
- Часть 2 — «хвост над предметами в руках»: простым переносом слоя не решается (hand-слои динамические); потребовался бы отдельный top-слой хвоста, добавляемый при отрисовке (например, дублирующий marking на TailOversuit, вставляемый поверх hand-слоёв), — оценивать отдельно, потенциально дороже выгоды.
- Validation: ракурсы front/left/right/back с хвостом кицунэ + предмет в руке; лобби.

## Regression risk / Validation plan / Links-Related threads
Risk: перенос хвоста за спину скроет часть «wrap-around» хвостов; hand-слои поверх хвоста сохранятся без отдельной доработки.
Validation: скриншот-матчинг по 4 ракурсам; проверка, что хвост скрывается при hideLayersOnEquip (скафандры).
Related: 1379120791253418124 (первичный), 1379904937223520296, 1471561763647389861.

## Severity & complexity (if actionable)
Severity: S4 COSMETIC
Complexity: S (часть 1) / M (часть 2, если делать)
