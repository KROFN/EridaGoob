# Сломали команду mapping

## Metadata
Thread ID: 1429111124636340244 / Date: 2025-10-18 / Author: Раби Абрахам / Tags: unknown_tag / Archive state: resolved in-thread (permissions) / Messages: 13 / Attachments: 3 images (BUG_EVIDENCE: "unknown command" screenshots, not read)

## Discord evidence
- Entering the mapping command yields an "unknown command" error for the reporter.
- Replies: Vecortys — works, just tested; Дай сюда! — "маппинга нет у ролей не хост", command works if the role has the mapper admin flag, but the menu only appears for host; Vecortys — "Ишуй исключительно в правах" (the issue is purely permissions).

## Search vocabulary
User terms: команда mapping, неизвестная команда, права / Domain terms: mapping command, admin flags, mapper role, host / Prototype candidates: none / Code symbols: MappingCommand, AdminCommand, AdminFlags.Mapping, MappingScreen host check.

## Repository investigation
FILE: Content.Server/Mapping/MappingCommand.cs
SYMBOL: MappingCommand (lines 17-18, `[AdminCommand(AdminFlags.Server | AdminFlags.Mapping)]`)
WHY RELEVANT: the `mapping <id> <path>` command is an admin command requiring `Server` or `Mapping` admin flags; players/roles without those flags get the console "unknown command" treatment.
CURRENT BEHAVIOR: works for admins/mappers; "unknown command" for everyone else — by design.

FILE: Content.Server/Mapping/MappingManager.cs
SYMBOL: line 48 (`!_admin.HasAdminFlag(session, AdminFlags.Host)`)
WHY RELEVANT: the mapping menu/overlay requires the Host flag; matches "но меню только у хоста вылезать будет".

FILE: Content.Client/Mapping/MappingState.cs
SYMBOL: line 752 (`!_admin.IsAdmin(true) || !_admin.HasFlag(AdminFlags.Host)`)
WHY RELEVANT: client-side gating of the mapping UI for non-host admins.

## History
Relevant commits: 953744ba44 "Admin flags fix", 54ce591914 "20 july upstream sync (#4307)", 5946e8af71 "Command resolve and LEC conversion batch 3 (#38378)". No change that removed or broke the command around the report date; thread participants confirmed the command functions with proper flags.

## Upstream comparison
SS14 upstream: same `AdminCommand(AdminFlags.Server | AdminFlags.Mapping)` requirement. Not investigated further — local evidence sufficient.

## Verdict
USER_ERROR_OR_NOT_BUG — the command is intact; "unknown command" is the expected console response for users lacking `AdminFlags.Mapping`/`Server` (and the mapping menu additionally requires `Host`).

## Confidence
High

## Root cause
CONFIRMED: permission gate. `MappingCommand` is declared `[AdminCommand(AdminFlags.Server | AdminFlags.Mapping)]` (Content.Server/Mapping/MappingCommand.cs:17); accounts without the mapper/host admin flags cannot invoke it, which surfaces as "неизвестная команда".

## Proposed fix (do NOT implement!)
None. Optional: grant the mapping admin flag to trusted mapper roles via admin YAML, or improve the console error text for unauthorized admin-command use (robust core, out of scope).

## Regression risk / Validation plan / Links-Related threads
None. Validation: run `mapping` as host/admin → loads map; as regular player → command unavailable. Related: none.

## Severity & complexity
Severity: S4 COSMETIC / Complexity: XS

## Verdict JSON
See .ai-triage/data/verdicts/batch_B31.json
