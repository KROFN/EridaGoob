# Аннигиляция из вселенной (vampire mouse transform failed → white mouse → player annihilated)

## Metadata
Thread ID: 1379189259877093508 / Date: 2025-06-02 / Author: Фобос (communistsenko) / Tags: unknown_tag_1365427630660128861 / Archive state: archived / Messages: 4 / Attachments: 0

## Discord evidence
- «пытаясь за мышь сбежать я в итоге не превратился в мышь... А высрал белую мышь... я переместился в мышь из которой высосал кровь, а после съев последнюю мышь аннигилировался из пространства и времени, что все вещи пропали, мозга нет, тела нет»
- Expected: mouse transform turns the vampire into a mouse, with an ability to revert to human.
- Actual chain: (1) transform produced a separate WHITE mouse entity instead of transforming; (2) the player "moved into" the mouse he drank blood from; (3) after the last mouse was eaten, the player was annihilated — no body, no brain, no items.
- Admin cybertrash (2025-06-10): «Баг основного билда» — accepted as a main-build bug.

## Search vocabulary
User terms: вампир, мышь (белая), превращение, аннигиляция.
Domain terms: vampire mouse form → polymorph; revert on eat (RevertOnEat); revert on delete; white mouse = MobMouse default skin; body/mind loss.
Prototype-entity candidates: MobMouse (white), polymorph prototypes (none vampire-related in fork).
Code symbols: PolymorphSystem.OnBeforeFullySliced / Revert / OnPolymorphedTerminating / PolymorphedEntityComponent.Parent.

## Repository investigation
- FILE: Content.Server/Polymorph/Systems/PolymorphSystem.cs
  SYMBOL: OnBeforeFullySliced (L163-170) — on slicing (which accompanies eating a corpse) a live PolymorphedEntity with RevertOnEat is reverted instead of being processed.
  WHY RELEVANT: matches "съев последнюю мышь аннигилировался" — eating/slicing the mouse-form triggered Revert; the revert then had no valid original body (already consumed by the earlier failed transform chain) → permanent deletion of the character.
- FILE: Content.Server/Polymorph/Systems/PolymorphSystem.cs
  SYMBOL: PolymorphEntity (L214+) — Spawn(configuration.Entity/Entities/Groups...) + AddComp<PolymorphedEntityComponent>{Parent=uid}.
  WHY RELEVANT: explains the "высрал белую мышь" — the form entity spawns at the player's position as an independent entity; if the subsequent body replacement fails, you get a separate white mouse (MobMouse is the white/default mouse prototype) plus an unchanged player.
- FILE: Content.Server/Polymorph/Systems/PolymorphSystem.cs
  SYMBOL: Update (L95-119), OnDestruction (L176-182), OnPolymorphedTerminating (L184+)
  WHY RELEVANT: revert-on-death/delete semantics — each surviving form entity keeps triggering Revert on its shared Parent; when the parent is gone, the mind ends up nowhere ("все вещи пропали, мозга нет, тела нет").
- FILE: Resources/Prototypes/** (rg -i vampire) — no vampire role/prototypes in fork; only cosmetic items.
  WHY RELEVANT: the vampire content that wires these polymorphs is absent from the current tree, so its configuration (Cooldown, AllowRepeatedMorphs, RevertOnEat, Entity) cannot be audited.

## History
- No vampire content in any commit of this repository (all-history pathspec searches empty for vampire files; "vampire" text hits are cosmetic only).
- PolymorphSystem history contains upstream fixes (e.g. d65cf1504a "Fix nuke disk getting lost when polymorphed holder is deleted") showing this revert-chain area is a recurring bug surface.

## Upstream comparison
SS14/Goob upstream: generic polymorph revert semantics match the reported chain; no vampire gamemode in this lineage's history. Not investigated further — local evidence sufficient for the duplicate call.

## Verdict
DUPLICATE — primary thread: 1373596253383954522 (cluster vampire-polymorph-revert). Shared root cause: vampire transform action spawns the form entity without replacing the player's body, then the PolymorphSystem revert-on-death/eat/delete chain acts on the shared/original body, culminating in permanent character annihilation. Both threads describe the same failed mouse/Remilia transformation; this one adds the terminal "annihilation" outcome. Underlying content absent from current fork (hence the pair itself is CANNOT_VERIFY in this codebase — see primary dossier).

## Confidence
High (duplicate relation: identical ability, identical failure signature "spawn form without transforming", directly connected outcomes).

## Root cause
CONFIRMED (pair-level): same root cause as 1373596253383954522 — vampire polymorph configuration/desync in the live build: form entity spawned (white mouse) without body replacement; RevertOnEat/RevertOnDeath/RevertOnDelete chain then destroyed or invalidated the original body (annihilation). Mechanism mapped to present PolymorphSystem symbols; triggering vampire prototypes not in this tree (HYPOTHESIS for the exact config values).

## Proposed fix (do NOT implement!)
Handle once with the primary thread's fix (enforce transform cooldown, atomic mind transfer + original-body deletion, single-revert guard). Additionally: never set RevertOnEat on player-controlled forms whose parent body may already be deleted.

## Regression risk / Validation plan / Links-Related threads
- Validation: transform → fail path must never leave an orphan PolymorphedEntity; eat form → exactly one revert; repeated transforms blocked by cooldown.
- Related: 1373596253383954522 (primary), 1391687746484965376 (other vampire defects).

## Severity & complexity (if actionable)
Severity: S1 CRITICAL (total, unrecoverable character loss) / Complexity: M.

## Verdict JSON (also written to batch file)
see .ai-triage/data/verdicts/batch_B15.json
