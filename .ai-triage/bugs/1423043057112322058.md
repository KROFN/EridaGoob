# Багуля на карте Box, связанная целью вернуться свободным и полётом через под

## Metadata
Thread ID: 1423043057112322058 / Date: 2025-10-01 / Author: vanomorodellefake / Tags: unknown_tag / Archive state: explained in-thread / Messages: 14 / Attachments: 1 image (BUG_EVIDENCE, not read)

## Discord evidence
- Traitor agent objective "вернуться свободным" (escape alive & free) did not complete when leaving on an escape pod at round end on Box.
- Replies (Дай сюда!, Кат_Л): by design the objective counts only the emergency evacuation shuttle, not pods; an MC proposal to count pods was rejected; "сам код написан так, чтоб цель засчитывалась только от эвака".

## Search vocabulary
User terms: цель вернуться свободным, под, эвак / Domain terms: escape shuttle condition, escape pod, emergency shuttle / Prototype candidates: corporageagent objectives / Code symbols: EscapeShuttleConditionSystem, EmergencyShuttleSystem.IsTargetEscaping, EmergencyShuttleComponent.

## Repository investigation
FILE: Content.Server/Objectives/Systems/EscapeShuttleConditionSystem.cs
SYMBOL: GetProgress (lines 28-41)
WHY RELEVANT: computes objective progress; explicit comment on line 39: "Any emergency shuttle counts for this objective, but not pods."
CURRENT BEHAVIOR: progress = 1.0 only if `_emergencyShuttle.IsTargetEscaping(mind.OwnedEntity)`.

FILE: Content.Server/Shuttles/Systems/EmergencyShuttleSystem.cs
SYMBOL: IsTargetEscaping (lines 654-667)
WHY RELEVANT: returns true only if `EmergencyShuttleArrived` and the player's grid has `EmergencyShuttleComponent`. Escape pods are separate grids without that component → pods never complete the objective.
CURRENT BEHAVIOR: pod escape yields 0 (or 0.5 when cuffed) — exactly what the reporter saw.

FILE: Resources/Prototypes/_Goobstation/Objectives/corporageagent.yml
SYMBOL: `type: EscapeShuttleCondition` (line 54)
WHY RELEVANT: the agent ("корпоративный агент") objective in question uses this condition; behavior is not Box-specific — same on every map.

## History
No relevant commits found (objective condition stable; not changed around report date).

## Upstream comparison
SS14 upstream: same system and same "shuttle-only" semantics; upstream comment identical. Not investigated further — local evidence sufficient.

## Verdict
CONFIG_OR_CONTENT_ISSUE — works as coded; pods are deliberately excluded from the "escape" objective. Making pods count would be a design change (already rejected on MC per thread).

## Confidence
High

## Root cause
CONFIRMED (as designed): `EscapeShuttleConditionSystem.GetProgress` → `EmergencyShuttleSystem.IsTargetEscaping` requires the player's grid to carry `EmergencyShuttleComponent` and the evac to have arrived; escape pods (separate grids) never satisfy it, so the objective stays incomplete.

## Proposed fix (do NOT implement!)
If the design decision changes: extend `IsTargetEscaping` (EmergencyShuttleSystem.cs:654) to also accept grids with an escape-pod marker component that have FTL'd to centcomm/away, or tag pods with `EmergencyShuttleComponent` at round end. Otherwise: update wiki/objective text to say "улететь на эвакуационном шаттле".

## Regression risk / Validation plan / Links-Related threads
Counting pods would trivialize the objective (pods launch early and survive). Validation: complete round on pod as agent → objective stays 0; on evac → 1. Related: none.

## Severity & complexity
Severity: S4 COSMETIC / Complexity: S

## Verdict JSON
See .ai-triage/data/verdicts/batch_B31.json
