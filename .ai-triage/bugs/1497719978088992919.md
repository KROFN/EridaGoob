# Все действия в космосе прерываются если летишь по инерции

## Metadata
Thread ID: 1497719978088992919 / Date: 2026-04-25 / Author: Mokko (mokko1111) / Tags: Незначительный, Визден-Баг / Archive state: archived / Messages: 5 / Attachments: 0

## Discord evidence
- [SRC] Root: "Нельзя не вещи снять с людей, ничерта" — в космосе, летя по инерции, все действия прерываются; нельзя даже снять предметы с людей.
- [SRC] alexfire: лечиться можно, но "закидывать патроны тоже нельзя". Странная избирательность.
- [SRC] Mokko: просит убрать это в космосе — без гравы можно удерживать предметы силой, но с джетпаком в космосе это трудновыполнимо.
- [SRC] alexfire: побочный вопрос про джетпак на станции без гравитации (не относится к root cause).
- Медиа: отсутствует.

## Search vocabulary
User terms: инерция, космос, снять вещи, патроны, джетпак / Domain terms: DoAfter, BreakOnMove, weightless, inertia drift, strip, insert ammo / Prototype-entity candidates: — / Code symbols: DoAfterArgs.BreakOnMove, SharedStrippableSystem, SharedDoAfterSystem

## Repository investigation
- FILE: Content.Shared/Strip/SharedStrippableSystem.cs [SRC]
  SYMBOL: DoAfterArgs — `BreakOnMove = true` в 4 местах (строки 229, 332, 436, 546)
  WHY RELEVANT: снятие/перестановка предметов с игроков и вставка предметов — это DoAfter'ы с BreakOnMove=true.
  CURRENT BEHAVIOR: любой тик движения отменяет DoAfter; при полёте по инерции игрок движется постоянно → DoAfter не может завершиться.
- FILE: Content.Shared/DoAfter/ (upstream-стандарт) [INFER]
  SYMBOL: SharedDoAfterSystem — обработка BreakOnMove через подписку на MoveEvent
  WHY RELEVANT: механизм отмены при движении находится здесь; код не открыван в рамках бюджета.
- Объясняет избирательность: лечение (medical DoAfter) не имеет BreakOnMove=true → "лечится можно", а стрипп/вставка магазина — нет. [INFER]

## History
- [HIST] a7edf47940 "Second inventory (#11)" — форковое изменение, трогало SharedStrippableSystem.cs; остальные коммиты — upstream-линия (06b00b9795, a541e2d0f7, fa4ad09bfe, 68b7977bee).
- Релевантных коммитов после даты репорта (2026-04-25), меняющих BreakOnMove-логику, в логе не видно.

## Upstream comparison
SS14 upstream: поведение "strip DoAfter отменяется движением" — стандартное (BreakOnMove=true в upstream StrippableSystem) [UP]. Goob: не отличается. Erida: не найдено форк-специфичной отмены.

## Verdict
LIKELY_CURRENT_BUG — код-путь доказан (BreakOnMove=true на всех стрипп-DoAfter'ах), недостающее звено — точная строка отмены в SharedDoAfterSystem (не открыта в рамках бюджета). По сути это дизайн-пробел: непроизвольное движение по инерции не отличимо от произвольного ходьбы.

## Confidence
Medium

## Root cause
HYPOTHESIS (Medium): DoAfter'ы с BreakOnMove=true (Content.Shared/Strip/SharedStrippableSystem.cs:229/332/436/546) отменяются потоком MoveEvent при инерционном дрейфе; система не различает "иду сам" и "меня несёт". Отсюда: снять/повесить предметы и вставить патроны невозможно, а лечение (без BreakOnMove) работает.

## Proposed fix (do NOT implement!)
- Content.Shared/DoAfter/SharedDoAfterSystem.cs: при отмене по BreakOnMove пропускать отмену, если сущность weightless и не имеет активного input-движения (velocity только от инерции; см. input mover / RelativeVelocity ниже порога).
- Либо минимально: BreakOnMove=false для стрипп-аргументов (SharedStrippableSystem.cs), если дизайн позволяет.
- Почему фиксит: устраняет источник постоянных MoveEvent-отмен при дрейфе.

## Regression risk / Validation plan / Links-Related threads
Risk: отключение BreakOnMove в гравитации позволит "бегать и стриппить" — нужен порог/условие weightless+no-input. Validation: дрейфуя на джетпаке, начать снимать предмет с игрока и вставлять магазин — DoAfter должен завершаться; на гравитации поведение не меняется. Related: — (возможно кластер с "aiming jitter" B35, не подтверждено).

## Severity & complexity (if actionable)
Severity: S3 MINOR / Complexity: M

## Verdict JSON (also written to batch file)
см. .ai-triage/data/verdicts/batch_B34.json
