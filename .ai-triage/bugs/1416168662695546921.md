# Ии и голопад (AI cannot understand language when entering holopad)

## Metadata
Thread ID: 1416168662695546921 / Date: 2025-09-12T21:08Z / Author: Parodist (papjijict) / Tags: unknown_tag_1414266059208265799 / Archive state: archived / Messages: 1 / Attachments: 1 (PNG screenshot)

## Discord evidence
- [SRC] Root: "Ии не понимает язык при вхождении в голопад" — the AI does not understand (players') language when entering a holopad. No replies, no reproduction steps, no error text quoted.
- [MEDIA] attachments/1416168662695546921/1416168663610036295_image.png — screenshot (presumably AI chat showing "unknown language" lines); not renderable in triage context. Classification: tentative BUG_EVIDENCE.

## Search vocabulary
User terms: голопад, цИИ, не понимает язык / Domain terms: holopad, hologram projection, language system, understood languages, speech relay / Prototype-entity candidates: hologram prototype (HolopadComponent.HologramProtoId) / Code symbols: HolopadSystem, HolopadUserComponent, HolopadHologramComponent, Einstein Engines Language

## Repository investigation
FILE: Content.Server/Holopad/HolopadSystem.cs
SYMBOL: hologramUid = Spawn(entity.Comp.HologramProtoId, Transform(entity).Coordinates) (line 539); LinkHolopadToUser → AddComp<HolopadUserComponent>(user) (573-574); OnHolopadUserInit/OnHolopadUserShutdown (334-359); `using Content.Shared.Chat; // Einstein Engines - Language` [SRC]
WHY RELEVANT: [SRC] when an AI uses a holopad, the system spawns a SEPARATE hologram entity from HologramProtoId and marks the AI as HolopadUser; the file explicitly integrates with the fork's language/chat stack.
CURRENT BEHAVIOR: [INFER] speech picked up at the holopad is relayed to the AI through the spawned hologram; if language understanding is evaluated against the hologram entity (spawned from a prototype that does not carry the AI's understood languages) or the relay path bypasses the AI's language components, the AI sees "speaks an unknown language" while on the holopad. NOT PROVEN — the speech-relay and language-check code was not read within budget.

## History
[HIST] Not checked (budget). Report date 2025-09-12 — fresh at export time; no fix mentioned anywhere in thread.

## Upstream comparison
[UP] Not investigated — language understanding is an Einstein Engines/Goob-side feature; upstream holopads have no language concept, so upstream comparison is moot.

## Verdict
CANNOT_VERIFY (plausible causal chain exists but the relay/language code path was not verified; single report, no repro steps, media not inspectable)

## Confidence
Low

## Root cause
HYPOTHESIS (Low): language understanding for speech relayed via holopad is resolved on the spawned hologram/proxy entity instead of the AI user entity, and the hologram prototype lacks understood-language components → AI cannot comprehend speech while linked to the holopad.

## Proposed fix (do NOT implement!)
- In HolopadSystem speech/relay path (find TransformSpeech/Listen handlers around HolopadUserComponent usage), resolve language skills from HolopadUserComponent.HolopadUser (the AI) rather than the hologram entity; alternatively give the hologram prototype inherited/copied language components from the linking user.

## Regression risk / Validation plan / Links-Related threads
Risk: touches speech relay for all holopad users (AI + humans). Validation: AI enters holopad, human speaks near the holopad → AI chat shows normal text; hologram speech still works. Related threads: none in batch.

## Severity & complexity (if actionable)
Severity: S3 MINOR / Complexity: M

## Verdict JSON (also write to batch file)
Covered by .ai-triage/data/verdicts/batch_B18.json
