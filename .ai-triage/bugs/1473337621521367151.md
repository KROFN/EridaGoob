# Нет направленных урановых стекол в меню крафта

## Metadata
Thread ID: 1473337621521367151 / Date: 2026-02-17 / Author: maasarie / Tags: unknown_tag_1414266059208265799 / Archive state: archived / Messages: 1 / Attachments: 0

## Discord evidence
- Title: "Нет направленных урановых стекол в меню крафта" (no directional uranium glass in the craft menu). Root text «Ну да» adds nothing.
- Expected: directional uranium window + reinforced directional uranium window craftable like other directional windows. Actual: absent from construction menu. No media, no replies.

## Search vocabulary
User terms: направленные урановые стёкла / Domain terms: directional window, uranium glass, construction recipe / Prototype-entity candidates: UraniumWindowDirectional, UraniumReinforcedWindowDirectional / Code symbols: graph WindowDirectional, nodes uraniumWindowDirectional, uraniumReinforcedWindowDirectional

## Repository investigation
1. [SRC] FILE: Resources/Prototypes/Recipes/Construction/Graphs/structures/windowdirectional.yml
   SYMBOL: graph `WindowDirectional`; edges `to: uraniumWindowDirectional` (L33-37, material UraniumGlass), `to: uraniumReinforcedWindowDirectional` (L39-43, material ReinforcedUraniumGlass); nodes L141-142 (`entity: UraniumWindowDirectional`), L160-161 (`entity: UraniumReinforcedWindowDirectional`)
   WHY RELEVANT: the build STEPS exist — players can make these windows in-world from glass on a frame — but the graph alone does not create menu entries.
   CURRENT BEHAVIOR: graph is complete and loadable.
2. [SRC] FILE: Resources/Prototypes/Recipes/Construction/structures.yml
   SYMBOL: recipes for graph WindowDirectional: `WindowDirectional` (L444-447), `WindowReinforcedDirectional` (L457-460), `WindowClockworkDirectional` (L470-473), `PlasmaWindowDirectional` (L483-486), `PlasmaReinforcedWindowDirectional` (L496-499). Exhaustive `rg 'Directional'` shows NO `UraniumWindowDirectional` / `UraniumReinforcedWindowDirectional` recipe ids.
   WHY RELEVANT: the construction menu lists recipes (ConstructionPrototype), not graph nodes. Without a recipe with `graph: WindowDirectional` + `targetNode: uraniumWindowDirectional`, the item never appears in the menu.
   CURRENT BEHAVIOR: directional uranium windows are craftable in-world but invisible in the menu — exactly the report.
3. [SRC] Entities exist and are valid targets: Resources/Prototypes/Entities/Structures/Windows/uranium.yml:62 `UraniumWindowDirectional`; ruranium.yml:61 `UraniumReinforcedWindowDirectional`.
4. [SRC] Precedent: Erida already added the missing recipes for DIAGONAL uranium windows — structures.yml:537-541 `UraniumWindowDiagonal` and L551-555 `ReinforcedUraniumWindowDiagonal`, both with explicit `name:` loc keys present in Resources/Locale/{en-US,ru-RU}/construction/recipes/structures.ftl:7-8. Same pattern was simply not applied to directional.

## History
[HIST] git log --since=2026-02-01 on structures.yml + windowdirectional.yml: only upstream-sync style commits (1f9241fca2 "hello (#10)", 06b00b9795 "Nuke SPDX…", 7dff4c9517 "bonfire…"). No commit adds directional uranium recipes after the report → still missing at HEAD.

## Upstream comparison
Not investigated — local evidence sufficient; the gap is fork-specific recipe bookkeeping (graph+entities are upstream-complete, only recipe rows absent).

## Verdict
CONFIRMED_CURRENT_BUG
(code path: menu lists recipes from structures.yml → graph WindowDirectional lacks any recipe with targetNode uraniumWindowDirectional/uraniumReinforcedWindowDirectional → items missing from craft menu; entities and graph steps proven present.)

## Confidence
High

## Root cause
CONFIRMED: Missing ConstructionPrototype entries `UraniumWindowDirectional` and `UraniumReinforcedWindowDirectional` in Resources/Prototypes/Recipes/Construction/structures.yml for graph `WindowDirectional`, while nodes/entities/materials (UraniumGlass, ReinforcedUraniumGlass) all exist (windowdirectional.yml:33-43,141,160; uranium.yml:62; ruranium.yml:61).

## Proposed fix (do NOT implement!)
Add two recipes to structures.yml next to the other WindowDirectional recipes, mirroring the UraniumWindowDiagonal block (L537-555):
`- id: UraniumWindowDirectional` → graph: WindowDirectional, targetNode: uraniumWindowDirectional, category: construction, and `- id: UraniumReinforcedWindowDirectional` → targetNode: uraniumReinforcedWindowDirectional; add `name: construction-recipe-uranium-window-directional` / `…-reinforced-uranium-window-directional` + loc keys in en-US/ru-RU structures.ftl (optional — entity names also resolve). Why it fixes: menu enumerates recipes; with rows present, nodes become reachable and visible.

## Regression risk / Validation plan / Links-Related threads
- Risk: minimal — additive YAML; keep icon/entity fields consistent with siblings.
- Validation: in game, construction menu → Windows category → directional subsection shows both uranium entries; craft one on a girder.
- Related: B32 "directional windows tiles" (different topic); none shared.

## Severity & complexity (if actionable)
Severity: S3 MINOR / Complexity: XS

## Verdict JSON (also written to batch file)
CONFIRMED_CURRENT_BUG, High, S3, XS
