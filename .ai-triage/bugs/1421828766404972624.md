# ТТС рита (голос «Рита» есть у медборгов, нельзя выбрать в кастомизации)

## Metadata
Thread ID: 1421828766404972624 / Date: 2025-09-28 / Author: Medik (medik.alt) / Tags: unknown_tag_1414266059208265799 / Archive: archived / Messages: 3 / Attachments: 1 (image.png)

## Discord evidence
- Отчёт: «Существует в сборке ттс речевой информатор "Рита", можно встретить у медборгов, но в кастомайзе у игроков добавить невозможно, как-то можно поправить?»
- Ответ Vecortys: «ТТС бывает специализированный. И это один из них» — модерация считает поведение намеренным [HIST].
- image.png — BUG_EVIDENCE по описанию (голос у медборга), НЕ просмотрен [?].

## Search vocabulary
User terms: ТТС рита / медборги / кастомайз → Domain terms: TTS voice, silicon/borg announcer voice, voice picker
Prototype-entity candidates: ttsVoice / Code symbols: TTSVoicePrototype, TTSTab.LoadVoices, tts-voices.yml

## Repository investigation
FILE: Resources/Prototypes/_Erida/Voice/tts-voices.yml
SYMBOL: ttsVoice ids (Aidar, Baya, Kseniya, Xenia, Eugene, Arthas, Thrall, Kael, Maiev, Rexxar, Tyrande, Furion, Illidan, Kelthuzad, Jaina, Ladyvashj, Narrator, Cairne, Garithos, Anubarak, ...)
WHY RELEVANT: полный список выбираемых голосов. CURRENT BEHAVIOR: идентификатора «Rita» в yml НЕТ (case-sensitive grep "Rita" по всему репо не даёт совпадений, кроме датасетов имён vox) [SRC].

FILE: Content.Client/_CorvaxGoob/TTS/TTSTab.xaml.cs
SYMBOL: TTSTab.LoadVoices / UpdateResults (строки 39–124)
WHY RELEVANT: пикер перечисляет ВСЕ ttsVoice-прототипы без фильтров. CURRENT BEHAVIOR: если бы «Рита» была ttsVoice-прототипом — она была бы выбираемой; она не выбираема, потому что её нет в списке прототипов [SRC].

FILE: Content.Shared/_CorvaxGoob/TTS/TTSVoicePrototype.cs
SYMBOL: [Prototype("ttsVoice")]
WHY RELEVANT: класс прототипа — поле «спец-голос для кремния» отсутствует [SRC].

FILE: git -S'Rita' (pickaxe)
SYMBOL: коммит 5aa9874e56 "Tts (#9)"
WHY RELEVANT: строка «Rita» исторически существовала в репозитории (список голосов Corvax-импорта) и исчезла при переработке TTS — «Рита» у медборгов это либо остаточный хардкод голоса информатора, либо сохранённый в старых профилях id, который EnsureValid отбрасывает [HIST]+[INFER].

## History
5aa9874e56 "Tts (#9)" (до отчёта) — в нём менялось наличие 'Rita' в кодовой базе; 1d0d909af2 "Tts (#28)" — переработка списка голосов на текущий набор. После отчёта (2025-09-28) TTS-голоса не менялись [HIST].

## Upstream comparison
Corvax-Goob: «Rita» — стандартный голос информатора/боргов в оригинальном наборе TTS; в Erida список голосов заменён (Silero + Warcraft-имена), Rita из выбираемых удалена [INFER].

## Verdict
CONFIG_OR_CONTENT_ISSUE (голос у медборгов задаётся отдельно от списка ttsVoice; «специализированный» TTS — заявленное намерение; добавление «Риты» в кастомизацию — контентное решение: вернуть prototype в tts-voices.yml)

## Confidence
Medium (сам голос медборгов не локализован: строка «Rita» в текущем дереве отсутствует, источник озвучки медборгов не найден [?])

## Root cause
CONFIRMED (как «работает как задумано»): выбираемые голоса = прототипы ttsVoice в Resources/Prototypes/_Erida/Voice/tts-voices.yml; «Рита» там отсутствует, поэтому TTSTab её не показывает. Голос медборгов — отдельный (спец) канал, не попадающий в пикер.

## Proposed fix (do NOT implement!)
Если решено открыть голос игрокам: добавить в tts-voices.yml `- type: ttsVoice { id: Rita, name: ... }` (плюс ресурсы синтеза на стороне tts.api_url) — TTSTab подхватит автоматически. Если оставить спец-голосом — закрыть тред как intented.

## Regression risk / Validation plan / Links-Related threads
Риск: XS-минимальный (добавление прототипа). План: голос появляется в пикере, превью работает, медборги не меняют поведение. Related: 1365367741560913970 (подписные голоса), cluster tts-voice-availability.

## Severity & complexity
Severity: S4 COSMETIC / Complexity: S
