# WL без WL (роли с WL недоступны, вайтлиста нет)

## Metadata
Thread ID: 1365393402589614209 / Date: 2025-04-25 / Author: PuroSlavKing (puroslavking) / Tags: «В процессе» / Archive state: archived / Messages: 4 / Attachments: 1 (image0.gif — REACTION_MEME от abama, не баг-эвиденс)

## Discord evidence
- Root: «Невозможно играть за роли требующие WL, потому-что на сервере нету WL».
- abama: «Потомучто ждать надо» + GIF-реакция.
- Kreyd (staff): «В процессе» — т.е. система WL ещё разрабатывается/внедряется.
- Не указано: какие именно роли, что показывает UI, как выглядит «требование WL». Деталей для воспроизведения нет.

## Search vocabulary
User terms: WL (whitelist), роли, сервер.
Domain terms: job whitelist, role gating, playtime requirements.
Prototype-entity candidates: job prototypes (_Erida/_Goobstation/Roles).
Code symbols: JobWhitelist / WhitelistSystem (ожидаемые имена — НЕ найдены).

## Repository investigation
- FILE: Content.Server/**, Content.Shared/** (rg "Whitelist" -g '*.cs')
  SYMBOL: отсутствует какой-либо RoleWhitelist/JobWhitelist system — все совпадения относятся к другому: SharedHandLabelerSystem, TraitPrototype, ChatSystem, ObjectiveBlacklistRequirementSystem, EntityWhitelist (item-gating) и т.п.
  WHY RELEVANT: в кодовой базе НЕТ механики «роль по вайтлисту игрока» — ни проверки, ни команд выдачи WL, ни UI-локов.
  CURRENT BEHAVIOR: роли не могут быть ограничены WL-механикой на уровне кода форка.
- FILE: Resources/Prototypes/_Erida/** (rg "whitelist" -g '*.yml')
  SYMBOL: только EntityWhitelist в прототипах предметов (inventory templates, underwear, voice, borg) — это списки допустимых предметов/состояний, не ролевой WL.
  WHY RELEVANT: подтверждает отсутствие ролевого WL и в данных.
- FILE: git log --all --grep=whitelist (опция role-WL)
  SYMBOL: коммитов, добавляющих ролевой вайтлист, нет (совпадения — про belts/boxes/entity whitelist cleanup).
  CURRENT BEHAVIOR: фича не существовала и не добавлялась.

## History
- no relevant commits found: ни до, ни после репорта (25.04) коммитов с ролевым вайтлистом.
- Тег треда «В процессе» и ответ staff «ждать надо» согласуются с «фичи ещё нет».

## Upstream comparison
SS14: ролевой WL (account whitelist) — серверная опция вне контентного кода; Corvax/Wizden имеют job-whitelist фичи, не портированные сюда. Goob/Erida upstream: ролевого WL не найдено. Не углублялся — локальных данных достаточно.

## Verdict
CANNOT_VERIFY — заявленная блокировка «роли требуют WL» не может быть воспроизведена из кода: механики ролевого WL в форке не существует вообще, значит «роль нельзя взять из-за WL» в текущем дереве необъяснима. Вероятно, репорт описывает отсутствующую фичу (запрос на внедрение WL) и/или серверные admin-конфиги вне репозитория. Информации о конкретных ролях нет.

## Confidence
Medium (отсутствие системы в дереве верифицировано полнотекстовым поиском; причина наблюдения автора — за пределами репозитория).

## Root cause
HYPOTHESIS (Low-Medium): заявка = фича-реквест. Либо роли, о которых говорит автор, заблокированы средствами вне репо (admin-конфиг/билд сервера старше дерева), либо автор требует внедрить WL, которого «ещё нет» (согласуется с «В процессе»/«ждать надо»).

## Proposed fix (do NOT implement!)
- Если решено вводить WL: реализовать JobWhitelist (серверные команды выдачи + проверка при ApplyToJob + UI-хинт) — места: Content.Server/GameTicking (JobQueue/ApplyJob), Shared prefs UI; конфиг-квары.
- До внедрения:Roles не помечать в UI как «требующие WL», чтобы не вводить игроков в заблуждение.

## Regression risk / Validation plan / Links-Related threads
- Риск: WL может «запереть» роли при отсутствии админ-инструментов выдачи.
- Валидация: выдать WL тестовому аккаунту, проверить доступ к роли и отказ без WL.
- Related: — 

## Severity & complexity (if actionable)
Не подтверждён как баг. Если делать фичу: Severity S3 / Complexity M-L.
