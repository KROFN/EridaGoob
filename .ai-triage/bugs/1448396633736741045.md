# Направленные окна и плитка (directional windows: stacking fixed, floor tiles next to them broken)

## Metadata
Thread ID: 1448396633736741045 / Date: 2025-12-10 / Author: Пряники (fox_meowder)
Tags: На рассмотрении + unknown_tag / Archive: archived / Messages: 8 / Attachments: 1 (image.png webp screenshot — [MEDIA] BUG_EVIDENCE, not rendered per budget rule)

## Discord evidence
- "Визы починили стак окон, но теперь сломали плитки рядом с ними... будто так быть не должно." — WizDen fixed directional-window stacking, but tiles adjacent are now broken.
- Clarified by Yosif question + author "ага": **if a directional window is placed, you cannot remove (pry) the floor tile** (under/next to it).
- Author: "раньше и стакать можно было… типа в одном месте по 8 и больше окон" — previously windows stacked 8+ on one tile; tile removal worked before the fix. Media: screenshot (not rendered).

## Search vocabulary
User terms: "окна/плитка/снять плитку/стак" / Domain terms: directional window, floor tile prying, anchored structure, placement snap / Prototype candidates: WindowDirectional / Code symbols: WindowDirectional graph, TilePrying (not located in budget), anchor.

## Repository investigation
FILE: Resources/Prototypes/Entities/Structures/Windows/window.yml:146-238
SYMBOL: `WindowDirectional` — parent `[BaseDeltaPressureGlassQuarter, BaseStructure]`; `placement: mode: SnapgridCenter, snap: [Window]`; `WallMount arc 360`; `Fixtures` Aabb `-0.5,-0.5,0.5,-0.28125` mask FullTileMask; `Airtight airBlockedDirection [South]`; `Construction graph WindowDirectional`.
WHY RELEVANT: [SRC] current prototype state after the WizDen stacking fix: the window is a structure anchored on the tile edge; there is no visible exemption allowing tile deconstruction under/next to an occupied edge.

FILE: git log Resources/Prototypes/Entities/Structures/Windows/window.yml
SYMBOL: commits `06b00b9795` (Nuke SPDX), `ea42196c9c` (Atmos dP Window Inheritance #40192), `3cfecc0c91` (dP YAML refactor), `3f49c129d1` (dP Window Shattering #39238), `219092b209` (Re-anchorable structures #39542), `54ce591914` (20 july upstream sync)
WHY RELEVANT: [HIST] no isolated commit identifiable as the "window stacking fix" in this file's recent log — it likely arrived inside a bulk upstream sync; no post-report-date change found on window.yml.

FILE: Content.Shared/Construction + Content.Server/Construction (grep `diagonal|DirectionalWindow`)
SYMBOL: no dedicated stacking/anti-stack code found in construction systems within budget.
WHY RELEVANT: [INFER] the anti-stack behavior comes from placement/snap rules of `WindowDirectional` (WizDen-side), and the same class of structures is what floor-tile prying treats as blocking.

## History
Relevant commits: see git log above; none after the report date (2025-12-10) in this file. [HIST] no revert/fix of the tile-pry regression found in current fork.

## Upstream comparison
WizDen: stacking fix is upstream behavior the fork synced ("визы починили"). Upstream follow-up (allow tile prying under directional windows) not verified — budget.

## Verdict
LIKELY_CURRENT_BUG (regression caused by the WizDen directional-window stacking fix: anchored directional window now blocks floor-tile prying on its tile)

## Confidence
Medium

## Root cause
HYPOTHESIS (Medium): the stacking fix made `WindowDirectional` a proper anchored, snapping structure (window.yml:146-238); floor-tile prying refuses tiles occupied by anchored structures → with a directional window on a tile edge the tile can no longer be pried, whereas pre-fix unanchored/stacked windows did not block it. Exact pry-blocker code path not located within budget.

## Proposed fix (do NOT implement!)
Locate the tile-pry obstruction check (crowbar/floor-tile deconstruction path) and exempt `WallMount`/edge-mounted directional windows (or check only full-tile blockers, e.g. ignore fixtures whose Aabb doesn't cover tile center). Files: tile prying system (Content.Server/Content.Shared tools-tile area) + possibly `WindowDirectional` prototype.

## Regression risk / Validation plan / Links-Related
Risk: low. Validate: place directional window → crowbar tile under/behind it → must be removable; verify windows still can't stack 2+ per edge; airtightness intact. Related: B19 windows/collision batch.

## Severity & complexity
Severity: S3 MINOR (construction/deconstruction QoL regression) / Complexity: S
