# Дверки

## Metadata
Thread ID: 1483498644257640691 / Date: 2026-03-17 / Author: Mokko / Tags: Незначительный, Визден-Баг / Archive state: archived / Messages: 2 / Attachments: 1 (image.png — BUG_EVIDENCE, не читал: бюджет)

## Discord evidence
- "Один перезаход делает дверки постоянно открывающимися (именно огоньки открытия зелёные) - второй фиксит это."
- Т.е. после одного релогина у (закрытых) дверей постоянно горят зелёные «огоньки открытия»; второй перезаход возвращает нормальный вид.

## Search vocabulary
User terms: дверки, огоньки открытия зелёные / Domain terms: airlock unlit lights layer, DoorState, AppearanceChangeEvent / Prototype-entity candidates: AirlockComponent (OpeningSpriteState/OpenUnlitVisible) / Code symbols: AirlockSystem.OnAppearanceChange, DoorVisualLayers.BaseUnlit, OpeningAnimation

## Repository investigation
FILE: Content.Client/Doors/AirlockSystem.cs
SYMBOL: OnAppearanceChange + switch(state) (BaseUnlit layer)
WHY RELEVANT: именно этот код до фикса мапил состояния двери на слой «огоньков».
CURRENT BEHAVIOR (ДО фикса bc02215fde): `case DoorState.Open: BaseUnlit → comp.ClosingSpriteState`, `case DoorState.Closed: BaseUnlit → comp.OpeningSpriteState` — при ресинке внешности (перезаход) закрытой двери слой огоньков получал спрайт OPENING-состояния (зелёные огоньки) и оставался в нём, пока видимость слоя не пересчитается снова (второй релогин/новое AppearanceChange). Открытие: нестандартные flick-анимации Unlit-слоя (строки 33–66) без ключевого кадра Open/Closed состояния усугубляли залипание.
FILE: Content.Client/Doors/DoorSystem.cs
SYMBOL: OnAppearanceChange / UpdateAppearanceForDoorState
WHY RELEVANT: общий механизм DoorVisuals.State на релогине пересоздаёт визуал дверей — точка входа рассинхрона.

## History
- bc02215fde 2026-08-14 "content port (#15)" — `git show`: правки ТОЛЬКО Content.Client/Doors/AirlockSystem.cs (+25/-8): «erida edit» — теперь `Open → OpenSpriteState`, `Closed → ClosedSpriteState`, добавлены конечные ключевые кадры в opening/closing flick-анимации и условие `state == DoorState.Closed && comp.OpenUnlitVisible` в расчёте видимости. Коммит ПОСЛЕ репорта (2026-03-17) и адресует ровно инвертированные огоньки.
- 804b7711f8, 90889ccb3d и др. — не относятся к визуалу огоньков.

## Upstream comparison
Upstream (dead-space-server/EridaGoob, git log upstream/master -- Content.Client/Doors): те же коммиты; инвертированный маппинг был унаследованной локальной правкой/портом — в текущем форке уже заменён «erida edit»-фиксом.

## Verdict
ALREADY_FIXED_IN_CURRENT_FORK — commit bc02215fde (2026-08-14) изменил маппинг Open/Closed состояний на слое BaseUnlit и анимационные ключевые кадры.

## Confidence
Medium — старый код объективно рисовал OpeningSpriteState на закрытых дверях при ресинке; сценарий «первый релогин ломает, второй чинит» согласуется с односторонним AppearanceChange-ресинком, но видео/логов для 100% воспроизведения нет.

## Root cause
CONFIRMED (код): до bc02215fde клиентский `AirlockSystem.OnAppearanceChange` при `DoorState.Closed` ставил на DoorVisualLayers.BaseUnlit спрайт `OpeningSpriteState` (зелёные огоньки) → после перезахода (пересоздание appearance-стейта) двери «постоянно открывающиеся»; второй релогин переинициализировал слой заново.

## Proposed fix (do NOT implement!)
Не требуется — реализовано в bc02215fde. При регрессии: держать маппинг Open/Closed → Open/ClosedSpriteState и конечные кадры flick-анимаций.

## Regression risk / Validation plan / Links-Related threads
Валидация: релогин рядом с закрытыми шлюзами — огоньки не горят; открытие/закрытие — анимация огоньков завершается в правильное состояние.

## Severity & complexity
Severity: S4 COSMETIC (в репорте тег «Незначительный») / Complexity: S (уже сделано)
