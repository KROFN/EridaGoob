# Баг со столами и закрытыми кнопками (ползком через раздвижное окно)

## Metadata
Thread ID: 1388575425562673282 / Date: 2025-06-28 / Author: Дай сюда! (daisuda)
Tags: «На рассмотрении» / Archive: archived / Messages: 2 / Attachments: 6 (PNG-скриншоты)

## Discord evidence
- «Если встать на стол, лечь и просто пройти дальше, то раздвижное [любое] окно просто
  пропускает дальше, потому что стол выталкивает во время падения на пол, позволяя
  попасть спокойно за него» — [MEDIA] 6 PNG (не просмотрены: изображения недоступны в
  контексте саб-агента; классифицированы как BUG_EVIDENCE по описанию).
- Реплай NickCatXD1 (2025-07-08): подтверждает, багользуют «черти».
- Второй баг в том же треде: «закрытая кнопка»: правый клик → в меню только «попытка
  открыть», но левый клик активирует кнопку, попутно показывая «доступ запрещён»
  (обход access-проверки кнопки). Отдельно не репортился.

## Search vocabulary
User terms: стол, лечь, пройти, раздвижное окно, закрытая кнопка, доступ запрещён /
Domain terms: downed movement, table eject, windoor fixture tunneling, sliding, access
reader on switch / Prototype-entity: Windoor, BaseStructureTables / Code symbols:
MobStateSystem (CollisionWake), SlidingComponent/SlidingSystem, StandingStateSystem,
SharedDoorSystem fixtures, SignalSwitchSystem, AccessReader

## Repository investigation
FILE: Content.Shared/Mobs/Systems/MobStateSystem.Subscribers.cs
SYMBOL: OnStateExits/OnStateEntered — EnsureComp<CollisionWakeComponent>(target) при
MobState.Dead (строки 126–133), RemComp при выходе из Dead (строка 91).
WHY RELEVANT: система коллизий тела в зависимости от состояния; для живого-but-downed
коллизии сохраняются, значит проход сквозь windoor — не «слой коллизии», а физический
протык (tunneling).
FILE: Resources/Prototypes/Entities/Structures/Doors/Windoors/windoor.yml
SYMBOL: BaseWindoor — тонкая дверная фикстура windoor (толщина ~0.1 тайла).
WHY RELEVANT: тонкая фикстура + дискретный шаг физики = протык при импульсе.
FILE: Content.Shared/Slippery/SlidingComponent.cs, SlidingSystem.cs
SYMBOL: SlidingComponent — состояние «скольжения» после падения.
WHY RELEVANT: падение со стола уходит в slide с заметной скоростью; «стол выталкивает
во время падения» совпадает с eject-импульсом + скольжение → тела на скорости проходят
сквозь тонкую фикстуру закрытого windoor. [INFER]
FILE: Content.Server/DeviceLinking/Systems/SignalSwitchSystem.cs
SYMBOL: SignalSwitch (кнопки) — доступ-проверку в этом системe не проверял; под-баг с
кнопкой не локализован [?]. Кандидаты: SignalSwitchSystem, AccessReader-обвязка кнопок.

## History
git log -- Content.Shared/Doors/: с даты репорта только upstream-мержи (bolting, emag,
TimeSpan) — проверку/фикстуры windoor не меняли. Слаг не найден; фикс отсутствует.

## Upstream comparison
Wizden: известное семейство багов «downed/скоростные тела протыкают тонкие двери/окна»;
в Erida не патчилось. (не исследовалось детально — локальных доказательств достаточно)

## Verdict
LIKELY_CURRENT_BUG (часть 1 — проход через windoor). Часть 2 (кнопка/доступ) —
CANNOT_VERIFY внутри этого досье; требует отдельного треда-репорта.

## Confidence
Medium

## Root cause
HYPOTHESIS: комбинированная физическая цепочка «встал на стол → лёг → стол выталкивает
тело в момент fall/slide (SlidingSystem) → импульс + дискретная физика протыкает тонкую
фикстуру закрытого windoor». Downed-тело коллизии сохраняет (MobStateSystem.Subscribers),
значит это tunneling, а не отключение слоёв. (confidence: Medium)

## Proposed fix (do NOT implement!)
- Движок/контент: включить continuous collision detection (CCD) для тел в состоянии
  Sliding/Downed (Robust.Shared.Physics) ИЛИ в Content.Shared/Slippery/SlidingSystem
  гасить скорость при контакте с Impassable-фикстурами windoor.
- Альтернатива контентом: у BaseWindoor уплотнить фикстуру (расширить AABB на тайл) —
  затронет геймплей прохода, риск выше.
- По кнопке: аудит ActivateInteraction у SignalSwitchSystem на предмет access-гейта.

## Regression risk / Validation plan / Links-Related
Risk: CCD/усиление фикстур может ломать близкие проходы и потолки. Validation: стол у
windoor → встать/лечь/ползти ×20 — ни одного прохода; обычный проход стоя сохраняется.
Related: 1366747516258881536, 1449508411325677692 (кластер симптома «сквозь закрытый
windoor»).

## Severity & complexity
Severity: S2 MAJOR (эксплойт обхода закрытых дверей) / Complexity: M
