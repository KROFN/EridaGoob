# Дюп пособников по Кунг-Фу на ОСЩ.

## Metadata
Thread ID: 1542496476415000757 / Date: 2026-08-27 / Author: XoLox (xolox1) / Tags: Разработка / Archive state: not archived (актуальный) / Messages: 7 / Attachments: 1 (image/webp — скриншот меню, классификация: BUG_EVIDENCE, [MEDIA], не открывался по бюджету)

## Discord evidence
- [SRC] Root: если выбрать револьверный набор И набор Кунг-Фу, то при нажатии "Подтвердить" менюшка залагивает и выдаётся лишь Кунг-Фу; если продолжать спамить кнопку — учебники выдаются до бесконечности.
- [SRC] rOmannn299: "нт подобрело и дает всей станции кунгфу" — на практике дюп раздают станции.
- [SRC] 9Kvac_/Mokko: шутки про раздачу книг — подтверждают факт бесконечной выдачи.

## Search vocabulary
User terms: ОСЩ, револьверный набор, набор Кунг-Фу, подтвердить, дюп / Domain terms: role loadout, loadout group MaxLimit, exclusive kit, confirm button, duplicate apply / Prototype-entity candidates: набор Кунг-Фу (книга с GrantMartialArtKnowledge), револьверный набор / Code symbols: RoleLoadout, LoadoutGroupPrototype.MaxLimit, VerifyValid, StationSpawningSystem apply, GrantMartialArtKnowledgeComponent

## Repository investigation
- FILE: Content.Shared/Preferences/Loadouts/RoleLoadout.cs [SRC]
  SYMBOL: `SelectedLoadouts` (стр. 27); VerifyValid-логика: стр. 113–129 — `loadouts[..Math.Min(groupLoadouts.Count, groupProto.MaxLimit)]` (кламп к MaxLimit), стр. 163–167 MinLimit-проверка; стр. 299–302 — кламп при добавлении выбора (`limit = Math.Max(0, groupLoadouts.Count + 1 - MaxLimit)`).
  WHY RELEVANT: лимит группы (эксклюзивный набор, MaxLimit=1) enforced в этих клиентских/шаред-проверках; UI-лаг и "остаётся только Кунг-Фу" согласуются с клампом последнего выбора.
- FILE: Content.Goobstation.Shared/MartialArts/Components/GrantMartialArtKnowledgeComponent.cs + Resources/Prototypes/_Goobstation/MartialArts/dragonkungfu.yml [SRC]
  SYMBOL: книга обучения кунг-фу — предмет выдачи; повторная выдача даёт повторное обучение (нет idempotence) → бесконечные книги при спаме.
- FILE: Content.Server/Station/Systems/StationSpawningSystem.cs [?]
  SYMBOL: серверный apply loadout (файл содержит ApplyLoadout-семантику по grep, но блок не открыт в бюджете) — предполагаемое место отсутствия server-side MaxLimit/dedup-проверки.

## History
- [HIST] d2356f3ec9 "Implemented parenting and minimum default for loadout groups (#40861)" (upstream) — свежие изменения групп лоадаута; 09bff9edd3 "Fix loadout entity names not being exported/imported"; 4b456ed9ef "Reuse (#5)". Прямого фикса "сервер не клампит MaxLimit при спавне" не видно.

## Upstream comparison
SS14 upstream: RoleLoadout.VerifyValid клампит выбор к MaxLimit (клиент/профиль); серверный спавн в upstream проходит через ту же структуру, но защита от re-apply/duplicate на кнопку "подтвердить" вне upstream-гарантий [UP/INFER]. Goob: без спец-отличий найдено.

## Verdict
LIKELY_CURRENT_BUG — воспроизведение детально описано, лимит группы enforced только в VerifyValid/клиентском клампе (RoleLoadout.cs:129,302), а серверная выдача/повторное подтверждение не имеет доказанной проверки → дюп. Точный код apply не открыт [?].

## Confidence
Medium

## Root cause
HYPOTHESIS (Medium): набор Кунг-Фу и револьверный набор лежат в одной эксклюзивной группе лоадаута (MaxLimit=1). Клиент позволяет держать оба выбранными; при "Подтвердить" UI-состояние ломается (лаг), сервер применяет только последний (кунг-фу), а повторные нажатия триггерят повторное применение лоадаута без server-side MaxLimit/dedup → бесконечная выдача GrantMartialArtKnowledge-книг.

## Proposed fix (do NOT implement!)
- Серверная сторона: в точке применения лоадаута (Content.Server/Station/Systems/StationSpawningSystem.cs, apply-метод) клампить groupLoadouts к MaxLimit (как RoleLoadout.cs:129) и делать apply идемпотентным (пометка "выдано", повторный confirm — no-op).
- Клиент: не позволять подтверждение при невалидном выборе (disabled-кнопка вместо лага), исправить обработку двойного выбора в одной группе (LoadoutGroupContainer.xaml.cs).

## Regression risk / Validation plan / Links-Related threads
Risk: кламп на сервере может отрезать легальные группы с MaxLimit>1 — применять per-group. Validation: выбрать оба набора на ОСЩ, подтвердить, спамить кнопку — должен выдаться максимум 1 комплект, меню не виснет. Related: B06/B07 (loadout UI треды) — тематическая семья, root cause другой.

## Severity & complexity (if actionable)
Severity: S2 MAJOR (эксплойт бесконечной выдачи) / Complexity: M

## Verdict JSON (also written to batch file)
см. .ai-triage/data/verdicts/batch_B34.json
