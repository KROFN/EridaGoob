# Ошибка терминатора

## Metadata
Thread ID: 1501936787692589197 / Date: 2026-05-07 / Author: ReddyFord / Tags: Дискорд-Бот / Archive state: archived / Messages: 1 / Attachments: 4 (image.png x4 — не отрисовались в контексте субагента; presumed BUG_EVIDENCE)

## Discord evidence
- «Черта внешности "Терминатор" неправильно отображается на спрайте персонажа. Должна фактически перекрывать правый глаз и правую часть лица, но отображается почему-то с левой».
- Expected: аугментация на правой половине лица; Actual: на левой. Repro: выбрать черту «Терминатор» в кастомизации внешности.
- Replies: нет.

## Search vocabulary
User terms: терминатор, черта внешности, глаз / Domain terms: head augmentation marking, Eyes bodyPart / Prototype-entity candidates: marking `Terminator` / Code symbols: EyeColoring, headaugs.rsi

## Repository investigation
FILE: Resources/Prototypes/_Erida/Entities/Mobs/Customization/Markings/headaugs.yml
SYMBOL: marking `Terminator` — bodyPart: Eyes, markingCategory: Head, coloring: default → !type:EyeColoring (negative: true), sprite `_Erida/Customization/Markings/headaugs.rsi`, state `terminator`
WHY RELEVANT: это и есть «Терминатор» из кастомизации (head-augmentation).
CURRENT BEHAVIOR: одиночный маркинг; сторона лица задана только рисунком спрайта, зеркалирования в коде нет (соседние ауги `Beholder`, `Iron_jaw` устроены так же).

FILE: Resources/Textures/_Erida/Customization/Markings/headaugs.rsi/meta.json
SYMBOL: state `terminator` — directions: 4, без offset и без парного состояния для второй стороны лица
WHY RELEVANT: отсутствие зеркального состояния означает: какая сторона лица покрыта — решает только пиксельное содержимое RSI (просмотр PNG в субагенте недоступен).

## History
- headaugs.yml и headaugs.rsi/meta.json созданы коммитом 72a45ae642 2026-08-12 "Species (#12)" — ПОЗЖЕ отчёта (2026-05-07): текущая реализация headaugs — переработка, выполненная после репорта.
- `git log -S'Terminator' --all` не завершился (таймаут в рамках лимита вызовов) — где жил «Терминатор» на момент репорта, не установлено; проверить до фикса.

## Upstream comparison
Not investigated — `_Erida` headaugs уникальны для форка; апстрим-эквивалент не установлен.

## Verdict
CANNOT_VERIFY — на дату репорта текущие файлы не существовали (перезаведены в "Species (#12)" 2026-08-12); без просмотра PNG/старых спрайтов невозможно доказать, что зеркальность воспроизводится в текущем форке.

## Confidence
Low

## Root cause
HYPOTHESIS: спрайт state `terminator` нарисован на левой половине лица вместо правой (контентная ошибка RSI); кодовой логики зеркалирования нет (headaugs.yml:Terminator; headaugs.rsi:terminator, 4 directions, без зеркального состояния). Требуется перепроверка после переработки #12. (Low)

## Proposed fix (do NOT implement!)
- Если воспроизводится: отзеркалить/перерисовать state `terminator` в Resources/Textures/_Erida/Customization/Markings/headaugs.rsi (контент-фикс); код менять не нужно.

## Regression risk / Validation plan / Links-Related threads
Risk: минимальный (один state). Validation: персонаж с чертой «Терминатор», скриншоты в 4 направлениях; сверить с ожиданием «правый глаз + правая сторона лица». Related: B04 (species markings/customization).

## Severity & complexity (if actionable)
Severity: S4 COSMETIC / Complexity: XS

## Verdict JSON (also write to batch file)
См. .ai-triage/data/verdicts/batch_B05.json
