# Куча ошибок

## Metadata
Thread ID: 1367396276349698049 / Date: 2025-05-01T07:04:37Z / Author: Святой Отчим (holy_stepfather, id 966516327047106620) / Tags: none / Archive state: archived / Messages: 5 / Attachments: 1 (SS14.Loader_Q7guy9WD7C.mp4, 50.6 s, 1920x1000, 30 fps — видео консоли клиента/сервера)

## Discord evidence
- Автор: «Всё в видео.» — video-only report.
- Обработка видео: ffprobe (метаданные) → `ffmpeg -i <file> -vf fps=1/2 /tmp/frames_1367396276349698049_%03d.jpg` (25 кадров) → tesseract OCR. Read-рендер кадров недоступен в контексте триажа, текст извлечён OCR.
- Ключевые записи из видео (OCR):
  1. `state: Added component PsionicInsulation to entity BecyenbHbii (548/n656820, MobShadowkin, ISKARIOT)` + stack trace: `System.Environment.get_StackTrace()` → `EntityManager.AddComponentInternal` → `EntityManager.AddComponent[T]` → `Content.Shared.Backmen.Abilities.Psionics.PsionicItemsSystem.OnTinfoilEquipped(EntityUid uid, TinfoilHatComponent component, GotEquippedEvent args) in /home/container/Content.Shared/Backmen/Abilities/Psionics/Items/PsionicItemsSystem.cs:line 29` — при экипировке жестяной шляпки сервер добавляет PsionicInsulation, хотя псионика на сервере отключена; путь печатается как ошибка/исключение.
  2. `runtime: Caught exception in "ResetPredictedEntities": System.InvalidOperationException: Collection was modified; enumeration operation may not execute` at `Robust.Client.GameStates.ClientGameStateManager.ResetPredictedEntities()` (line 586) → `ApplyGameState` (line 356) — клиентское исключение движка при применении состояний.
  3. Фоновые предупреждения: `[WARN] eng: MainLoop: Cannot keep up!`, `[WARN] res: Loading raw texture inside RSI: /Textures/Objects/Specific/Medical/Surgery/scalpel.rsi/scalpel.png` (файл существует и в текущем репо).
- Разработчик TREST (2025-05-01): «тебе псионику пытается накрутить, но не может, так как она отключена» + «исправлю»; автор поблагодарил.
- Классификация медиа: BUG_EVIDENCE (видео с логами и трейсами).

## Search vocabulary
User terms: «куча ошибок», псиónica/жестянка (из реплик) / Domain terms: psionics disabled, tinfoil hat, psionic insulation, ResetPredictedEntities, InvalidOperationException / Prototype-entity candidates: TinfoilHat, PsionicInsulation / Code symbols: PsionicItemsSystem.OnTinfoilEquipped, EntityManager.AddComponent, ClientGameStateManager.ResetPredictedEntities

## Repository investigation
- Деплой на момент репорта (2025-05-01) — старый Backmen-базированный билд: путь в трейсе `/home/container/Content.Shared/Backmen/...`, заголовок окна в смежном треде 1366681937032511539 — «Space Station 14 - Backmen».
- Текущий форк (EridaGoob, lineage Goob-Station):
  - `rg "PsionicItemsSystem|OnTinfoilEquipped" -g '*.cs'` — 0 совпадений.
  - `rg "PsionicInsulation"` — 0 совпадений.
  - `ls Content.Shared` / `git log --all -- Content.Shared/Backmen` — директория Backmen никогда не существовала в истории текущего репозитория.
  - Псионика в текущем контенте ограничена файлом Content.Shared/_EinsteinEngines/Psionics/TelepathyComponent.cs (телепатия) — путь OnTinfoilEquipped → AddComponent(PsionicInsulation) не существует.
- FILE: RobustToolbox (сабмодуль, `git submodule status` → `-391567ba5be3... RobustToolbox`, uninitialized)
  SYMBOL: Robust.Client.GameStates.ClientGameStateManager.ResetPredictedEntities
  WHY RELEVANT: клиентское исключение «Collection was modified; enumeration operation may not execute» — движковый race при ResetPredictedEntities/ApplyGameState; код локально недоступен (пустой сабмодуль). Триггером совпадало с серверным добавлением/сбросом компонентов (PsionicInsulation) во время применения состояния.
  CURRENT BEHAVIOR: вне области Content-репозитория.

## History
No relevant commits found — баггованный код никогда не существовал в git-истории текущего форка (`git log --all -- Content.Shared/Backmen` пусто; `git log -S` по 44 901 коммитам не выполнялся целиком из-за объёма, но absence of directory/путей подтверждён ls-tree и ls). Баг принадлежал предыдущему Backmen-деплою; в день репорта принят разработчиком (TREST, «исправлю»). Текущий форк переехал на Goob-основу (merge-base master↔upstream = 511cc23b7c «Automatic Changelog (#35)»).

## Upstream comparison
Goob-Station / SS14: системы PsionicItemsSystem/Backmen psionics нет — ошибка-путь в upstream-lineage отсутствует по построению. RobustToolbox ResetPredictedEntities race — движок, вне данного репо (не исследовался детально — локальные evidence достаточны).

## Verdict
ALREADY_FIXED_IN_CURRENT_FORK — описанный путь ошибок целиком отсутствует в текущем форке: кодовая база заменена с Backmen на Goob-основу, символов PsionicItemsSystem/PsionicInsulation нет нигде (`rg` 0 совпадений). Примечание: proof-by-commit невозможен, поскольку старый код не входил в git-историю текущего репозитория — доказательство отсутствие кода + признание разработчика в треде.

## Confidence
Medium

## Root cause
CONFIRMED (для деплоя 2025-05): Content.Shared/Backmen/Abilities/Psionics/Items/PsionicItemsSystem.cs:29 (OnTinfoilEquipped) при экипировке жестяной шляпки добавлял компонент PsionicInsulation при отключённой серверной псионики → серверный лог/исключение с печатью стека; параллельно на клиенте срабатывал движковый race в Robust.Client ResetPredictedEntities («Collection was modified»). В текущем форке оба пути отсутствуют/вне репо.

## Proposed fix (do NOT implement!)
Не требуется. Опционально при будущих псионики-фичах: перед AddComponent проверять серверный cvar включённости псионики (аналогично тому, что TREST сделал в старом деплое).

## Regression risk / Validation plan / Links-Related threads
Regression risk: none. Validation plan: подключение текущего клиента к текущему серверу, экипировка жестяной шляпки-аналога — отсутствие ошибок в консоли. Related threads: 1366681937032511539 (тот же автор, Backmen-эпоха, спам ошибок клиента).

## Severity & complexity (if actionable)
Severity: S3 MINOR (был спам ошибок/исключений клиента без потери сервера) / Complexity: XS (в текущем форке действий не требуется).

## Verdict JSON (also write to batch file)
{"thread_id":"1367396276349698049","verdict":"ALREADY_FIXED_IN_CURRENT_FORK","confidence":"Medium","severity":"S3","complexity":"XS","cluster_hint":null,"root_cause_short":"Старый Backmen-деплой: PsionicItemsSystem.OnTinfoilEquipped добавлял PsionicInsulation при выключенной псионики (+ клиентский race ResetPredictedEntities); в текущем Goob-форке символы отсутствуют","evidence":["video OCR /tmp/frames_1367396276349698049:OnTinfoilEquipped @ Content.Shared/Backmen/Abilities/Psionics/Items/PsionicItemsSystem.cs:29","video OCR:ClientGameStateManager.ResetPredictedEntities InvalidOperationException","rg PsionicInsulation -> 0 hits in current repo","TREST 2025-05-01 reply: 'исправлю'"],"dossier":".ai-triage/bugs/1367396276349698049.md"}
