# Лампа шахтеров

## Metadata
Thread ID: 1391702186244706354 / Date: 2025-07-07 / Author: GreenMaill / Tags: none / Archive state: resolved in-thread / Messages: 9 / Attachments: 2 images (not read per budget — context images of the lamp)

## Discord evidence
- Author: using the "miner lamp" in hands triggers it like a flash/flashbang; toggling via the action panel works normally.
- Replies: Svaxe suggests it is the contraband lamp that can spawn in tech storage; GreenMaill confirms the lamp came from techs; GreenMaill: "Не баг, а фича" — thread self-resolved as NOT a bug.
- Media: BUG_EVIDENCE (screenshots, not read — resolution reached in-thread).

## Search vocabulary
User terms: шахтерская лампа, флешка, светильник из контрабанды, техи / Domain terms: lamp, flash, contraband, interrogation lamp / Prototype candidates: BaseLamp, Lamp, LampInterrogator / Code symbols: FlashComponent, FlashOnUse, ItemTogglePointLight.

## Repository investigation
FILE: Resources/Prototypes/Entities/Objects/Misc/fluff_lights.yml
SYMBOL: LampInterrogator (lines ~135-190)
WHY RELEVANT: the only lamp prototype with a Flash component; "Ultra-bright lamp for the bad cop", contraband (BaseSecurityContraband), LimitedCharges 3, AutoRecharge 30s — exactly "светильник из контрабанды, который флешит".
CURRENT BEHAVIOR: `- type: Flash  flashOnMelee: false  flashOnUse: false` — in the CURRENT fork activating it in hand does NOT flash.

FILE: Content.Shared/Flash/Components/FlashComponent.cs
SYMBOL: FlashOnUse (line 19, default `true`)
WHY RELEVANT: older builds without the explicit `flashOnUse: false` override flashed on in-hand activation — matches the reported behavior ("в руках срабатывает как флешка", toggle via panel = light only via ItemTogglePointLight).

FILE: Resources/Prototypes/Entities/Objects/Misc/fluff_lights.yml
SYMBOL: BaseLamp / Lamp
WHY RELEVANT: the actual plain lamp family — no Flash component, toggles only as a light.

## History
Relevant commits: af272d8114 "Predict Flashes (#37640)", 68b7977bee "2 july upstream (second attempt)", a5ce30bdc8 revert, 54ce591914 "20 july upstream sync (#4307)". `flashOnUse` string was moved through those syncs; current file state disables flash-on-use for LampInterrogator. NOTE: fork-native commit dates (2026) are inconsistent with 2025 report dates — date-based claims treated cautiously.

## Upstream comparison
SS14 upstream PR #37640 ("Predict Flashes") introduced/normalized FlashOnUse handling; upstream sets LampInterrogator to not flash on use. Not investigated further — local evidence sufficient.

## Verdict
USER_ERROR_OR_NOT_BUG — reporter used the contraband interrogator lamp (misidentified as the miner lamp); thread self-resolved ("Не баг, а фича").

## Confidence
High

## Root cause
CONFIRMED: item misidentification. The lamp found in techs was `LampInterrogator` (Flash component, contraband), not the plain miner lamp (`Lamp`/`BaseLamp`, no Flash). On current fork `flashOnUse: false` prevents the described behavior entirely.

## Proposed fix (do NOT implement!)
None required. Optional UX: LampInterrogator already distinguished by contraband classification; no change needed.

## Regression risk / Validation plan / Links-Related threads
No regression risk. Validation: activate LampInterrogator in hand on current build → no flash; melee/flash usage still available. Related: none.

## Severity & complexity
Severity: S4 COSMETIC / Complexity: XS

## Verdict JSON
See .ai-triage/data/verdicts/batch_B31.json
