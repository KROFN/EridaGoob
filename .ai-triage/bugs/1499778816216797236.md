# сумка (нельзя перетаскивать/переворачивать предметы внутри сумки)

## Metadata
Thread ID: 1499778816216797236 / Date: 2026-05-01 / Author: KiriKuru (.kirikuru) / Tags: Серьёзный / Archive state: archived / Messages: 2 / Attachments: 1 (image/webp — скриншот, классификация: BUG_EVIDENCE, [MEDIA], не открывался по бюджету)

## Discord evidence
- [SRC] Root: "Некоторые вещи, абсолютное большинство, нельзя нормально перевернуть или перетащить внутри самой сумки, если зажимать по предмету ЛКМ. Так-же проблемы с поворотом в целом. Багуется даже пачка сигарет."
- [SRC] alexfire (дев): "раньше при зажатии предмета игра считывала его отсутствие в сумке... а теперь она этого не считывает, ты зажимаешь предмет, но то где он до этого лежал всё ещё занято и не даёт там разместить предмет" — точный механизм: ячейка-источник не освобождается при драге.

## Search vocabulary
User terms: сумка, перетащить, перевернуть, ЛКМ, пачка сигарет / Domain terms: storage UI drag, item rotate in container, grid slot, ItemMapper sprite state / Prototype-entity candidates: cigarette pack (item-mapper) / Code symbols: StorageWindow drag-drop, StorageSystem, ItemMapper

## Repository investigation
- Целевой поиск обработки drag/rotate в Content.Client/Storage и Content.Shared/Storage в рамках бюджета совпадений не дал [?] — точное расположение системы драга в этом форке не локализовано (возможно переименовано/перенесено; разбирать Content.Client/Storage/UI + Content.Shared/Storage/StorageSystem).
- [INFER] Известная upstream-архитектура: клиентский storage window делает локальный drag с ghost-превью и посылает серверу запрос перемещения; если ghost не освобождает исходную ячейку, дроп в соседнюю клетку блокируется "занятостью".
- [INFER] "проблемы с поворотом": поворот предмета в контейнере пересчитывает спрайт (у пачки сигарет — ItemMapper-состояния), рассинхрон grid-состояния объясняет "багуется даже пачка сигарет".

## History
- [HIST] Релевантные коммиты по storage-UI в рамках бюджета не проверялись (бюджет поиска исчерпан). Репорт 2026-05-01; дев-ответ описывает регрессию ("раньше считывала... теперь нет"), т.е. свежее изменение клиентского UI-storage повлекло поведение.

## Upstream comparison
SS14 upstream: drag внутри storage UI — клиентская фича; известно о периодических багах ghost-ячейки [UP]. Прямое сравнение коммитов не проводилось — локальное доказательство не найдено.

## Verdict
LIKELY_CURRENT_BUG — баг отчётливо воспроизводим по описанию и механизму от дева (ячейка-источник остаётся занятой при зажатии ЛКМ), но код в репозитории в рамках бюджета не локализован; root cause — HYPOTHESIS.

## Confidence
Low

## Root cause
HYPOTHESIS (Low, механизм подтверждён девом в [SRC]): клиентский drag внутри storage не освобождает исходную ячейку ghost-а (по формулировке alexfire: "то где он до этого лежал всё ещё занято"), из-за чего перетаскивание в соседнюю клетку и операции поворота падают/блокируются; страдают предметы с мапперами спрайтов (пачка сигарет).

## Proposed fix (do NOT implement!)
- Локализовать клиентский drag-обработчик storage UI (Content.Client/Storage/UI, классы StorageWindow/DragDropHelper-типа) и серверную часть StorageSystem.TransferItem.
- Минимальный фикс: при старте драга помечать исходную ячейку свободной для placement-валидации; после поворота/перемещения пересчитывать ItemMapper-спрайты контейнера.

## Regression risk / Validation plan / Links-Related threads
Risk: правка ghost-логики может сломать drag-and-drop из/в слоты персонажа — тестировать оба направления. Validation: зажать ЛКМ пачку сигарет внутри сумки, перетащить на соседнюю клетку и повернуть — должно работать; повторить с рюкзаком/ПСЖ. Related: — (B30 mannequin/slots тематически, root cause другой).

## Severity & complexity (if actionable)
Severity: S3 MINOR (тэг репорта "Серьёзный", но функционал перекомпоновки) / Complexity: M

## Verdict JSON (also written to batch file)
см. .ai-triage/data/verdicts/batch_B34.json
