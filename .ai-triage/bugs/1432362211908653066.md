# Воксы языки (Vox language name/description wrong)

## Metadata
Thread ID: 1432362211908653066 / Date: 2025-10-27 / Author: Товарищ Дементор (twilight_angelo)
Tags: unknown_tag_1414266059208265799 / Archive: archived / Messages: 1 / Attachments: 1 (image.png, screenshot — [MEDIA] BUG_EVIDENCE, not rendered per budget rule)

## Discord evidence
- "Проблема с названием и описанием языка воксов" — Vox language shows wrong name and/or description (screenshot; not rendered — no image reads allowed this batch).
- No replies, no repro steps. Expected: correct Vox language name/description; actual: wrong values shown in language UI.

## Search vocabulary
User terms: "язык воксов" / Domain terms: language prototype, loc keys, Vox / Prototype candidates: `NewKinPidgin` (EE language system) / Code symbols: `language-NewKinPidgin-name`, `language-NewKinPidgin-description`, `chat-language-NewKinPidgin-name`.

## Repository investigation
FILE: Resources/Prototypes/_EinsteinEngines/Language/Species-Specific/vox.yml
SYMBOL: `- type: language id: NewKinPidgin` ("PORTED FROM FLOOFSTATION", comment line 3)
WHY RELEVANT: this IS the Vox language prototype (Einstein Engines language system; syllable obfuscation "ca/ra/ma/skre/kraa…").
CURRENT BEHAVIOR: prototype defines NO name/description data fields — both come purely from locale keys. So "wrong name/description" is a loc-layer issue.

FILE: Resources/Locale/en-US/_EinsteinEngines/language/languages.ftl:198-199
SYMBOL: `language-NewKinPidgin-name = Ka'rakk`; description "A debased dialect cobbled together by lower-caste Vox…"
WHY RELEVANT: EN shows name "Ka'rakk" — a FloofStation-port naming, arguably not the canonical Vox language name.

FILE: Resources/Locale/ru-RU/_EinsteinEngines/language/languages.ftl:103-104
SYMBOL: `language-NewKinPidgin-name = Ка'ракк`; `language-NewKinPidgin-description =` (line 104 — value empty / continuation not captured by grep)
WHY RELEVANT: [HIST] on a Russian-locale fork a blank description key renders an empty description in the language UI — matches "проблема с названием и описанием" complaint for RU players.

FILE: Resources/Locale/ru-RU/_EinsteinEngines/chat/managers/chat-language.ftl:27 and en-US .../chat-language.ftl:28
SYMBOL: `chat-language-NewKinPidgin-name` — third place where the name must stay in sync.

## History
No relevant commits found within budget (no git log run on these loc files — budget exhausted). [INFER] keys came with the EE/Floof language port; ru-RU translation of the description appears missing/truncated.

## Upstream comparison
EE upstream uses `NewKinPidgin` with its own ftl; not compared commit-wise — local evidence sufficient for a loc fix.

## Verdict
LIKELY_CURRENT_BUG (RU language UI shows wrong/empty Vox language name+description; exact rendered strings unverified — screenshot not read)

## Confidence
Medium

## Root cause
HYPOTHESIS (Medium): vox language relies 100% on loc keys (`Resources/Locale/ru-RU/_EinsteinEngines/language/languages.ftl:103-104`); the ru-RU `language-NewKinPidgin-description` is empty/mistranslated and the name "Ка'ракк" (Floof port) does not match what the fork intends to display.

## Proposed fix (do NOT implement!)
Fill/correct `language-NewKinPidgin-name/description` in `Resources/Locale/ru-RU/_EinsteinEngines/language/languages.ftl` and mirror in `chat-language.ftl` (both locales if a canon name change is wanted, e.g. proper Vox name instead of "Ka'rakk"). Pure content/loc change, no code.

## Regression risk / Validation plan / Links-Related
Risk: none (loc-only). Validate: RU client → character language UI + chat language label for Vox; screenshot compare. Related: B26 accents batch (species language).

## Severity & complexity
Severity: S4 COSMETIC / Complexity: XS
