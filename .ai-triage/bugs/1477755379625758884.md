# Импорт персов - всё?!?!

## Metadata
Thread ID: 1477755379625758884 / Date: 2026-03-01 / Author: 9Kvac_ / Tags: unknown_tag / Archive state: archived / Messages: 7 / Attachments: 0

## Discord evidence
- "Импорт персонажей не работает. Как повторить? Попробовать импортировать yml персонажа."
- Jill Stingray: файл импортируется "без указания типа файла иногда", совет дописать .yml
- 9Kvac_: "Я импортировал с .yml" → позже: "Проверил - импортирует с указанием yml. Но.. в редакторе импортируется НИЧЕГО"
- Итог: файл выбирается через диалог, но редактор персонажа не меняется, без каких-либо ошибок в UI.
- Медиа: отсутствует.

## Search vocabulary
User terms: импорт персонажей, yml / Domain terms: profile import, HumanoidProfileEditor / Prototype-entity candidates: - / Code symbols: ImportProfile, HumanoidAppearanceSystem.FromStream

## Repository investigation
FILE: Content.Client/Lobby/UI/HumanoidProfileEditor.xaml.cs
SYMBOL: ImportProfile() (строки 2428–2458), вызов кнопкой ImportButton (строка 168)
WHY RELEVANT: единственный путь импорта профиля на клиенте.
CURRENT BEHAVIOR: `_entManager.System<HumanoidAppearanceSystem>().FromStream(file, ...)` — парсинг DataNode профиля; любой брошенный эксепшн ловится в `catch (Exception exc)` и ТОЛЬКО пишется в sawmill (`_sawmill.Error`). UI-фидбек об ошибке отсутствует: SetProfile() не вызывается, редактор молча остаётся прежним — ровно наблюдение репорта «импортируется НИЧЕГО».
FILE: Content.Client/Humanoid/Systems/HumanoidAppearanceSystem.cs (FromStream/ToDataNode)
SYMBOL: HumanoidAppearanceSystem.FromStream
WHY RELEVANT: парсер профиля; падает на незнакомых/устаревших species, markings, cargaout-полях, особенно после портов контента с изменением схемы маркировок.

## History
- a2ba0ebe00 2026-08-01 "[PORT + BOUNTY] moth shrink-wrapping port + multilayer markings and colorlinks" — изменение схемы markings (после репорта; старые yml-экспорты могут переставать парситься ещё сильнее).
- bc02215fde 2026-08-14, 4b456ed9ef 2026-08-10 — прочие правки лобби/профилей; явного «исправления импорта» нет.

## Upstream comparison
Erida upstream: те же правки приходят портами; Goob/SS14: в upstream известны аналогичные репорты «import silently fails» (молчаливый catch — поведение унаследовано). Не investigated глубже — локальных данных достаточно.

## Verdict
LIKELY_CURRENT_BUG — код-путь доказан: любая ошибка парсинга профиля проглатывается без UI-фидбека; точный эксепшн у конкретного игрока не извлечён (нужен client log, client.log: "Error when importing profile").

## Confidence
Medium

## Root cause
HYPOTHESIS (Medium): `HumanoidAppearanceSystem.FromStream` бросает исключение (несовместимая схема профиля/species/markings другого сервера или старый экспорт), `ImportProfile()` перехватывает его молча → пользователь видит «ничего не происходит». Дополнительный UX-дефект подтверждён кодом: нет сообщения об ошибке ни при каком сценарии.

## Proposed fix (do NOT implement!)
1) В `ImportProfile()` catch-блоке показывать игроку диалог/inline-ошибку (Loc-ключ, напр. `character-import-failed`) и лог. 2) В `HumanoidAppearanceSystem.FromStream` делать деградацию незнакомых markings/species вместо throw. Файлы: HumanoidProfileEditor.xaml.cs, HumanoidAppearanceSystem.cs, ru/en ftl.

## Regression risk / Validation plan / Links-Related threads
Риск: низкий (клиентский UX). Валидация: импорт валидного экспорта своего сервера, битого yml, экспорта другого форка; проверить наличие сообщения об ошибке.

## Severity & complexity
Severity: S3 MINOR / Complexity: S
