# Перчатки ниндзя и шкафы (не открывают закрытые шкафчики)

## Metadata
Thread ID: 1425261607369310339 / Date: 2025-10-07 / Author: Пряники (fox_meowder) / Tags: unknown_tag_1414266059208265799 / Archive: archived / Messages: 13 / Attachments: 0

## Discord evidence
- Автор не уверен, баг ли это: перчатки ниндзя не открывают закрытые шкафы (мед, СБ, капитан и т.д.).
- Ответ ERП-ЛРП: «так было всегда, с момента создания ниндзи».
- Ответ WhatApple: «Оно открывает, но откатывает действие. Шкаф сверкает, моргает зелёным, но ему пофек. Это баг» — [?] не подтверждается кодом (см. ниже).
- Контраргумент: у ниндзя есть катана, ломающая шкафы (автор: «да, но шумно»).
- Медиа нет. Классификация: без медиа, спор репутационный.

## Search vocabulary
User terms: перчатки ниндзя, шкафы закрытые. / Domain terms: Space Ninja gloves, emag provider, whitelist, closet/locker, AccessReader, Lock. / Prototype candidates: ClothingHandsGlovesSpaceNinja. / Code symbols: EmagProviderSystem, EmagProviderComponent, NinjaGloves, GotEmaggedEvent, LockSystem.OnEmagged.

## Repository investigation
FILE: Resources/Prototypes/Entities/Clothing/Hands/gloves.yml (240-278)
SYMBOL: ClothingHandsGlovesSpaceNinja -> NinjaGloves.abilities -> EmagProvider
WHY RELEVANT: перчатки дают EmagProvider с whitelist: components: [Airlock].
CURRENT BEHAVIOR: взлом доступа применим ТОЛЬКО к сущностям с компонентом Airlock. Шкафы (EntityStorage/Closet + Lock/AccessReader) под whitelist не подпадают.

FILE: Content.Shared/Ninja/Systems/EmagProviderSystem.cs (37-66)
SYMBOL: OnBeforeInteractHand
WHY RELEVANT: перед взломом `_whitelist.IsWhitelistFail(comp.Whitelist, target)` -> ранний return; GotEmaggedEvent поднимается только для airlock'ов.
CURRENT BEHAVIOR: клик ниндзя-перчаткой по шкафу не вызывает взлом вовсе (ни искр, ни звука) — проходит обычное взаимодействие и отклоняется по доступу.

FILE: Content.Shared/Lock/LockSystem.cs (384-402)
SYMBOL: LockSystem.OnEmagged
WHY RELEVANT: Lock УМЕЕТ сниматься взломом (EmagType.Access, BreakOnAccessBreaker), но через перчатки до этого не доходит из-за whitelist выше. Никакой обработчик GotEmaggedEvent для EntityStorage в Content.Shared/Content.Server не зарегистрирован (проверено rg по всем подписчикам).

CURRENT BEHAVIOR (итог): ниндзя не может взломать шкаф перчатками — это осознанное ограничение контента (whitelist Airlock), а не сломанная цепочка. Утверждение «моргает зелёным» кодом не объясняется; вероятна путаница с воздушными шлюзами/искрами [?].

## History
No relevant commits found — whitelist Airlock в перчатках не менялся после отчёта (проверка git не выявила правок gloves.yml по EmagProvider в зоне отчёта).

## Upstream comparison
SS14/Goob: такое же поведение — EmagProvider ниндзя рассчитан на двери (Airlock). Форк не отходит от апстрима. Не investigated глубже — локальных улик достаточно.

## Verdict
CONFIG_OR_CONTENT_ISSUE — работает как задумано: перчатки ниндзя взламывают только шлюзы (whitelist [Airlock]); открытие шкафов перчатками — контент-решение (добавить Closet/EntityStorage в whitelist или отдельный компонент).

## Confidence
High

## Root cause
CONFIRMED: EmagProviderComponent.Whitelist = [Airlock] (gloves.yml:264-267) + EmagProviderSystem.OnBeforeInteractHand ранний выход для не-whitelist целей. Шкафы не взламываются по дизайну.

## Proposed fix (do NOT implement!)
Контент-опция: в gloves.yml для EmagProvider добавить в whitelist components: - EntityStorage (и/или тег) — тогда сработает цепочка GotEmaggedEvent (AccessReaderSystem.OnEmagged + LockSystem.OnEmagged при BreakOnAccessBreaker). Балансовое решение за контент-командой.

## Regression risk / Validation plan / Links-Related
Risk: расширение whitelist ослабит анти-ниндзя баланс (СБ-шкафы). Validation: на тест-сервере клик перчаткой по закрытому шкафу СБ до/после правки.
Related: 1369302532916252772 (Lock+AccessReader на кнопках), GenpopLocker (отдельный механизм).

## Severity & complexity
S4 COSMETIC / Complexity: S (если решат менять контент).
