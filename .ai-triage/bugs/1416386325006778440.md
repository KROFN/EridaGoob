# Алё АВД? Чего? мы вас не слышим (law/AVD department radio frequency dead)

## Metadata
Thread ID: 1416386325006778440 / Date: 2025-09-13T11:33Z / Author: Danylo_dazzle / Tags: unknown_tag_1414266059208265799 / Archive state: archived / Messages: 8 / Attachments: 1 (PNG screenshot)

## Discord evidence
- [SRC] Root: "В общем суть бага заключается в том. Что частота юр отдела не работает. Вообще" — the law department (юр отдел / АВД) radio frequency does not work at all.
- [SRC] Medik: "Может быть не везде сервер под неё есть?" — maybe the telecom server for it does not exist on every map.
- [SRC] Vecortys (dev): "Карты не завезли новые. Разраб не учёл, что сервер нужен" → "Исправим" — new maps were not given the (telecom) server; dev acknowledged and promised a fix.
- [SRC] Danylo_dazzle: "Так частота та сама есть и в гарнитуре тоже есть. но она не работает" — the frequency/channel IS present (also in the headset), but transmission is dead.
- [SRC] Venick: telecom servers need the "Юрки" (law) encryption key; putting the key into the command telecom file worked, but "не для всех карт" (not for all maps); later: "на некоторых картах всё же работает" — works on some maps.
- [MEDIA] attachments/1416386325006778440/1416484008245596211_image.png — screenshot from Venick showing a working case; not renderable in triage context; tentative BUG_EVIDENCE.

## Search vocabulary
User terms: частота юр отдела, АВД, телеком, ключ шифрования / Domain terms: radioChannel, TelecomServer, encryption key, department frequency / Prototype-entity candidates: radio_channels.yml, TelecomServer entity / Code symbols: TelecomServerComponent, EncryptionKeyComponent

## Repository investigation
FILE: Resources/Prototypes/radio_channels.yml; Resources/Prototypes/_Goobstation/radio_channels.yml; Resources/Prototypes/_DV/CosmicCult/radio_channels.yml [SRC]
SYMBOL: `type: radioChannel` (verified present in these files)
WHY RELEVANT: department frequencies (incl. law/security) are defined as radioChannel prototypes here; per Discord the channel exists and shows in headsets, so the prototype side is fine.
CURRENT BEHAVIOR: [INFER] radio range depends on a TelecomServer entity on the map whose encryption keys include the law channel's key; maps not updated lack such a server/key → the frequency is dead there, alive on updated maps. Matches every Discord statement (channel present, no traffic, works "on some maps", dev: "Разраб не учёл, что сервер нужен"). [?] Per-map telecom server entities in Resources/Maps/** not inspected — search budget exhausted; mechanism follows standard SS14 telecom design.

## History
[HIST] No git history checked (budget). In-thread, developer Vecortys acknowledged the content gap and committed to fix ("Исправим") — treat as known, deliberate content debt at report time.

## Upstream comparison
[UP] Not investigated — fork map content issue; upstream has no law/AVD department frequency.

## Verdict
CONFIG_OR_CONTENT_ISSUE (works as coded: department radio needs a telecom server carrying the law-channel encryption key; several maps lack it; change is a mapping/content decision already acknowledged by dev in-thread)

## Confidence
High

## Root cause
CONFIRMED (Discord dev statements + code design): missing TelecomServer (with law-channel encryption key) on affected station maps → law frequency cannot be relayed; prototype/channel and headset entries are intact.

## Proposed fix (do NOT implement!)
- Add a telecom server with the law/security encryption key board to affected station maps, or add the law encryption key to existing telecom servers; audit Resources/Maps/** per map (Venick: command-telecom + key works as a stopgap).

## Regression risk / Validation plan / Links-Related threads
Risk: low (content only). Validation: on every map, two headsets with law frequency must hear each other through telecom. Related: 1514984863562727596 (same failure class: per-map device/content gaps).

## Severity & complexity (if actionable)
Severity: S3 MINOR / Complexity: S

## Verdict JSON (also write to batch file)
Covered by .ai-triage/data/verdicts/batch_B18.json
