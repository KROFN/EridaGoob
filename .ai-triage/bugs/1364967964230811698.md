# переход на другие сервера (invisible phantom buttons in job window)

## Metadata
Thread ID: 1364967964230811698 / Date: 2025-04-24T14:15Z / Author: Дай сюда! (daisuda) / Tags: unknown_tag_1365427630660128861 / Archive state: archived / Messages: 2 / Attachments: 1 (PNG screenshot)

## Discord evidence
- [SRC] Root message: "в окне должностей активны невидимые кнопки перехода на другие сервера, где-то в области красного прямоугольника" — in the job ("должности") window, invisible buttons that switch to other servers are active/clickable somewhere in the red-rectangle area of the screenshot.
- [SRC] Reply (TREST, 2025-04-24): "На основу МК Конфедерация" — ambiguous remark (possibly pointing at another server/branch "МК Конфедерация"); no reproduction steps anywhere in thread.
- Expected: job window has no clickable invisible elements; Actual: phantom interactive controls remain in the layout and respond to clicks.
- [MEDIA] attachments/1364967964230811698/1364967964419559454_image.png — screenshot with red rectangle marking the phantom area; image content was NOT renderable in the triage agent context, so exact UI element could not be visually identified. Classification: tentative BUG_EVIDENCE (matches description).

## Search vocabulary
User terms: окно должностей, невидимые кнопки, переход на другие сервера / Domain terms: lobby job window, job list, server switch/hop, cross-server join / Prototype-entity candidates: none (pure client UI) / Code symbols: LobbyGui, JobList, ServerName, ServerInfo, EUI job window

## Repository investigation
FILE: Content.Client/Lobby/UI/LobbyGui.xaml (LobbyGui.xaml.cs)
SYMBOL: ServerName, ServerInfo, Loc key 'ui-lobby-server-info-block', custom "Server Currency" block [SRC]
WHY RELEVANT: the only lobby UI with server-related custom (fork) blocks found; proves lobby UI carries fork-added server widgets.
CURRENT BEHAVIOR: no code symbol rendering "join other servers" buttons was located. [INFER] The "окно должностей" is most likely a job-list window (EUI/preferences UI) — its file was not found within the search budget (searches for JobList/LobbyGui/*.xaml server-switch strings came up empty), so the phantom buttons could not be traced to a concrete control.

## History
[HIST] Not investigated — no candidate file/symbol identified to blame; report is old (2025-04-24) and the UI may have changed since.

## Upstream comparison
[UP] Not investigated — local evidence insufficient; no fork symbol identified.

## Verdict
CANNOT_VERIFY (insufficient info to reproduce or locate; screenshot not inspectable in triage context; job window code not found in budgeted searches)

## Confidence
Low

## Root cause
HYPOTHESIS: fork-added server-switch buttons exist in the job window layout but render invisibly (style/text lost after a refactor or theme change), while remaining click-active (orphan controls in XAML). Confidence: Low — neither the window file nor the buttons were located.

## Proposed fix (do NOT implement!)
- Locate the job window UI (search ru locale for "должности" → resolve to .xaml/.cs, or dump BoundUserInterface list), delete or hide orphaned buttons (Visible="False"), audit remaining controls for missing styles; verify hit-test regions match visible content.

## Regression risk / Validation plan / Links-Related threads
Risk: UI-only, low. Validation: open lobby job window, sweep the previously-marked red area for clickable invisible regions; check all buttons render labels. Related threads: none in batch B18.

## Severity & complexity (if actionable)
Severity: S3 MINOR / Complexity: M

## Verdict JSON (also write to batch file)
Covered by .ai-triage/data/verdicts/batch_B18.json
