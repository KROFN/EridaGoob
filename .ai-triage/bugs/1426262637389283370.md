# Проблемы дракона

## Metadata
Thread ID: 1426262637389283370 / Date: 2025-10-10 / Author: Parodist / Tags: unknown_tag / Archive state: contested in-thread ("А не фича разве?", "Чекну") / Messages: 12 / Attachments: none

## Discord evidence
- Claim: a dragon (carp) rift charged to 100% can still be broken/destroyed; should be invulnerable once charged.
- Replies: Vecortys ("А не фича разве?"), Jill Stingray ("на старом билде это было и никто не жаловался"), Yosif (on other servers the only way to remove a charged rift is killing the dragon; carp can't break it), xkttty clarifies the complaint ("портал ломается, а должен быть неуязвимым"), WhatApple: everything is fine — kill the dragon, rift disappears, that's intended.

## Search vocabulary
User terms: разлом дракона, 100%, сломать, неуязвимый / Domain terms: carp rift, dragon rift, charging, invulnerability / Prototype candidates: DragonRift / Code symbols: DragonRiftSystem, DragonRiftState.Finished, DamageableComponent, Destructible.

## Repository investigation
FILE: Content.Server/Dragon/DragonRiftSystem.cs
SYMBOL: Update (lines 53-76)
WHY RELEVANT: when `Accumulator >= MaxAccumulator` (100%) the system calls `RemComp<DamageableComponent>(uid)` (line 69) and sets `State = Finished`.
CURRENT BEHAVIOR: a fully charged rift has NO DamageableComponent → the prototype's `Destructible` threshold (300 damage, see dragon.yml) cannot trigger → the charged rift is immune to damage, exactly as defenders of "фича" claim.

FILE: Resources/Prototypes/Entities/Structures/Specific/dragon.yml
SYMBOL: Damageable + Destructible (damage 300 → Destruction)
WHY RELEVANT: destruction path exists only while the rift still has DamageableComponent (state Charging/AlmostFinished, below 100%).
CURRENT BEHAVIOR: below 50% the rift also self-destructs if unanchored (`OnAnchorChange`, lines 116-122, only while `State == Charging`).

FILE: Content.Server/Dragon/DragonRiftSystem.cs
SYMBOL: OnShutdown (lines 124-130)
WHY RELEVANT: rift removal while the dragon is alive/not weakened calls `RiftDestroyed` — dragon just spawns a new rift; killing the dragon removes rifts (intended path per WhatApple).

## History
Relevant commits: a29d8b9fa2 "Carp wave spawner and dragons as an actual event (#10254)" (2022-08-08), 94a11f28ca "dragon refactor, objectives and use GenericAntag (#20201)" (2023-09-30) — both long predate the report; the charged-rift invulnerability (RemComp of Damageable) has been in the code since the 2023 refactor. No changes after the report date.

## Upstream comparison
SS14 upstream: same mechanics — rift becomes damage-immune once Finished; matches "это было и на старом билде". Not investigated further — local evidence sufficient.

## Verdict
USER_ERROR_OR_NOT_BUG — current code makes a 100% charged rift undamageable; the reported destruction almost certainly happened before full charge (examine rounds the displayed percentage; also the >50% "AlmostFinished" window is still damageable) or refers to the intended kill-the-dragon flow.

## Confidence
Medium

## Root cause
HYPOTHESIS (Medium): reporter/mates destroyed the rift while it was still charging (below `Finished`); note `carp-rift-examine` rounds `Accumulator/Max*100`, so "100%" read on examine can occur marginally before the next update tick removes `DamageableComponent`. At true 100% (`DragonRiftState.Finished`) the rift is invulnerable by code.

## Proposed fix (do NOT implement!)
None needed. Optional polish: when rift finishes, broadcast examine hint "разлом заряжен и неуязвим" (Loc key) to avoid recurring confusion; or clamp examine display to <100% while still damageable.

## Regression risk / Validation plan / Links-Related threads
No code change → no risk. Validation: charge rift to 100%, hit with tools/weapons → no damage progression; destroy a rift at ~50% → works (Destructible 300). Related: none.

## Severity & complexity
Severity: S4 COSMETIC / Complexity: XS

## Verdict JSON
See .ai-triage/data/verdicts/batch_B31.json
