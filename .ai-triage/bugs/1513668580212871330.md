# Пропали часы

## Metadata
Thread ID: 1513668580212871330 / Date: 2026-06-08T22:18:32Z / Author: roycce_ (id 501407467096244226) / Tags: [Разработка] / Archive state: archived / Messages: 5 / Attachments: 1 (image.png, 1273x733, content webp — скриншот отказа подключения)

## Discord evidence
- Автор: «я вот играю на этом сервере с 2025 не помню точно когда начал но у меня 20 часов точно наиграно на сервере и старый акк и не пускает всё равно на сервер в режиме бункера»; SS14-логин: Roycce.
- Скриншот (OCR, рендер недоступен в контексте триажа): окно «Space Station 14 - Erida»: «Не удалось подключиться к серверу: Connect denied: Этот сервер находится в режиме "Бункер", часто используемом в качестве меры предосторожности против рейдов, новые подключения от аккаунтов, не соответствующих определённым требованиям, временно не принимаются. Повторите попытку позже» — дословное совпадение с ключом локали `panic-bunker-account-denied` (generic-вариант без «Причины»). Классификация медиа: BUG_EVIDENCE (скриншот отказа подключения).
- Пряники (2026-06-15): «БД падала и так не подтянули, ибо некому. А по бункеру к администрации»; повторно (2026-06-23): «Нет». — персонал подтвердил: данные playtime после падения БД не восстановлены; решение — через администрацию.

## Search vocabulary
User terms: «пропали часы», «не пускает в режиме бункера», «20 часов наиграно» / Domain terms: panic bunker, bunker mode, overall playtime gate, connection denied, whitelist bypass / Prototype-entity candidates: none / Code symbols: ConnectionManager, CCVars.PanicBunker*, CCVars.BypassBunkerWhitelist, panic-bunker-account-denied

## Repository investigation
- FILE: Content.Server/Connection/ConnectionManager.cs
  SYMBOL: проверка panic bunker (lines 247–292): `if (_cfg.GetCVar(CCVars.PanicBunkerEnabled) && adminData == null)`; line 272: `var overallTime = (await _db.GetPlayTimes(e.UserId)).Find(p => p.Tracker == PlayTimeTrackingShared.TrackerOverall);`; line 273: `haveMinOverallTime = overallTime != null && overallTime.TimeSpent.TotalMinutes > minOverallMinutes`; lines 288–290: если `!validAccountAge || !haveMinOverallTime` → deny с generic-сообщением.
  WHY RELEVANT: гейт читает наигранное время ИЗ СЕРВЕРНОЙ БД; если строка playtime отсутствует (потеря данных после падения БД), `overallTime == null` → `haveMinOverallTime == false` → отказ. Ровно это и произошло (подтверждено персоналом).
  CURRENT BEHAVIOR: работает как закодировано; отказ с текстом `panic-bunker-account-denied` (без причины — в скриншоте именно generic-текст).
- FILE: Content.Server/Connection/ConnectionManager.cs
  SYMBOL: `bypassAllowed = _cfg.GetCVar(CCVars.BypassBunkerWhitelist) && await _db.GetWhitelistStatusAsync(userId)` (line 256)
  WHY RELEVANT: существующий механизм обхода бункера через whitelist — административное решение без правки кода.
- FILE: Resources/Locale/ru-RU/connection-messages.ftl
  SYMBOL: panic-bunker-account-denied (line 22)
  WHY RELEVANT: дословное совпадение текста отказа со скриншотом пользователя.
- FILE: Resources/ConfigPresets/WizardsDen/*.toml
  SYMBOL: panic_bunker.* cvars (например `panic_bunker.min_overall_minutes = 15`)
  WHY RELEVANT: конфигурируемость гейта; реальный config сервера в репо не хранится.

## History
No relevant commits found — механизм panic bunker в Content.Server/Connection не менялся в связи с инцидентом; гейт стандартный (Erida-меток в ConnectionManager.cs нет).

## Upstream comparison
Механизм panic bunker — стандартный механизм SS14 (Wizards Den), поведение идентично upstream; локальных модификаций гейта в форке нет. Не исследовался детально — локальные evidence достаточны.

## Verdict
USER_ERROR_OR_NOT_BUG — гейт работает как закодирован; причина отказа — утерянные/не восстановленные после падения БД данные наигранного времени в серверной БД (подтверждено персоналом в треде). Решение — восстановление данных или административный whitelist-обход, не правка кода.

## Confidence
High

## Root cause
CONFIRMED: panic bunker отказывает аккаунт, у которого в серверной БД нет Overall playtime ≥ порога (ConnectionManager.cs:271–290, `_db.GetPlayTimes` → `overallTime == null` → deny); после падения БД время пользователя не было подтянуто («БД падала и так не подтянули» — Пряники), что даёт отказ с текстом `panic-bunker-account-denied` (Resources/Locale/ru-RU/connection-messages.ftl:22). Фикс — восстановление play_time или whitelist (`CCVars.BypassBunkerWhitelist`, ConnectionManager.cs:256).

## Proposed fix (do NOT implement!)
Вне кода: восстановить play_time-записи из бэкапа/старой БД, либо выдать пользователю whitelist (bypass бункера), либо временно снять `panic_bunker.enabled`. В коде правок не требуется.

## Regression risk / Validation plan / Links-Related threads
Regression risk: none. Validation plan: попытка подключения пользователя после восстановления данных/выдачи whitelist. Related threads: 1534207157308756059 (тот же класс данных — playtime в серверной БД, но другой инцидент: не обнулилась статистика одного аккаунта).

## Severity & complexity (if actionable)
Severity: S3 MINOR (конкретный пользователь не может подключиться; персональный случай, известен workaround через администрацию) / Complexity: XS (операция с данными/админ-действие).

## Verdict JSON (also write to batch file)
{"thread_id":"1513668580212871330","verdict":"USER_ERROR_OR_NOT_BUG","confidence":"High","severity":"S3","complexity":"XS","cluster_hint":null,"root_cause_short":"Panic bunker отказывает, т.к. в серверной БД нет Overall playtime после падения БД (персонал подтвердил); gate работает как закодирован","evidence":["Content.Server/Connection/ConnectionManager.cs:272:GetPlayTimes","Content.Server/Connection/ConnectionManager.cs:256:BypassBunkerWhitelist","Resources/Locale/ru-RU/connection-messages.ftl:22:panic-bunker-account-denied"],"dossier":".ai-triage/bugs/1513668580212871330.md"}
