# Отсутствие принтеров на большинстве станций (нет document printers на Amber и др.)

## Metadata
Thread ID: 1419457062882250874 / Date: 2025-09-21T22:55Z / Author: Σ Ξ ✦ Jill Stingray / Tags: unknown_tag / Archive: archived / Messages: 8 / Attachments: 0

## Discord evidence
- Репорт: «Под данный баг относится станция Amber как минимум. Так же хотелось бы проверить и другие карты кроме Бокса, ибо там принтера есть» — речь о принтерах документов (заполнение отчётов РП, «заполняешь отчёт с полного нуля»).
- Ответы разработчиков [HIST]: Yosif «Скоро будут», «Почти перенесены карты»; Vecortys: «До переноса карт я добавлял принтеры только на бокс и, вроде, маралон», «Так что не баг, просто временная заглушка», «Да карты старые скоро будут уже. Нет смысла дёргать существующие».
- Медиа: нет (attachments_on_disk: []). Классификация: n/a.

## Search vocabulary
User terms: принтер, отчёт, РП / Domain terms: document printer, PrinterDoc, paper printing, game map pool / Prototype candidates: PrinterDoc, PrinterDocMachineCircuitboard, PrinterDocFlatpack / Code symbols: GameMapPrototype, map pools.

## Repository investigation
FILE: Resources/Locale/ru-RU/ss14-ru/prototypes/_corvaxgoob/entities/structures/machines/printer.ftl
SYMBOL: ent-PrinterDoc = «принтер документов» (также ent-PrinterDocMachineCircuitboard, ent-PrinterDocFlatpack в production.ftl / flatpacks.ftl)
WHY RELEVANT: это единственное место во всём дереве, где вообще упоминается PrinterDoc.
CURRENT BEHAVIOR: локаль-сирота — прототипа с `id: PrinterDoc` в Resources/Prototypes НЕТ (rg -c 'PrinterDoc' Resources/Prototypes -g '*.yml' → 0 файлов). Слой контента `_corvaxgoob` отсутствует (есть только `_Corvax`, `_CorvaxNext`).

FILE: Resources/Maps/_Goobstation/amber.yml, box.yml, marathon.yml; Resources/Maps/.amber.yml
SYMBOL: proto-ссылки в картах
WHY RELEVANT: проверка наличия принтеров на картах.
CURRENT BEHAVIOR: `rg -l 'PrinterDoc' Resources/Maps` → пусто; в box.yml/amber.yml есть только UniformPrinter (одежда) и FaxMachineBase/FaxMachine* (факс, не документ-принтер). Т.е. в текущем дереве документ-принтера нет НИ НА ОДНОЙ карте, включая Box.

FILE: Resources/Prototypes/Maps/Pools/default.yml
SYMBOL: пул дефолтных карт
WHY RELEVANT: ротация карт.
CURRENT BEHAVIOR: строка 7: `#- Amber kil by upstream we still have ours but it testfails oh well.` — Amber ЗАКОММЕНТИРОВАНА из пула (upstream-тесты падают); в пуле `- Box`. Gamemap-прототипа Maps/amber.yml в Resources/Prototypes/Maps нет (файлы: box, fland, oasis, marathon, reach, bagel, saltern, exo, core, …).

## History
- `git log --all --oneline -S'PrinterDoc'` → 4cf391efb1 «The beginning of success (#4)» и triage-коммиты: строка PrinterDoc менялась только в раннем коммите (вероятно, выпилен вместе со слоем контента при ре-базе); в истории карт _Goobstation/ -S'PrinterDoc' совпадений нет [HIST].
- `git log -- Resources/Maps/_Goobstation/amber.yml`: недавние коммиты — апстрим-мержи (5ef9369475 «Merge branch 'master' into Upstream2», 2b8b9eb302 «Roundstart echem removal (#6706)»), т.е. карты перетащены из апстрима, локальный контент (принтеры) при этом потерян [HIST].
- После даты репорта (2025-09-21) принтеры на Amber/прочие карты так и не добавлены [HIST].

## Upstream comparison
Erida upstream: PrinterDoc исторически приходил из CorvaxGoob-слоя (локальные пути `_corvaxgoob/...` в ru-локали ss14-ru) [INFER]. Goob/Wizden: в текущем дереве прототип отсутствует. Не копал глубже — локальных доказательств достаточно.

## Verdict
CONFIG_OR_CONTENT_ISSUE — контент-решение: прототип PrinterDoc выпал из контент-слоя при переносе карт, и карты (включая Box) в текущем дереве его не содержат; Amber вдобавок выведена из ротации. Разработчики в треде подтвердили статус «временная заглушка, будут перенесены карты».

## Confidence
High

## Root cause
CONFIRMED: документ-принтер (PrinterDoc, «принтер документов») существует в текущем форке только как осиротевшая ru-локаль `ent-PrinterDoc` (Resources/Locale/ru-RU/ss14-ru/prototypes/_corvaxgoob/entities/structures/machines/printer.ftl); прототип (`id: PrinterDoc`) отсутствует в Resources/Prototypes, ни одна карта (Resources/Maps/_Goobstation/*.yml) на него не ссылается. Причина — потеря контент-слоя CorvaxGoob при апстрим-ре-базе карт; Amber дополнительно закомментирована в Resources/Prototypes/Maps/Pools/default.yml (строка 7).

## Proposed fix (do NOT implement!)
- Восстановить/перенести прототипы `PrinterDoc`, `PrinterDocMachineCircuitboard`, `PrinterDocFlatpack` (слой `_corvaxgoob` или `_Goobstation`) из источника CorvaxGoob/старого дерева.
- Расставить proto: PrinterDoc в.Resources/Maps/_Goobstation/{box,amber,marathon}.yml (офисы HoP/нsm/библиотека) либо добавить в пул карт.
- Раскомментировать Amber в Resources/Prototypes/Maps/Pools/default.yml после починки test-fail.

## Regression risk / Validation plan / Links-Related
Риск: дубли сущностей при повторном переносе карт; падение тестов карт (упомянуто в комментарии пула). Валидация: `rg -c 'PrinterDoc' Resources/Maps` > 0; запуск тестов gamemap; в игре — печать документа с принтера на Amber/Box. Related: batch B20 thread 038 (doc printer vanish, 1368929127822000138).

## Severity & complexity
Severity: S3 MINOR / Complexity: M

## Verdict JSON
См. .ai-triage/data/verdicts/batch_B24a.json
