# 1984 отменяется до поправления Эриды.

## Metadata
Thread ID: 1365705684582404157 / Date: 2025-04-26T15:06:48Z / Author: Ykika (mlefur, id 602460075335876609) / Tags: none / Archive state: archived / Messages: 2 / Attachments: 1 (image.png, PNG 1920x1080)

## Discord evidence
- Автор: «Думаю, про правила известно, думаю и без меня скрин/доступ к консоли есть у каждого, но скину это потому что.. Могу?» — прикладывает скриншот; заголовок — мем-отсылка к «1984» (цензура/правила).
- Ответ abama: «Попробуй контент серверов и движков очистить в лаунчере» — стандартное средство при замусоренных content-паках лаунчера.
- Медиа: `export/attachments/1365705684582404157/1365705685018476584_image.png` — скриншот клиентской консоли/окна «Правила» с бесконечным спамом ошибок.
- Классификация медиа: BUG_EVIDENCE (скриншот спама клиентских ошибок). Прямой рендер изображения недоступен в среде триажа; содержимое подтверждено OCR (tesseract): повторяющиеся строки
  `[ERRO] Couldn't find the following prototype: RulesBackmen. Falling back to DefaultRuleset, please check` и `Attempted to resolve invalid ProtoId<GuideEntryPrototype>: RulesBackmen` — т.е. окно правил («Rules and info») не находит прототип правил и сыплет ошибками.

## Search vocabulary
User terms: «правила», «скрин/доступ к консоли», «лаунчер», «1984» (мем) / Domain terms: rules window, guide entry, ruleset, prototype fallback, launcher content packs / Prototype-entity candidates: GuideEntryPrototype (RulesBackmen, DefaultRuleset, CoreRules) / Code symbols: InfoUIController, SendRulesInformationMessage, CCVars.RulesFile, RulesManager

## Repository investigation
FILE: Content.Client/UserInterface/Systems/Info/InfoUIController.cs
SYMBOL: InfoUIController.RulesEntryId, InfoUIController.DefaultRuleset, InfoUIController.OnRulesInformationMessage(SendRulesInformationMessage), Log.Error("Couldn't find the following prototype: {RulesEntryId}. Falling back to {DefaultRuleset}, please check that the server has the rules set up correctly")
WHY RELEVANT: точный источник строк ошибки со скриншота: клиент берёт id прототипа правил из серверного сообщения и при отсутствии такого GuideEntryPrototype в менеджере прототипов логирует эту ошибку и откатывается на DefaultRuleset.
CURRENT BEHAVIOR: при невалидном id — Log.Error + fallback на DefaultRuleset (функционально правила всё равно открываются дефолтным набором).

FILE: Content.Server/Info/RulesManager.cs
SYMBOL: RulesManager → `CoreRules = _cfg.GetCVar(CCVars.RulesFile)` (в SendRulesInformationMessage)
WHY RELEVANT: id правил, отправляемый клиенту, задаётся серверным cvar `server.rules_file`; «RulesBackmen» мог прийти только от сервера с такой конфигурацией/контентом.
CURRENT BEHAVIOR: Erida-сервер шлёт id из своего cvar (набор Erida — «CoreRules»/DefaultRuleset).

FILE: Content.Shared/CCVar/CCVars.Server.cs (строки 20–23)
SYMBOL: CCVars.RulesFile = CVarDef.Create("server.rules_file", "DefaultRuleset", ...)
WHY RELEVANT: конфигурационный вектор наблюдаемого id.
CURRENT BEHAVIOR: по умолчанию DefaultRuleset.

FILE: Resources/Prototypes/_Erida/Guidebook/rules.yml
SYMBOL: guideEntry CoreRules, RuleEridaC0..RuleEridaC8 (children); а также Resources/Prototypes/Guidebook/rules.yml: DefaultRuleset (text "/ServerInfo/Guidebook/_Erida/ServerRules/DefaultRules.xml", помечено «Erida edit»)
WHY RELEVANT: собственный набор правил форка существует и полон; `RulesBackmen` в нём отсутствует.
CURRENT BEHAVIOR: корректный ruleset Erida.

FILE: (поиск) rg "RulesBackmen" по Resources и git log -S'RulesBackmen' --all
SYMBOL: —
WHY RELEVANT: прототип `RulesBackmen` отсутствует в контенте репозитория И НИКОГДА не существовал в истории форка (git -S пуст). «RulesBackmen» — прототип стороннего SS14-форка (Backmen).
CURRENT BEHAVIOR: наблюдаемые на скриншоте ошибки не воспроизводятся на чистом контенте Erida — источник: чужой/замусоренный набор контента у клиента либо скриншот сделан на другом сервере.

## History
- `git log -S'RulesBackmen' --oneline --all` — пусто: символ никогда не вводился и не удалялся в форке.
- Регрессий, связанных с правилами/guidebook, вокруг даты репорта (2025-04-26) не найдено.
- Вывод: не регрессия форка.

## Upstream comparison
Not investigated — local evidence sufficient: id `RulesBackmen` чужд всей линейке репозитория (Goob → EridaGoob → Erida).

## Verdict
USER_ERROR_OR_NOT_BUG — клиентское окружение: контент стороннего форка (Backmen) в кэше лаунчера либо скриншот с чужого сервера; контент Erida согласован, воспроизводящего кода в репозитории нет.

## Confidence
Medium

## Root cause
CONFIRMED (механизм): окно правил клиента (InfoUIController) получило id `RulesBackmen`, отсутствующий в загруженном наборе прототипов → Log.Error + fallback на DefaultRuleset. Причина попадания невалидного id — окружение пользователя (замусоренные content-паки лаунчера/чужой контент), НЕ код форка: `RulesBackmen` отсутствует в репозитории и его git-истории.

## Proposed fix (do NOT implement!)
В коде ничего. Пользователю: очистить «контент серверов и движков» в лаунчере (как и посоветовал abama). Админам (опционально, вне репо): убедиться, что на проде Erida cvar `server.rules_file` = «CoreRules» (Erida ruleset).

## Regression risk / Validation plan / Links-Related threads
Regression risk: none (изменений кода нет). Validation plan: на чистом клиенте с чистыми пакетами открыть окно правил на Erida — спам ошибок должен отсутствовать. Related threads: 1363943524676010095 (правила канала), 1400493654493233195 (аналогичная рекомендация «очистить движки/библиотеку лаунчера» при проблемах подключения).
