# Воксы...

## Metadata
Thread ID: 1429385681876025414 / Date: 2025-10-19 / Author: Товарищ Дементор / Tags: unknown_tag / Archive state: explained in-thread / Messages: 6 / Attachments: 2 images (BUG_EVIDENCE: damage/status UI, not read)

## Discord evidence
- Wiki claims vox have "пассивная регенерация ядов"; reporter stood 7 minutes and poison damage did not decrease.
- Replies (Кат_Л, bebrus): passive regen (0.2/tick poison, 0.7 heat) only works while TOTAL damage is below/equal 20 — the condition is missing from the wiki.

## Search vocabulary
User terms: воксы, пассивная регенерация ядов, яды не падают / Domain terms: species passive regeneration, poison metabolism, damage cap / Prototype candidates: BaseMobVox, PassiveDamage / Code symbols: PassiveDamageComponent, DamageCap, SplitEnsureAllDamagedAndOrganic.

## Repository investigation
FILE: Resources/Prototypes/Entities/Mobs/Species/vox.yml
SYMBOL: `- type: PassiveDamage` on BaseMobVox (lines 86-97)
WHY RELEVANT: the actual regen source:
```
splitBehavior: SplitEnsureAllDamagedAndOrganic
allowedStates: [Alive]
damageCap: 20
damage: { types: { Heat: -0.07, Poison: -0.2 }, groups: { Brute: -0.07 } }
```
CURRENT BEHAVIOR: poison heals at -0.2/tick (heat -0.07, brute -0.07) — regen EXISTS and matches the thread numbers; it is gated by `damageCap: 20`, so with total damage ≥ 20 nothing regenerates — matching Кат_Л/bebrus and the reporter's 7-minute observation.

FILE: Content.Shared/Damage/Components/PassiveDamageComponent.cs
SYMBOL: DamageCap (lines ~37-40: "The maximum HP the damage will be given to. If 0, disabled.")
WHY RELEVANT: implements the threshold that stops healing past 20 total damage.

FILE: Resources/Prototypes/Entities/Mobs/Species/vox.yml
SYMBOL: line 232 commented-out `#- type: PassiveDamage # Slight passive regen... # Specified above already`
WHY RELEVANT: confirms the single authoritative regen block above.

## History
Relevant commits: 0572564025 "[Fix] Bug fixes (#32)", 1f9241fca2 "hello (#10)" — fork git dates are internally inconsistent (2026 dates vs 2025 reports); no evidence the regen block changed after the report. Current values match the in-thread explanation, so no post-report fix is implied.

## Upstream comparison
Goob/Erida: `PassiveDamage` with `SplitEnsureAllDamagedAndOrganic` is a Goob-station med-split feature (component comment: "Split for damage and SplitEnsureAllDamagedAndOrganic for passive regen"); upstream SS14 has no equivalent vox regen. Not investigated further — local evidence sufficient.

## Verdict
CONFIG_OR_CONTENT_ISSUE — the mechanic works as coded; the wiki description omits the `damageCap: 20` condition (and the actual heat value is 0.07, not 0.7). Content/wiki decision: document the cap or raise it.

## Confidence
High

## Root cause
CONFIRMED: vox regen is `PassiveDamage` with `damageCap: 20` (vox.yml:86-97) — while the character's total damage is ≥ 20, no poison/heat/brute regen applies; the reporter's expectation came from an incomplete wiki description, not from a code fault.

## Proposed fix (do NOT implement!)
Wiki: describe the regen condition (total damage ≤ 20, values 0.2 poison / 0.07 heat / 0.07 brute per tick). If the design intent is unconditional poison regen, raise `damageCap` in vox.yml — content tuning decision for maintainers.

## Regression risk / Validation plan / Links-Related threads
Wiki-only change → no risk. Validation (if tuning): vox with >20 total damage → no regen; with ≤20 → poison falls 0.2/tick. Related: none.

## Severity & complexity
Severity: S4 COSMETIC / Complexity: XS

## Verdict JSON
See .ai-triage/data/verdicts/batch_B31.json
