# А как встать.

## Metadata
Thread ID: 1368929127822000138 / Date: 2025-05-05 / Author: BlackAngel (blck_angl) / Tags: none / Archived: yes / Messages: 1 / Attachments: 1 PNG (2025-05-05_22-34-31.png, reactions ⭐3 — classified BUG_EVIDENCE; not renderable in triage env)

## Discord evidence
- Root: "При попытке встать - ударяюсь своей тупой башкой об след" — while trying to stand up the player "hits their head" on a footprint trail; title "А как встать." implies they are stuck crawling.
- Context: dated 2025-05-05, ~3 weeks after the Footprints port; crawling leaves "body" prints (FootprintSystem.FootprintInteraction state "body" when not standing).
- Expected: standing up works; actual: crawl→stand interaction with trail causes a slip/bump.

## Search vocabulary
User terms: встать→stand up (StandingState), удариться башкой→head bump/damage, след→footprint/bodyprint, ползти→crawl (layout/crawling).
Domain terms: FootprintOwnerComponent.FootDistance/BodyDistance, Slippery, slipData.requiredSlipSpeed, SticksToSkin, superSlippery.

## Repository investigation
FILE: Content.Goobstation.Server/Footprints/FootprintSystem.cs
SYMBOL: OnMove lines 66-125 (standing ? FootDistance : BodyDistance; Footprints.Add(... standing ? "foot" : "body"))
WHY RELEVANT: crawling players leave bodyprints each BodyDistance step.
CURRENT BEHAVIOR: bodyprints spawn under a crawling player; combined with lube contents this made crawl/stand cycles slip-prone in the report era.

FILE: Resources/Prototypes/_White/Entities/Effects/puddle.yml
SYMBOL: Footprint prototype (id: Footprint, lines 5-35): Clickable, anchored, Physics bodyType Static, Fixtures slipFixture (mask ItemMask / layer SlipLayer, hard: false)
WHY RELEVANT: footprints are non-hard slip-capable fixtures under the crawler; no collision for standing per se — the "head bump" reads as the slip/stun consequence, not hard collision [INFER].

FILE: Resources/Prototypes/Reagents/cleaning.yml
SYMBOL: SpaceLube (lines 103-120): slipData.requiredSlipSpeed: 0.1 # Goobstation - no crawling to avoid it; superSlippery: true; sticksToSkin: false
WHY RELEVANT: direct tuning commit that disables lube slipping for crawling and stops lube from sticking to skin (no more lube footprints/bodyprints at all).

FILE: Content.Goobstation.Common/Footprints/FootprintComponent.cs / FootprintOwnerComponent.cs
SYMBOL: Footprint list; MaxFootprintVolume/MaxBodyVolume fields
WHY RELEVANT: footprint formation parameters used by OnMove.

## History
- 71790ef585 2025-04-16 [PORT] Footprints refactor (#2389) — feature present at report date (in HEAD).
- 8122b6a82b 2025-05-18 lube tweaks (#2836) — requiredSlipSpeed 1 → 0.1 with comment "no crawling to avoid it" + sticksToSkin: false for SpaceLube; diff verified (ReagentPrototype.SticksToSkin added; PuddleSystem re-adds non-stick reagents). In HEAD, 13 days after report.
- 6b19bdd93a 2025-07-22 [Tweak] No footprints without gravity (#3585) — in HEAD.

## Upstream comparison
Goob: lube tweaks (#2836, Ilya246) upstream Erida received; SS14 upstream: no footprints. No further upstream check — local evidence sufficient.

## Verdict
ALREADY_FIXED_IN_CURRENT_FORK

## Confidence
Medium

## Root cause
CONFIRMED (history) for the slip part / HYPOTHESIS for the literal "head bump": in the report era a crawling player over a lube bodyprint trail could trigger lube slips (pre-tweak requiredSlipSpeed allowed crawling slips — see the commit's own comment "no crawling to avoid it"), knocking the crawler down again while trying to stand — perceived as "hitting head on the trail". 8122b6a82b (2025-05-18) made SpaceLube sticksToSkin:false (no lube prints) and tuned requiredSlipSpeed so crawling no longer slips; lube bodyprints no longer form. The exact head-impact mechanic was not reproduced from code [INFER, media unrendered].

## Proposed fix (do NOT implement!)
None — fixed by 8122b6a82b. Optional: verify bodyprints from mixed puddles (sticky reagent + lube) cannot still slip a crawler; if needed, gate SlipEvent for non-standing mobs in SharedSlipperySystem path.

## Regression risk / Validation plan / Links-Related threads
Low. Validation: spill SpaceLube, crawl through it, attempt stand — expect no slip/bump; walk through lube while standing — slip still works (superSlippery preserved). Related: 1391191286165540995 (footprints-lube-slip); B19 thread 1368875720373309480 (crawl footprints).

## Severity & complexity (if actionable)
Severity: S3 MINOR / Complexity: S
