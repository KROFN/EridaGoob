# Акимбо баг

## Metadata
Thread ID: 1497884085656485989 / Date: 2026-04-26 / Author: Mokko (mokko1111) / Tags: Незначительный / Archive state: archived / Messages: 18 / Attachments: 0

## Discord evidence
- [SRC] Root: когда у одной пушки кончаются патроны и ты достаёшь вторую во вторую руку, игра считает, что выстрел — акимбо: одна пушка "КОСО" стреляет, другая щёлкает пустым затвором.
- [SRC] vanomorodellefake (2026-05-03): "на губах точно также", "ток они в отдельных класс засунули" — обсуждение реализации/перекладки.
- [SRC] vanomorodellefake (2026-05-23): "решил что лучше будет переписать" — дев-решение переписать систему целиком.
- [SRC] Mokko (2026-07-28): "всё ещё кстати есть" — баг воспроизводился спустя 3 месяца после репорта.

## Search vocabulary
User terms: акимбо, пустой затвор, перекладка пушки / Domain terms: akimbo, dual wield, offhand, gun empty, WeaponSystem / Prototype-entity candidates: — / Code symbols: (ожидался) AkimboComponent, AkimboSystem

## Repository investigation
- Поиск: case-insensitive `akimbo` по всему дереву репозитория → 0 файлов [SRC]. Ни Content.Shared/_Goobstation, ни Content.* не содержат символов Akimbo.
- Pickaxe `git log -i -S akimbo --all` → совпадения только в triage-коммитах индексации баг-трекера [HIST]; подтверждения существования кода в истории форка не найдено (важно: -i c -S работает не во всех режимах, результат считать осторожно [?]).
- Вывод: система акимбо в текущем чекауте отсутствует — вероятно, живёт в зависимости/модуле вне этого репозитория, либо была вырезана/переименована в рамках обещанной перезаписи (2026-05-23).
- Примечание: репорт датирован 2026-04-26; последнее подтверждение бага — 2026-07-28, но дев уже говорил о переписи 2026-05-23. Статус после переписи неизвестен.

## History
- [HIST] Коммитов, добавляющих/меняющих/удаляющих akimbo-код, не найдено (pickaxe дал только triage-индексацию: 29a7b080ed, 4d5dbbe6f0, 5c8541decd).
- Прототипы Goob-контента (_Goobstation) в дереве присутствуют, но akimbo среди них нет.

## Upstream comparison
Goob-Station upstream: акимбо реализован в Goob (Content._Goobstation, отдельные классы — о чём и говорит участник "в отдельных класс засунули") [UP]. В дереве Erida система не обнаружена — сравнение поведения невозможно без кода.

## Verdict
CANNOT_VERIFY — баг многократно подтверждён игроками, но реализация акимбо отсутствует в текущем дереве репозитория: код не найти, причинную цепочку не доказать. Переписка в треде указывает, что система планировалась к переписи; требуется ретест на актуальной сборке и/или указание репозитория/модуля, где живёт akimbo-код.

## Confidence
Medium

## Root cause
HYPOTHESIS (Low): проверка "две пушки в руках → акимбо" не учитывает, что вторая пушка пуста/только что взята, поэтому выстрел основного оружия трактуется как акимбо с разбросом. Доказательств в коде нет — символа в дереве нет [?].

## Proposed fix (do NOT implement!)
- Сначала локализовать код: подтвердить у дев-команды, где живёт акимбо после перезаписи (внеэтот репо или ветка). Затем: в offhand-логике требовать, чтобы вторая пушка имела боезапас и была "в состоянии акимбо" (взведена), прежде чем применять разброс/двойной выстрел.

## Regression risk / Validation plan / Links-Related threads
Risk: —. Validation: опустошить одну пушку, выхватить вторую, стрелять — не должно быть акимбо-разброса/сухого щелчка второй руки. Related: B09 (weapons) — прямых дублей в батче нет.

## Severity & complexity (if actionable)
Severity: S3 MINOR / Complexity: M

## Verdict JSON (also written to batch file)
см. .ai-triage/data/verdicts/batch_B34.json
