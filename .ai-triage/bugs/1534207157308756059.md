# Не сбросилось время (((0(((

## Metadata
Thread ID: 1534207157308756059 / Date: 2026-08-04T14:31:30Z / Author: Yuika (mlefur2.0, id 582970156674973758) / Tags: [Разработка] / Archive state: not archived / Messages: 3 / Attachments: 0

## Discord evidence
- Автор: «У всех время сбросилось, а я какой-то особенный йухан. Как фиксить?» — серверная акция по обнулению наигранного времени не затронула этот аккаунт.
- Σ Ξ ✦ Jill Stingray: «как ивент мастер и часть проекта ты имеешь доступ ко всем ролям, потому-то не сбросилось» (версия: данные смешались с админ/ролевыми).
- Автор уточняет: «Не, именно статистика не сбросилась, понимаешь?» — речь о статистике наигранного времени (playtime).
- Медиа: нет.

## Search vocabulary
User terms: «время не сбросилось», «статистика» / Domain terms: playtime, overall playtime, playtime reset/wipe, play_time DB, tracker Overall / Prototype-entity candidates: none / Code symbols: PlayTimeCommands, PlayTimeTrackingManager, EridaWebhooks.SendTimeChangedMessage

## Repository investigation
- FILE: Content.Server/Administration/Commands/PlayTimeCommands.cs
  SYMBOL: PlayTimeAddOverallCommand (`playtime_addoverall`, line 97+), PlayTimeAddRoleCommand (`playtime_addrole`, с Erida-добавкой вебхука), PlayTimeGetOverallCommand, PlayTimeGetRoleCommand, PlayTimeSaveCommand (`playtime_save`), PlayTimeFlushCommand (`playtime_flush`)
  WHY RELEVANT: полный набор админ-команд playtime — только add/get/save/flush. Команды массового сброса/обнуления статистики в коде НЕТ → «сброс у всех» был ручной операцией над БД, а не кодовой фичей.
  CURRENT BEHAVIOR: `playtime_addoverall <user> <minutes>` — plain число парсится через int.TryParse (PlayTimeCommandUtilities.CountMinutes, lines 57–59), т.е. принимает и отрицательные значения.
- FILE: Content.Server/Players/PlayTimeTracking/PlayTimeTrackingManager.cs
  SYMBOL: SaveSession (line 242), DoSaveSessionAsync (line 292)
  WHY RELEVANT: наигранное время персистится в серверную БД (таблица play_time) периодически и при выходе; «не сбросилось» = строка БД этого пользователя не была затронута wipe-ом либо время дозаписалось после него (flush/save), либо строка была исключена из выборки (например, по признаку админ-аккаунта).
- FILE: Content.Server/_Erida/Discord/EridaWebhook.Playtime.cs
  SYMBOL: EridaWebhooks.SendTimeChangedMessage
  WHY RELEVANT: ручные изменения времени админами логируются в Discord — подтверждает, что управление playtime на Erida — admin/DB-процесс, а не игровой механизм.
- В коде нет цикла/команды, которая могла бы «выборочно пропустить» одного пользователя — несоответствие объясняется данными, а не логикой.

## History
No relevant commits found — механизма сброса статистики в коде нет ни до, ни после даты репорта (2026-08-04).

## Upstream comparison
Не применимо — «сброс статистики» самодельная DB-операция администрации, upstream-фичи нет.

## Verdict
USER_ERROR_OR_NOT_BUG — у конкретного аккаунта playtime не обнулился из-за ручной DB-операции (строка не обработана/исключена/дозаписана после), а не из-за дефекта кода; в кодовой базе сброс не реализован вовсе. Персонал в треде указал на ролевой/админ-статус аккаунта как причину.

## Confidence
Medium

## Root cause
CONFIRMED (механика): playtime хранится в серверной БД (PlayTimeTrackingManager.SaveSession → DB); кодового массового сброса нет (PlayTimeCommands.cs — только add/get/save/flush), значит обнуление выполнялось вручную по БД; у данного аккаунта строка не была обнулена/время было восстановлено записью. Точная причина пропуска строки по треду неустановима (логов операции нет) — данные, не код.

## Proposed fix (do NOT implement!)
Вне кода: скорректировать значение пользователю (SQL по таблице play_time либо команда `playtime_addoverall Yuika -<минуты>` — отрицательное plain число поддерживается веткой int.TryParse). На будущее: выполнять wipe транзакционно по всей таблице (включая админ-аккаунты) и логировать операцию (вебхук уже существует).

## Regression risk / Validation plan / Links-Related threads
Regression risk: none. Validation plan: `playtime_getoverall <user>` после коррекции (PlayTimeGetOverallCommand). Related threads: 1513668580212871330 (тот же класс данных — playtime в серверной БД, но другой инцидент: потеря данных после падения БД).

## Severity & complexity (if actionable)
Severity: S4 COSMETIC (личная статистика одного игрока) / Complexity: XS (точечная правка данных).

## Verdict JSON (also write to batch file)
{"thread_id":"1534207157308756059","verdict":"USER_ERROR_OR_NOT_BUG","confidence":"Medium","severity":"S4","complexity":"XS","cluster_hint":null,"root_cause_short":"Playtime хранится в БД; кодового массового сброса нет (только add/get/save/flush) — обнуление было ручной DB-операцией, строка этого аккаунта не обработана","evidence":["Content.Server/Administration/Commands/PlayTimeCommands.cs:PlayTimeAddOverallCommand/PlayTimeGetOverallCommand","Content.Server/Players/PlayTimeTracking/PlayTimeTrackingManager.cs:242:SaveSession","Content.Server/_Erida/Discord/EridaWebhook.Playtime.cs:SendTimeChangedMessage"],"dossier":".ai-triage/bugs/1534207157308756059.md"}
