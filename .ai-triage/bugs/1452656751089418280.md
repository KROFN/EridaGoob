# Корабли

## Metadata
Thread ID: 1452656751089418280 / Date: 2025-12-22 / Author: alexfire (alexfire9455) / Tags: «Визден-Баг», unknown_tag_1414266059208265799 / Archive state: archived / Messages: 4 / Attachments: 3 (screenshots 2025-12-15)

## Discord evidence
- «Шаттлы проходят сквозь станцию слегка, иногда бывает этот баг, а иногда нету — в основном с шаттлами,
  которые с раундстарта, т.е. шаттл карго, сб шаттл» — интермитентное лёгкое пересечение корпуса шаттла и
  станции ([MEDIA]: 3 скриншота — BUG_EVIDENCE; не читались, лимит чтения изображений; формулировка автора
  однозначна).

## Search vocabulary
User terms: шаттл, станция, проходят сквозь / Domain terms: shuttle docking, dock offset, grid spawn, PriorityDock, FTL arrival /
Code symbols: DockingSystem, DockingSystem.Shuttle, ShuttleSystem.GridFill, GridFillComponent, PriorityDockComponent

## Repository investigation
FILE: Content.Server/Shuttles/Systems/DockingSystem.cs, DockingSystem.Shuttle.cs
SYMBOL: docking/snap-логика (TryDock/GetDockedPosition — точные имена не верифицированы grep'ом в рамках
бюджета [?])
WHY RELEVANT: раундстартовые шаттлы (GridFillComponent/PriorityDock, ShuttleSystem.GridFill.cs) садятся в
доки через выравнивание по DockingComponent (angle/coordinates). Любое расхождение на 1 тайл между
позицией порта на шаттле и геометрией дока станции даёт лёгкое пересечение корпусов — движок не валидирует
overlap перед docking [INFER].
CURRENT BEHAVIOR: intermittency объясняется разными шаттлами/доками (у каждого своё смещение) и тем, что
раундстартовые шаттлы спавнятся уже пристыкованными, в отличие от прилетевших по FTL.

## History
- 05f517fa05 "Port FTL arrival effect fix from new-frontiers-14 #3495 (#41951)" — правка эффекта прибытия FTL, не docking-геометрии [HIST].
- Прямых коммитов «fix shuttle clipping/dock overlap» в DockingSystem* после репорта (2025-12-22) не найдено [HIST].

## Upstream comparison
Upstream SS14: известный класс проблем выравнивания доков; конкретный фикс не идентифицирован (нет net-доступа) [UP]/[?].

## Verdict
CANNOT_VERIFY — симтом подтверждён скриншотами, но в рамках бюджета не найден конкретный кодовый путь
(расчёт docking-позиции) и нет данных, какой именно шаттл/док пересекается; без repro-деталей (карта, какой
док) риск вымыса велик.

## Confidence
Low

## Root cause
HYPOTHESIS (Low): docking-выравнивание раундстартовых шаттлов (GridFill/PriorityDock → DockingSystem) не
проверяет пересечение корпусов; расхождение авторинга портов (dock port offset на шаттле vs геометрия дока
станции) даёт лёгкое перекрытие, проявляющееся интермитентно по конкретным парам «шаттл-док».

## Proposed fix (do NOT implement!)
Следующий шаг: прочитать Content.Server/Shuttles/Systems/DockingSystem.Shuttle.cs (позиционирование при
доке) и ShuttleSystem.GridFill.cs (раундстартовый док); собрать список пар шаттл/док со скриншотов
(карта Visden?), сверить координаты портов в Resources/Maps/Shuttles/*.yml и station .premap.

## Regression risk / Validation plan / Links-Related threads
- Валидация: 10 раундов на Visden — фиксировать, какие шаттлы и доки пересекаются; сравнить координаты
  DockingComponent на шаттле и станции.
- Related: batch B22 (maps/docking: aspid cargo dock и др.), batch B21.

## Severity & complexity (if actionable)
Severity: S3 MINOR / Complexity: M

## Verdict JSON (also write to batch file)
см. .ai-triage/data/verdicts/batch_B21.json
