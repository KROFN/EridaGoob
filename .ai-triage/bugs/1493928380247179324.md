# Отображение предыдущих носимых предметов

## Metadata
Thread ID: 1493928380247179324 / Date: 2026-04-15 / Author: Mokko / Tags: Визден-Баг, unknown_tag / Archive state: archived / Messages: 1 / Attachments: 1 (image.webp — НЕ отрисовался в контексте субагента; классификация presumed BUG_EVIDENCE, не подтверждена)

## Discord evidence
- Title: «Отображение предыдущих носимых предметов». Text (сарказм): «Ахуенно будет, если забагает какой-нибудь арбалет или ствол. Ни капли не палевно».
- Интерпретация: при смене/снятии носимого предмета спрайт ПРЕДЫДУЩЕГО предмета продолжает отображаться на персонаже; автор опасается, что то же случится с оружием (арбалет/ствол) — «палевно», т.е. спалит скрытое ношение.
- Репро-шаги, конкретный слот, клиент/сервер — не указаны; ответов нет. Медиа недоступно для просмотра → точный сценарий не подтверждён.

## Search vocabulary
User terms: предыдущие носимые предметы, остаётся спрайт / Domain terms: equipment visuals, GotEquipped/GotUnequipped, clothing layers, map keys, swap race / Prototype-entity candidates: n/a / Code symbols: ItemSystem.OnEquipped, ItemSystem.OnUnequipped, VisualsChangedEvent, HumanoidAppearanceSystem.UpdateLayers

## Repository investigation
FILE: Content.Client/Items/Systems/ItemSystem.cs
SYMBOL: OnEquipped / OnUnequipped (GotEquippedEvent/GotUnequippedEvent → `_sprite.SetVisible(false/true)`)
WHY RELEVANT: скрывается только «мировой» спрайт предмета при надевании; OnUnequipped безусловно включает видимость — рассинхрон при быстрой замене возможен здесь.
CURRENT BEHAVIOR: видимость мирового спрайта toggлится безусловными вызовами; гонки с другими подписчиками не обрабатываются.

FILE: Content.Client/Inventory/ClientInventorySystem.cs
SYMBOL: — (grep `GetEquipmentVisualsEvent|RemoveLayer|ClearLayers` — совпадений НЕТ)
WHY RELEVANT: удаление/замена clothing-слоёв в этом файле не выполняется → слоты носителя обслуживаются другим визуализатором (предположительно HumanoidAppearanceSystem/attachment-система Goob), который в рамках лимита вызовов не локализован.
CURRENT BEHAVIOR: точка отказа «старый слой не снят при замене» не найдена — проверка остановлена лимитом.

FILE: Content.Client/Humanoid/HumanoidAppearanceSystem.cs
SYMBOL: UpdateLayers (скрытие «старых» базовых слоёв, lines ~83–115), ApplyMarkingSet, `_sprite.LayerMapReserve`
WHY RELEVANT: общий life-cycle слоёв на носителе; clothing-слои добавляются/снимаются в родственном коде, где и может «залипать» предыдущий предмет.

## History
no relevant commits found (ItemSystem.cs: только SPDX/cleanup — 06b00b9795, 969a610b24, 9a2a6f44a6, 68a8008ce9; изменений логики видимости нет).

## Upstream comparison
Not investigated — код-путь отрисовки экипировки не локализован в лимитах; сравнение с upstream бессмысленно до локализации.

## Verdict
CANNOT_VERIFY — репорт без шагов воспроизведения; скриншот недоступен для просмотра в контексте субагента; слот и последовательность действий неизвестны; «залипающий» код-путь не локализован. По стандарту доказательности — CANNOT_VERIFY вместо гипотезы-верdict.

## Confidence
Low

## Root cause
HYPOTHESIS: при замене/снятии носимого предмета слой с визуалом старого предмета не снимается (или предмет остаётся видимым из-за безусловного `_sprite.SetVisible(true)` в ItemSystem.OnUnequipped) — клиентский рассинхрон слоёв носителя при быстрой смене. Нужны скриншот/репро для локализации. (Low)

## Proposed fix (do NOT implement!)
- Сначала сбор репро: слот, последовательность (swap / unequip / деспавн предмета / выход из поля зрения), клиент.
- Затем локализовать визуализатор слоёв экипировки и проверить удаление слоёв по map-key при GotUnequipped; при необходимости — явное снятие слоя вместо toggle видимости.

## Regression risk / Validation plan / Links-Related threads
Risk: n/a (фикса нет). Validation: надеть A → заменить на B → снять → удалить A из мира; спрайт A не должен оставаться ни в одном из случаев, включая перезаход клиента. Related: 1449885909653651496 (per-hand визуалы того же ItemSystem.cs).

## Severity & complexity (if actionable)
Severity: S4 (если подтвердится) / Complexity: S (расследование)

## Verdict JSON (also write to batch file)
См. .ai-triage/data/verdicts/batch_B05.json
