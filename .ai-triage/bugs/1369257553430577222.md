# Сапожник без сапог

## Metadata
Thread ID: 1369257553430577222 / Date: 2025-05-06 / Author: TehniQ (tehnik_08) / Tags: На рассмотрении / Archive state: archived / Messages: 4 / Attachments: 0

## Discord evidence
- Мед-киборг с продвинутыми хирургическими инструментами не может оперировать: по нажатию кнопки «начать операцию» не появляется меню выбора части тела и операций.
- Repro: играть за мед-киборга, поставить модуль с (продвинутыми) хирургическими инструментами, попытаться начать операцию. Перезаход в игру, снятие/установка модуля — не помогают.
- Ответы: автор напоминал (2025-05-15); админ cybertrash (2025-06-02): «Исправления багов медицинского дрона временно приостановлены как и все работы с медициной» — т.е. проблема признана, но работа заморожена.
- Media: none.

## Search vocabulary
User terms: мед киборг, продвинутые инструменты хирургические, начать операцию, меню выбора части тела
Domain terms: medical cyborg, borg module, surgery tool, surgery UI / BUI, body part selection
Prototype-entity candidates: мед-модуль борга, SurgeryTool (tools.yml), SurgeryBui
Code symbols: SurgeryToolComponent, AttemptStartSurgery, SurgeryBuiState, SurgeryWindow

## Repository investigation
FILE: Content.Shared/_Shitmed/Surgery/SharedSurgerySystem.Start.cs
SYMBOL: AttemptStartSurgery (строки 27–40), OnUtilityVerb (42–62)
WHY RELEVANT: единственный путь старта операции: верб на SurgeryToolComponent → OpenUi(target, SurgeryUIKey.Key, user).
CURRENT BEHAVIOR: проверок на силикон/киборга НЕТ (только IsLyingDown, self-surgery cvar). Явного блокера для борга в текущем коде не видно.

FILE: Content.Client/_Shitmed/Medical/Surgery/SurgeryBui.cs
SYMBOL: RefreshUI (270–320)
WHY RELEVANT: клиент требует, чтобы `_player.LocalEntity` (т.е. ХИРУРГ) имел `SurgeryTargetComponent` с `CanOperate` (строки 275–277), иначе шаги операции не активируются/не обновляются.
CURRENT BEHAVIOR: для борга-хирурга, у которого шасси не имеет SurgeryTargetComponent, кнопки шагов остаются неактивными; для списков частей тела (Update, 55–172) блока нет.

FILE: Resources/Prototypes/Entities/Objects/Tools/tools.yml
SYMBOL: `type: SurgeryTool` (строки 62, 129, 370 — скальпель/ретрактор/дрель и продвинутые варианты)
WHY RELEVANT: инструменты модуля — SurgeryToolComponent, вербы доступны.
CURRENT BEHAVIOR: инструмент валиден; модуль мед-киборга выдаёт эти инструменты в руки (borg hands).

## History
- 1ce0c11d58 «fucking predict surgery» (#4040, 2025-09-07) — крупная переделка старта/предикции хирургии ПОСЛЕ отчёта (2025-05-06).
- 58ed77be1a «Lobotomies were fixed...» (#5354), c2a4380507 «Fix examine texture» (#6193, 2026-03-29).
- git log -S'Silicon' --all -- Content.*_Shitmed: изменений с блокировкой/разблокировкой силиконов в хирургии не найдено.

## Upstream comparison
Erida upstream: Shitmed — форк-медицина, специфичный для Goob/Erida; прямого апстрим-фикса не искал — локальных доказательств для вердикта достаточно/недостаточно (см. вердикт).

## Verdict
CANNOT_VERIFY — в текущем коде нет найденного блокера «синтетикам запрещено открывать интерфейс»; отчёт от мая 2025 предшествует серьёзной переработке хирургии (#4040, сентябрь 2025). Воспроизвести/доказать цепочку по коду нельзя; отвергнуть тоже нельзя.

## Confidence
Low

## Root cause
HYPOTHESIS (Low): клиентский гейт SurgeryBui.RefreshUI (Content.Client/_Shitmed/Medical/Surgery/SurgeryBui.cs:276) требует SurgeryTargetComponent.CanOperate на сущности ХИРУРГА; у шасси киборга компонента нет → UI шагов мёртв. Возможен и серверный отказ OpenUi, не видимый в коде. Точная причина на момент отчёта не восстановима (код переписан).

## Proposed fix (do NOT implement!)
1) Воспроизвести на текущем билде: мед-киборг + модуль, пациент на столе; проверить, открывается ли SurgeryWindow.
2) Если шаги мертвы — в SurgeryBui.RefreshUI проверять `Owner` (пациента), а не `_player.LocalEntity`, либо обеспечить SurgeryTargetComponent/CanOperate-флаг для силиконов-хирургов на сервере.

## Regression risk / Validation plan / Links-Related threads
Risk: низкий (UI-фикс). Validation: киборг проводит полный цикл операции (разрез→шаги→закрытие). Related: 1388553983613730969, 1372947789419516014 (кластер silicon-surgery-ui).

## Severity & complexity
Severity: S2 MAJOR (мед-киборг полностью лишён основного функционала) / Complexity: M
