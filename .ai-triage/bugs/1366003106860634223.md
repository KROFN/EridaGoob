# Холодильник и сумка ботаника.

## Metadata
Thread ID: 1366003106860634223 / Date: 2025-04-27 / Author: 𝗪𝗵𝗮𝘁𝗔𝗽𝗽𝗹𝗲 (whatapple) / Tags: — / Archive state: archived / Messages: 1 / Attachments: 0

## Discord evidence
- «На 1-2й день открытия был баг связанный с сумкой и холодильником Химика/Сервиса (Тот самый, который нельзя разобрать и собрать обратно). Он просто забрал сумку ботаника со всем продовольствием в ней и никак не хотел обратно. Можно было закликивать на Alt+ЛКМ и сумка вытаскивалась на пол секунды, но чтобы не делали — она вернётся в холодильник.»
- Ожидание: сумку можно достать обратно. Реально: сумка застревает внутри, Alt+ЛКМ временно «выталкивает», затем сумка возвращается.
- Отчёт с первых дней работы сервера (запуск ~2025-04-25/26), т.е. описывает старую сборку форка.
- Медиа: отсутствует (UNRELATED_MEDIA/none).

## Search vocabulary
User terms: холодильник, сумка ботаника, нельзя разобрать, Alt+ЛКМ / Domain terms: fridge, EntityStorage, SmartFridge, insert verb, whitelist, bag, PlantBag / Prototype-entity candidates: SmartFridge, SmartFridgeMedical / Code symbols: SmartFridgeSystem, DoInsert, EntityWhitelist

## Repository investigation
FILE: Resources/Prototypes/Entities/Structures/Machines/smartfridge.yml
SYMBOL: SmartFridge, SmartFridgeMedical (entity ids)
WHY RELEVANT: единственные прототипы «холодильников» в дереве (rg -il fridge → только smartfridge.yml + production.yml circuitboard). Старых FridgeChemistry/ServiceFridge и vending-машины с inventory SmartFridge нет (rg "inventory: SmartFridge" → пусто).
CURRENT BEHAVIOR: SmartFridge — storage-машина (Container `smart_fridge_inventory`), whitelist компонентов: Edible, FitsInDispenser, Pill, Produce, Seed (+ теги). Сумка ботаника (PlantBag, Resources/Prototypes/Entities/Objects/Specific/Hydroponics/tools.yml) не проходит whitelist.

FILE: Content.Shared/SmartFridge/SharedSmartFridgeSystem.cs
SYMBOL: DoInsert (строка 48), `_whitelist.CheckBoth(used, ent.Comp.Blacklist, ent.Comp.Whitelist)` (строка 59)
WHY RELEVANT: предметы, не прошедшие whitelist, просто пропускаются — сумку физически нельзя вставить в текущий SmartFridge.
CURRENT BEHAVIOR: всё, что попало в контейнер, выдаётся через UI (OnDispenseItem) — «потерять» предмет навсегда в текущей реализации негде; есть интеграционный тест Content.IntegrationTests/Tests/SmartFridge/SmartFridgeInteractionTest.cs.

## History
- 0e2c9e9307 2025-07-20 «Implement SmartFridge functionality (#38648)» — текущая storage-реализация появилась ПОСЛЕ отчёта (2025-04-27); апрельский холодильник был другой (legacy) системой, которой в дереве больше нет.
- a38216ba27 2025-08-22 «feat: add verb for smartfridge item insertion (#39807)».
- f74fe5cc75 2026-01-09 «feat: allow removing empty smart fridge entries (#39195)».

## Upstream comparison
Erida upstream (dead-space-server/EridaGoob): сменил реализацию вместе с апстримом SS14 #38648. Goob/SS14: storage-SmartFridge с whitelist и UI-выдачей — там же сценарий «сумка навсегда внутри» невозможен. Не копал глубже — локальных доказательств достаточно.

## Verdict
ALREADY_FIXED_IN_CURRENT_FORK — описанный баг относится к legacy-холодильнику из первых дней сервера; эта система/прототип в текущем форке отсутствует (заменена на SmartFridge #38648). В текущем коде (1) сумка не пройдёт whitelist и не будет вставлена (DoInsert:59), (2) любой вставленный предмет выдаётся через UI. Поведение «сумка навсегда внутри» в текущем дереве невоспроизводимо.

## Confidence
Medium (старую систему нельзя воспроизвести напрямую; вывод основан на отсутствии legacy-прототипов и на гарантированной извлекаемости в новой системе)

## Root cause
CONFIRMED (косвенно): баг жил в удалённой legacy-реализации холодильника (апрель 2025). Текущий SmartFridgeSystem + whitelist делают обе причины (застревание и невозврат) невозможными.

## Proposed fix (do NOT implement!)
Не требуется. Опционально: прогнать SmartFridgeInteractionTest на сценарий «вставить не-whitelisted предмет» — уже покрыто CheckBoth.

## Regression risk / Validation plan / Links-Related threads
Risk: none. Validation: в текущем клиенте Alt+ЛКМ/InteractUsing с PlantBag на SmartFridge — должен быть deny; вставить produce → достать через UI. Related: 1413891452001583227 (тот же домен SmartFridge, другой root cause).

## Severity & complexity (if actionable)
Severity: S4 COSMETIC (не воспроизводится в текущем форке) / Complexity: —

## Verdict JSON (also write to batch file)
см. .ai-triage/data/verdicts/batch_B11.json
