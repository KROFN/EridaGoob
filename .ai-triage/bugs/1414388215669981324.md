# Рандомный ттс каждый раунд

## Metadata
Thread ID: 1414388215669981324 / Date: 2025-09-07 / Author: BlackAngel (blck_angl) / Tags: unknown_tag_1414266059208265799 / Archive: archived / Messages: 3 / Attachments: 2 (sleekshot.png x2)

## Discord evidence
- Тело сообщения пустое; только 2 скриншота (предположительно выбор голоса/настройки) — BUG_EVIDENCE/REPRODUCTION_EVIDENCE по описанию, НЕ просмотрены (нет бюджета на image reads) [?].
- MonTrius отвечает ссылкой на другой тред Discord (1414369672790540389, вне выгрузки) — вероятно, обсуждение той же темы.
- Автор: «заебись» — ироничное/нейтральное закрытие; официального ответа нет.
- Заявление (из заголовка): TTS-голос персонажа рандомизируется каждый раунд вместо сохранённого в профиле.

## Search vocabulary
User terms: рандомный ттс → Domain terms: TTS voice randomization, profile voice persistence
Prototype-entity candidates: ttsVoice / Code symbols: HumanoidCharacterProfile.TTSVoice, ServerDbBase ttsVoice, DefaultSexVoice

## Repository investigation
FILE: Content.Server/Database/ServerDbBase.cs
SYMBOL: строки 232–234
WHY RELEVANT: серверный fallback голоса. CURRENT BEHAVIOR: если profile.TTSVoice пуст → `SharedHumanoidAppearanceSystem.DefaultSexVoice[sex]` — ДЕТЕРМИНИРОВАННЫЙ дефолт по полу, не рандом [SRC].

FILE: Content.Shared/Preferences/HumanoidCharacterProfile.cs
SYMBOL: TTSVoice (строки 257, 289), EnsureValid (1084–1085)
WHY RELEVANT: голос сохраняется в профиле; EnsureValid сбрасывает невалидный голос (прототип удалён → голос слетает на дефолт) [SRC].
- Гипотеза 1: рандом выдаётся, если выбранный голос невалиден/удалён (переезд списка голосов в Resources/Prototypes/_Erida/Voice/tts-voices.yml при "Tts (#28)") и EnsureValid каждый раунд заменяет его дефолтом; но это дефолт, а не рандом [INFER].
- Гипотеза 2: серверный код TTS-выдачи голоса (синтез на стороне tts.api_url, Content.Shared/_Erida/CCVar/CCVars.TTS.cs) находится вне найденных путей — клиентские Content.Client/_CorvaxGoob/TTS и Content.Server/_CorvaxGoob/TTS (пусто) — серверная логика назначения голоса не локализована [?].

## History
TTS-коммиты: 5aa9874e56 "Tts (#9)", 1d0d909af2 "Tts (#28)" (переработка, смена набора голосов), b67a4d5286 "Minor fix". После даты отчёта коммитов про рандомизацию голоса не найдено [HIST].

## Upstream comparison
Goob/Erida upstream: не найдено эквивалента «рандомный голос каждый раунд»; не исследовалось глубоко — локальных доказательств механики нет.

## Verdict
CANNOT_VERIFY (недостаточно данных: тело пустое, скриншоты не интерпретированы, серверная логика выбора голоса не локализована; детерминированный fallback в ServerDbBase противоречит «рандому»)

## Confidence
Low

## Root cause
HYPOTHESIS (низкая): невалидный/несохранённый TTSVoice в профиле + EnsureValid/DefaultSexVoice fallback; либо специальная серверная рандомизация, код которой не найден в репо (возможно, вне репо/конфиг tts.api_url).

## Proposed fix (do NOT implement!)
Сначала: запросить скриншоты/шаги, локализовать серверный код выдачи голоса (поиск RequestPreviewTTSEvent/TTSPlayEvent на сервере). Если подтверждается рандомизация — убрать её и использовать profile.TTSVoice → DefaultSexVoice fallback.

## Regression risk / Validation plan / Links-Related threads
Риск: неизвестен (нет фикс-плана). План: создать персонажа с явным голосом, проверить сохранение между раундами; очистить профиль и проверить fallback. Related: cluster tts-voice-availability (1365367741560913970, 1421828766404972624).

## Severity & complexity
Severity: S3 MINOR / Complexity: M (если подтверждится — маленький фикс XS)
