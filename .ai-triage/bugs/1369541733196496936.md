# Дальнозоркость цИИ или почему цИИ фиксирует датчики с сЦК (crew monitoring shows CC sensors)

## Metadata
Thread ID: 1369541733196496936 / Date: 2025-05-07T05:09Z / Author: TehniQ / Tags: На рассмотрении / Archive state: archived / Messages: 5 / Attachments: 2 (JPG photos of the monitor screen)

## Discord evidence
- [SRC] Root: "Встроенный мониторинг экипажа фиксирует датчики с сЦК вместо нашей родной НИС" — the built-in (AI) crew monitoring fixes sensors from Central Command (сЦК) instead of the local station (НИС). Lasted a whole shift on round 2; on round 1 it self-resolved over time.
- [SRC] Follow-up 2025-06-28 by same author: "Точно исправлено?" + new photo — still reproducible after 7 weeks. [MEDIA] both JPGs not renderable in triage context; tentative BUG_EVIDENCE (screen photos of monitoring list).
- [SRC] Дай сюда!: "такая хуйня и на мк была, ток вместо цк это на тайпане" — same phenomenon on MK, showing Typan crew — recurs across maps/servers.

## Search vocabulary
User terms: мониторинг экипажа, датчики, сЦК, цИИ / Domain terms: CrewMonitoringConsole, CrewMonitoringServer, SuitSensor, command tracker, device network broadcast / Prototype-entity candidates: crew monitoring console/server entities / Code symbols: CrewMonitoringConsoleSystem, CrewMonitoringServerSystem, CrewMonitorScanningComponent, IsCommandTracker

## Repository investigation
FILE: Content.Server/Medical/CrewMonitoring/CrewMonitoringConsoleSystem.cs
SYMBOL: OnPacketReceived (lines 35-51), UpdateUserInterface (61-89)
WHY RELEVANT: [SRC] console does `component.ConnectedSensors = sensorStatus` on EVERY CmdUpdatedState packet — full-list replacement, no map/station filtering at receive time; fork (GoobStation block, lines 76-88): `isCommandOnly = HasComp<CrewMonitorScanningComponent>(uid)` and displays only `IsCommandTracker` entries for command consoles, otherwise excludes them.
CURRENT BEHAVIOR: a console WITH CrewMonitorScanningComponent shows exclusively command-tracked (CC) sensors; the AI's built-in monitoring displaying "only CC crew instead of local crew" is exactly the isCommandOnly view. Alternatively, sensors mis-flagged as IsCommandTracker would leak into normal consoles. The full-replacement receive also allows flip-flop between servers broadcasting on the same frequency — matching the intermittent self-resolution.
FILE: Content.Server/Medical/CrewMonitoring/CrewMonitoringServerSystem.cs — BroadcastSensorStatus sends the whole SensorStatus dict to the frequency with no map filter [SRC].
FILE: Content.Server/DeviceNetwork/Systems/SingletonDeviceNetServerSystem.cs — "Keeps one active server entity per station" [SRC]: Central Command is a separate station with its own active monitoring server, so its collection is broadcast on the shared device network.
[?] Setter of IsCommandTracker and whether the AI's console entity carries CrewMonitorScanningComponent — not located within budget.

## History
[HIST] Not checked (budget). Report (2025-05-07) re-confirmed still live on 2025-06-28 by the reporter.

## Upstream comparison
[UP] Not investigated — the command-tracker filter is a GoobStation fork addition; upstream has no IsCommandTracker split.

## Verdict
LIKELY_CURRENT_BUG (strong hypothesis: command-tracker filtering / unfiltered per-frequency broadcast causes CC crew list on AI monitoring; some gaps remain)

## Confidence
Medium

## Root cause
HYPOTHESIS (Medium): the AI built-in crew monitoring console resolves as command-only (has CrewMonitorScanningComponent) or station sensors get IsCommandTracker=true; combined with unfiltered per-frequency broadcasts of whole sensor collections and full `ConnectedSensors` replacement, the console shows Central Command crew instead of the local station crew.

## Proposed fix (do NOT implement!)
- Locate AI console creation (AI subsystem/hud) → remove CrewMonitorScanningComponent if present; filter server broadcast and console entries by owning station (compare console station vs sensor station); key ConnectedSensors by server address to prevent cross-station clobbering; verify IsCommandTracker assignment in SuitSensorSystem.

## Regression risk / Validation plan / Links-Related threads
Risk: shared code with medbay/security consoles — must not hide local crew there. Validation: with CC round active, AI monitoring must list only НИС crew; medbay console unaffected. Related threads: none in batch.

## Severity & complexity (if actionable)
Severity: S2 MAJOR / Complexity: M

## Verdict JSON (also write to batch file)
Covered by .ai-triage/data/verdicts/batch_B18.json
