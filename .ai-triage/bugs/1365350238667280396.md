# Две вкладки с безделушками у инженера в редакторе снаряжения

## Metadata
Thread ID: 1365350238667280396 / Date: 2025-04-25 / Author: natalia (snug assessing) / Tags: unknown_tag_1365427630660128861 / Archive state: archived / Messages: 2 / Attachments: 1 (image/png)

## Discord evidence
- Автор: «Две вкладки с безделушками у инженера в редакторе снаряжения» (BUG_EVIDENCE, скриншот редактора лодаута).
- Суть: в редакторе лодаута у инженера отображались две вкладки с одинаковым названием «Безделушки».
- Replies: содержательных ответов нет.

## Search vocabulary
User terms: две вкладки, безделушки, редактор снаряжения, инженер
Domain terms: loadout, loadoutGroup, roleLoadout, trinkets, loadout editor UI
Prototype-entity candidates: Trinkets, StationEngineerJobTrinkets, JobStationEngineer
Code symbols: RoleLoadoutPrototype, loadout-group-trinkets, loadout-group-jobtrinkets

## Repository investigation
FILE: Resources/Prototypes/Loadouts/role_loadouts.yml
SYMBOL: roleLoadout JobStationEngineer (строки 489–506): groups содержит ОБЕ группы `- Trinkets` и `- StationEngineerJobTrinkets`
WHY RELEVANT: две группы безделушек в одном roleLoadout — источник двух вкладок.
CURRENT BEHAVIOR: обе группы присутствуют и сегодня, НО с разными отображаемыми именами (см. ниже), вкладки визуально различимы.

FILE: Resources/Prototypes/Loadouts/loadout_groups.yml (строка 4) и (строка ~1499)
SYMBOL: loadoutGroup Trinkets → `name: loadout-group-trinkets`; loadoutGroup StationEngineerJobTrinkets → `name: loadout-group-jobtrinkets`
WHY RELEVANT: в эпоху репорта обе группы отображались одним и тем же названием («Безделушки») — отсюда «две одинаковые вкладки».
CURRENT BEHAVIOR: Resources/Locale/en-US/preferences/loadout-groups.ftl: `loadout-group-trinkets = Trinkets` (стр. 26), `loadout-group-jobtrinkets = Job trinkets` (стр. 27) — имена теперь разные.

## History
- `git log -- Resources/Prototypes/Loadouts/role_loadouts.yml | head`: последние штрихи — порты WizDen-апстрима, включая коммит `12766fe6e3 Loadouts redux (#25715)` — переработка системы лодаутов, после которой группы получили раздельные локализационные ключи.
- Репорт от 2025-04-25 относится к старому Goobstation-лайнаджу; текущий fork синхронизирован значительно позже (fork HEAD 511cc23b7c, 2026-08).

## Upstream comparison
Erida upstream (dead-space-server/EridaGoob @ 511cc23b7c): идентичное текущему состояние (fork == upstream, 0 расхождений).

## Verdict
ALREADY_FIXED_IN_CURRENT_FORK — «дублирующиеся» вкладки существовали из-за двух групп с одинаковым отображаемым именем; в текущем fork группы сохранены (общие + должностные безделушки — это by-design), но их отображаемые имена разделены («Trinkets» / «Job trinkets»), воспроизведение «двух одинаковых вкладок» невозможно.

## Confidence
Medium

## Root cause
CONFIRMED: роль-лодаут инженера включает две loadout-группы безделушек (общую `Trinkets` и должностную `StationEngineerJobTrinkets`); в старом лайнадже обе имели одинаковое отображаемое имя → визуальный «дубль». Текущее состояние: имена разделены (loadout-groups.ftl:26-27).

## Proposed fix
Не требуется. (Опционально, UX: переименовать русскую локализацию ключей для явного различия «Безделушки» / «Должностные безделушки» в ru-RU.)

## Regression risk
Нет.

## Validation plan
Открыть редактор лодаута за инженера: две вкладки безделушек должны отображаться с разными названиями.

## Links / Related threads
DUPLICATE-родственный тред: 1366049076361166888 (та же механика, инженер + тех. ассистент).

## Severity & complexity
Severity: S4 COSMETIC / Complexity: XS
