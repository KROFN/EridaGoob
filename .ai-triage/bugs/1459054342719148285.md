# Кофемашина выдаёт кофе раньше, чем анимация закончится (coffee machine dispenses before animation finishes)

## Metadata
Thread ID: 1459054342719148285 / Date: 2026-01-09 / Author: maasarie / Tags: Незначительный, Визден-Баг, unknown_tag / Archive state: archived / Messages: 5 / Attachments: 1 (image.png)

## Discord evidence
[SRC] Root: «ну типо да» + screenshot — coffee machine produces the drink before the vend animation has finished.
[HIST] xkttty asks «есть анимация?» — maasarie: «да». Vecortys (2026-01-09): «Визден баг» — tagged as upstream (WizDen) bug by staff.
[MEDIA] attachments/1459054342719148285/1459054342899372052_image.png — not examined (NO image reads per task constraint); presumed BUG_EVIDENCE.

## Search vocabulary
User terms: кофемашина → coffee machine; выдаёт → dispense/vend; анимация → vend/eject animation.
Domain terms: VendingMachineComponent, ejectDelay, vendDelay, ejectState, eject-unshaded, RSI animation length.
Code symbols: VendingMachineCoffee, VendingMachine, HotDrinksMachineInventory.

## Repository investigation
FILE: Resources/Prototypes/Entities/Structures/Machines/vending_machines.yml
SYMBOL: entity `VendingMachineCoffee` (lines 506–545)
WHY RELEVANT: Erida's coffee machine ("Solar's Best Hot Drinks") — `parent: VendingMachine`, custom sprite `_Erida/Structures/Machines/VendingMachines/coffee.rsi`.
CURRENT BEHAVIOR: `dispenseOnHitChance: 0.25`, `ejectState: eject-unshaded`, `ejectDelay: 5` (line 522). The WizDen vending logic dispenses the item after `ejectDelay` seconds while the client plays the `eject-unshaded` state animation. If that RSI animation is longer than 5 s (Erida's custom coffee.rsi), the cup pops out while the animation is still running — the reported desync.

FILE: Resources/Prototypes/Entities/Structures/Machines/vending_machines.yml
SYMBOL: line 2489 description «Probably not the coffee machine.» — sibling funny machine; confirms the coffee machine is this vending family.
CURRENT BEHAVIOR: n/a (context).

[INFER] Exact RSI animation duration of `_Erida/Structures/Machines/VendingMachines/coffee.rsi` state `eject-unshaded` was not read (budget constraint; RSI is binary+json). The mismatch direction (animation > ejectDelay) is inferred from the report; screenshot corroborates item present mid-animation.

## History
[HIST] No git history pulled specifically for this file within budget; no "already fixed" evidence. Tag «Визден-Баг» by staff reflects perceived origin (upstream vending timing), but the sprite/delay pair is Erida content. [?]

## Upstream comparison
WizDen's coffee vending machine pairs its own coffee.rsi with `vendDelay` matched to its RSI; this fork ships a custom `_Erida` coffee.rsi while keeping `ejectDelay: 5`, so the desync is fork-content-specific. [?] (upstream commit not fetched)

## Verdict
LIKELY_CURRENT_BUG

## Confidence
Medium

## Root cause
HYPOTHESIS (strong): desync between `ejectDelay: 5` (vending_machines.yml:522, entity VendingMachineCoffee) and the longer `eject-unshaded` animation of the custom RSI `_Erida/Structures/Machines/VendingMachines/coffee.rsi` — dispense timer fires while the vend animation is still playing.

## Proposed fix (do NOT implement!)
Option A: raise `ejectDelay` on VendingMachineCoffee to the actual `eject-unshaded` animation length of coffee.rsi. Option B: shorten the RSI animation to ≤5 s. Why: dispense moment (server timer) then matches visual finish. One-line YAML or RSI edit.

## Regression risk / Validation plan / Links-Related threads
None (cosmetic). Validation: vend coffee, verify cup appears at/near animation end. Related: B20/B25 vending/sprite threads.

## Severity & complexity
Severity: S4 COSMETIC / Complexity: XS

## Verdict JSON
See .ai-triage/data/verdicts/batch_B30.json
