# Когда госты открывают документ, то окружающие это слышат (ghost paper interaction audible to nearby players)

## Metadata
Thread ID: 1458439379457675374 / Date: 2026-01-07 / Author: maasarie
Tags: Незначительный, Визден-Баг, unknown_tag / Archive: archived / Messages: 15 / Attachments: 1 (image.png webp screenshot — [MEDIA] BUG_EVIDENCE, not rendered per budget rule)

## Discord evidence
- "Проверено на АА." — confirmed on the AA instance (screenshot, not rendered).
- Replies give the in-game symptom: alexfire roleplays "Капитан, по моему этот листок кто-то осмотрел"; ᑕᗝᗩᒪᑕᑌᗷᗴᗪ: "я услышал что открыли не вещмешок а бумагу" — players HEAR a paper rustle/open sound when a ghost opens a sheet of paper (even one inside a backpack); loc/admin-meme example with nuke codes.
- No repro steps beyond: ghost opens a paper near living players → sound plays.

## Search vocabulary
User terms: "гост/документ/листок/слышно" / Domain terms: paper UI open sound, ghost interaction, PlayPvs vs PlayPredicted, write attempt gate / Prototype candidates: PaperComponent / Code symbols: PaperSystem.OnInputTextMessage, PaperWriteAttemptEvent, PaperScribbles, PlayPvs.

## Repository investigation
FILE: Content.Shared/Paper/PaperComponent.cs:32-35
SYMBOL: `[DataField("sound")] SoundSpecifier? Sound = new SoundCollectionSpecifier("PaperScribbles", …)`
WHY RELEVANT: [SRC] the audible paper sound (scribble/rustle collection) attached to every paper.

FILE: Content.Shared/Paper/PaperSystem.cs:189-217
SYMBOL: `OnInputTextMessage` — raises `PaperWriteAttemptEvent` on `args.Actor` (191-194); if not cancelled → `SetContent`, then `_audio.PlayPvs(entity.Comp.Sound, entity)` at line 212.
WHY RELEVANT: [SRC] the only paper sound broadcast in shared code is played **Pvs** (positional, audible to everyone nearby, not just the actor) after a UI input/save action, gated solely by `PaperWriteAttemptEvent`. [INFER] the ghost path is not visibly excluded here; whether some other system cancels `PaperWriteAttemptEvent` for dead/ghost actors was NOT verified (search budget exhausted — grep on the whole Content tree failed: repo projects are at root level, not under `Content/`).

FILE: Content.Shared/Paper/PaperSystem.cs:173
SYMBOL: `_audio.PlayPredicted(stampComp.Sound, entity, args.User)` — stamping already uses the actor-aware predicted API, unlike the line-212 broadcast.
WHY RELEVANT: [SRC] contrast shows the write sound could be scoped the same way.

## History
No relevant commits found (git log not run on Content.Shared/Paper — budget). [HIST] reported 2026-01-07, acknowledged in-thread as unfixed.

## Upstream comparison
Mods tag "Визден-Баг" suggests upstream origin; upstream WizDen PaperSystem shares this `PlayPvs` structure — not compared commit-wise.

## Verdict
LIKELY_CURRENT_BUG

## Confidence
Medium

## Root cause
HYPOTHESIS (Medium): paper's scribble sound is broadcast with `AudioSystem.PlayPvs` at the paper entity (`PaperSystem.cs:212`) on any `PaperInputTextMessage` that passes the `PaperWriteAttemptEvent` gate; ghost/dead actors are not (visibly) excluded from that path, so a ghost "opening"/interacting with a document triggers an audible positional sound for nearby living players.

## Proposed fix (do NOT implement!)
In `SharedPaperSystem.OnInputTextMessage`: early-return for ghosts/dead actors (check `GhostComponent`/`MobStateComponent` on `args.Actor`) before `PlayPvs`, and/or switch the write sound to `PlayPredicted(sound, entity, args.User)` so only the acting client plays it locally. File: Content.Shared/Paper/PaperSystem.cs. Also verify/`PaperWriteAttemptEvent` subscribers to cancel ghosts.

## Regression risk / Validation plan / Links-Related
Risk: low; real writers must still hear the sound locally. Validate: ghost opens/writes paper next to a player → no sound; living writer → sound. Related: B26 (emote/accents audibility), B28 TTS audibility cluster.

## Severity & complexity
Severity: S3 MINOR (metagame-free but spooky false info; tagged Незначительный) / Complexity: XS
