# Галочка на отказ от цели-убийства — не работает (всё равно можно стать целью Антажко)

## Metadata
Thread ID: 1536732595268747334 / Date: 2026-08-11 / Author: 9Kvac_ / Tags: Разработка / Archive state: active / Messages: 80 / Attachments: 3 (скрины чекбокса в UI [BUG_EVIDENCE] ×2 + скрин реакции; external GIF — мем [REACTION_MEME])

## Discord evidence
- Репорт: «Функция "Отказ от цели синдиката" не работает. Даже если ты её поставил, ты всё равно можешь оказаться целью Антажко».
- [MEDIA] Скрин 1: галочка в настройках персонажа выставлена. Скрины 2-3: подтверждение от Crisp Cloaker («Евген при упоминании удивился, ибо ставил эту штуку себе») — функция существует в UI, но не даёт эффекта.
- [HIST] Обсуждение выродилось в спор о RP-политике; 9Kvac_: «новые функции добавленные на Эриду должны работать, а не быть багованными».

## Search vocabulary
User terms: галочка, отказ от цели, синдикат, Антажко / Domain terms: kill target opt-out, character preference, objective target filter, PickRandomPerson / Prototype-entity candidates: PickRandomPerson, PickRandomTraitor / Code symbols: PickObjectiveTargetSystem, TargetObjectiveMindFilter, corporageagent.yml

## Repository investigation
- FILE: Resources/Prototypes/_Goobstation/Objectives/corporageagent.yml (строки 89-101, 372-401)
  SYMBOL: CorporateKillRandomPersonObjective / CorporatePermaKillRandomTraitorObjective / CorporatePermaKillRandomPersonObjective (PickRandomPerson без каких-либо opt-out фильтров)
  WHY RELEVANT: выбор kill-целей происходит из пула игроков через PickFromPool; ни один фильтр не проверяет «отказ от цели».
- FILE: Content.Server/Objectives/Systems/PickObjectiveTargetSystem.cs
  SYMBOL: OnRandomPersonAssigned → _mind.PickFromPool(ent.Comp.Pool, ent.Comp.Filters, args.MindId)
  WHY RELEVANT: единственная точка фильтрации целей; компонентов/событий вида OptOut/DeclineTarget/KillTargetDecline в Content.* не существует (rg 'Decline|OptOut|KillTarget|RefuseTarget' по .cs — 0 совпадений, по .yml/.ftl ru-RU «отказ от цели…» — 0 совпадений).
- FILE: Resources/Prototypes/_Erida/Traits/erida_traits.yml
  SYMBOL: категория Meta (единственный трейт NoParadoxClone)
  WHY RELEVANT: в кастомных трейтах Erida «отказа от цели» нет — галочка не реализована как трейт.
- Файл реализации чекбокса в Content.Client/Preferences, HumanoidProfileEditor, Content.Shared/Preferences — не найден за ~10 целевых поисков [?]: возможные объяснения: (а) UI-фича живёт вне рабочего дерева (внешняя панель/другая ветка), (б) галочка сохраняется в профиле, но серверный пайплайн выдачи целей её никогда не читает — что в точности объясняет симптом.

## History
- 1f9241fca2 "hello (#10)" (corporageagent.yml) — прототипы целей Erida; фикс opt-out после 2026-08-11 не найден. git -S по символам не выполнен (таймаут sandbox).

## Upstream comparison
Wizden/Goob: механики «opt-out из kill-целей» в апстриме нет — это кастом Erida; апстримную фиксированную реализацию взять неоткуда.

## Verdict
CANNOT_VERIFY — симптом подтверждён игроками [MEDIA], но реализация галочки в рабочем дереве не локализована; нельзя указать файл/символ, где настройка теряется. Локализация причины (внешняя панель vs «записывается, но не читается») невозможна в рамках этого прохода.

## Confidence
Low

## Root cause
HYPOTHESIS (low-medium): настройка сохраняется только в профиле игрока и не потребляется пайплайном выдачи kill-целей (corporageagent.yml PickRandomPerson без opt-out-фильтра; PickObjectiveTargetSystem.OnRandomPersonAssigned ничего не проверяет) → галочка декоративна. Альтернатива: фича реализована вне репо/не доведена до сервера.

## Proposed fix (do NOT implement!)
- Файлы: реализация префа (найти: HumanoidProfileEditor/prefs DB), CorporageKillRandom*-цели (добавить фильтр !type:OptOutMindFilter в corporageagent.yml:99/115/399), Content.Server/Objectives/Systems (новый MindFilter, читающий преф/компонент).
- Эскиз: при подборе цели из пула исключать mind с включённым opt-out; у KeepAlive-целей opt-out не применять.

## Regression risk / Validation plan / Links-Related
- Риск: пул kill-целей может опустеть при массовом opt-out — нужен fallback (оставшийся пул/выдача не-kill целей).
- Валидация: включить галочку → зайти на 20+ раундов с агентами → цель «убить» не должна выдаваться на персонажа; выключить — обычное поведение.
- Related: 1367843022061965395 (конфликт целей), 1489258968898469928 (тайминг спавна агентов).

## Severity & complexity
Severity: S3 MINOR / Complexity: M
