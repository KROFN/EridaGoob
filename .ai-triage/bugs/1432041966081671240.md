# Ты кому факс отправляешь? А? Кому факс отправляешь?! Тебе вот! Кому тебе то!

## Metadata
Thread ID: 1432041966081671240 / Date: 2025-10-26 / Author: KiriKuru / Tags: Маппинг, unknown_tag / Archive state: unresolved ("чекну завтра", "на мапперском это починено") / Messages: 13 / Attachments: 1 image (BUG_EVIDENCE: fax machine in ГВ office, not read per budget)

## Discord evidence
- "Факс ГВ пишется как факс СИ" — the fax machine in the CMO's (Главный врач, "ГВ") office on Box is presented as the Chief Engineer's ("СИ") fax.
- Дай сюда!: "это вроде мой почерк" (possibly a mapping authorship copy-paste artifact).
- kote19: "не поверите, но на мапперском это починено" — fixed on the mapper's build/branch.
- Side chatter: Box reportedly lacks the expeditions console; ГВ office on Box is filled with junk (separate content requests).

## Search vocabulary
User terms: факс ГВ, факс СИ, кабинет ГВ, бокс / Domain terms: fax machine, fax name, per-office fax, Box map / Prototype candidates: FaxMachineBase, FaxMachineCentcom, FaxMachineCaptain, FaxMachineSyndie / Code symbols: FaxMachineComponent.name, FaxPaperNameData.

## Repository investigation
FILE: Resources/Prototypes/Entities/Structures/Machines/fax_machine.yml
SYMBOL: FaxMachineBase / FaxMachineCentcom / FaxMachineSyndie / FaxMachineCaptain
WHY RELEVANT: these are ALL fax prototypes in the repo — there is NO per-role fax identity for ГВ (CMO) or СИ (Chief Engineer). Only Centcom (`name: "Central Command"`), Syndie and Captain (`name: "Captain's Office"`) carry a network name; `FaxMachineBase` has none (falls back to entity name on paper stamps, FaxSystem.cs:573).

FILE: Resources/Maps/.box.yml
SYMBOL: fax placements (lines 86956 `proto: FaxMachineBase`, 87063 `proto: FaxMachineCaptain`)
WHY RELEVANT: Box contains exactly two fax machines and no `- type: FaxMachine` name overrides — a wrong/generic entity in the CMO office cannot be distinguished as "факс ГВ"; mislabeling ("СИ") must come from map placement/copy-paste or client-side entity naming, i.e. map content, not fax system code.
CURRENT BEHAVIOR: offices share anonymous fax entities; in-office naming depends on which entity the mapper dropped.

FILE: Resources/Locale/ru-RU/holopad/holopad.ftl + ru-RU/chat/highlights.ftl
SYMBOL: holopad-command-cmo = "Командование - ГВ" (line 58), highlights-chief-medical-officer lists "ГВ" (line 9), holopad-command-ce = "Командование - СИ" (line 60), task-ntr-stamp-ce = "печати СИ"
WHY RELEVANT: confirms abbreviations — ГВ = Главный врач (CMO), СИ = Chief Engineer (CE). The complaint is a per-office identity mix-up on the Box map.

## History
git log (fax_machine.yml / .box.yml): be71aeb80e "Station goals (#13)", 1f9241fca2 "hello (#10)", upstream syncs — no fax placement/name fix on the current branch after the report; per kote19 the fix exists on a mapper branch not visible in this repo. Fork git dates internally inconsistent (2026 vs 2025 reports).

## Upstream comparison
SS14/Goob upstream: no per-role office faxes beyond Captain/Centcom/Syndie — per-office identity is fork map content. Not investigated further — local evidence sufficient.

## Verdict
CONFIG_OR_CONTENT_ISSUE — map/content matter: Box's ГВ office lacks a correctly identified fax (no ГВ fax identity exists at all); code is functioning. Fix is a mapper change, reportedly already done on the mapper branch.

## Confidence
Medium

## Root cause
HYPOTHESIS (Medium): the Box map (Resources/Maps/.box.yml) places a generic/foreign fax entity in the CMO office (map copy-paste per Дай сюда!'s "мой почерк"), and the repo has no fax prototype with a CMO/ГВ name to disambiguate offices; the displayed "СИ" identity is a mapping artifact. Code path is correct (FaxMachineComponent.name stamps fax identity).

## Proposed fix (do NOT implement!)
- Mapper: replace/verify the fax entity in the Box CMO office (Resources/Maps/.box.yml ~line 86956), ideally a new `FaxMachineCMO` child of FaxMachineBase with `name: "Главный врача"`-style identity (ru localization) — mirrors FaxMachineCaptain's pattern.
- Audit other Box office faxes for the same copy-paste mix-ups. Port the mapper-branch fix to master.

## Regression risk / Validation plan / Links-Related threads
Map edits risk shifting anchored entities — validate with mapper diff. Validation: fax in CMO office identifies as ГВ fax in UI/paper stamps; round-start test on Box. Related: none (Box content chatter in-thread is out of scope).

## Severity & complexity
Severity: S3 MINOR (cosmetic identity; if the placed entity is FaxMachineCaptain it also misroutes nukecodes/station goals — worth the audit) / Complexity: S

## Verdict JSON
See .ai-triage/data/verdicts/batch_B31.json
