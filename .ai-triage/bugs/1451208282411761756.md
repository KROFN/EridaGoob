# Призрак коробки (desynchronizer inside cardboard box → "ghost" pocket entity drives the box)

## Metadata
Thread ID: 1451208282411761756 / Date: 2025-12-18 / Author: alexfire
Tags: Незначительный, Визден-Баг, unknown_tag / Archive: archived / Messages: 3 / Attachments: 0

## Discord evidence
- "если в коробке использовать дессинхронизатор и покликать по коробке когда ты типо в карманном измерении, то ты можеш ходить коробкой" — use the desynchronizer while in the cardboard box, then click the box while you are "in the pocket dimension" → you can walk/drive the box while desynchronized (invisible).
- "Если человек залез в коробку то он не может ей управлять, человек из кармана для управления коробкой в приоритете почемуто" — a player who climbed into the box cannot control it; the pocket-dimension (desynchronized) entity has controller priority.
- Reply (Deleted User, 2026-01-08): "Баг от визов, если желающие будут, то фиксанём" — mods acknowledge it's an upstream (WizDen-origin) bug, fix welcome. Media: none.

## Search vocabulary
User terms: "коробка/дессинхронизатор/карманное измерение/призрак" / Domain terms: cardboard box stealth movement, polymorph, desynchronized pocket, occupant controller priority / Prototype candidates: DeviceDesynchronizer, DesynchronizedPocket / Code symbols: CardboardBoxSystem, SharedCardboardBoxSystem, PolymorphComponent.

## Repository investigation
FILE: Content.Server/CardboardBox/CardboardBoxSystem.cs:21-45
SYMBOL: `class CardboardBoxSystem` — subscriptions: `ActivateInWorldEvent → OnInteracted`, `EntInsertedIntoContainerMessage → OnEntInserted`, `EntRemovedFromContainerMessage → OnEntRemoved`, `StorageBefore/AfterOpen`, `GetAdditionalAccessEvent`.
WHY RELEVANT: [SRC] the box picks its controller from container insert events + click interaction — a single-occupancy controller model with no validation that the controller is a real, visible player (no ghost/desync state check in subscription list).

FILE: Content.Shared/CardboardBox/SharedCardboardBoxSystem.cs:5-8
SYMBOL: `SharedCardboardBoxSystem` — **empty** abstract class.
WHY RELEVANT: [SRC] all control logic is server-side; no shared guard against desynchronized/ghost entities → no protection there.

FILE: Resources/Prototypes/Entities/Objects/Devices/desynchronizer.yml:2-8
SYMBOL: `DeviceDesynchronizer` — "temporarily desynchronize the user from spacetime, effectively making them disappear".
WHY RELEVANT: [SRC] the device that starts the state used in the repro.

FILE: Resources/Prototypes/Polymorphs/polymorph.yml:253-266 + Resources/Prototypes/Entities/Mobs/Player/jaunt_mobs.yml:50-51
SYMBOL: "Desynchronized / Void Pocket status" polymorph → `entity: DesynchronizedPocket`, `effectProto: EffectDesynchronizer`.
WHY RELEVANT: [SRC] desynchronization is implemented as a **polymorph**: the user's mind is moved into a `DesynchronizedPocket` mob. [INFER] if triggered while inside the box's storage container, the pocket entity takes the occupant slot / controller priority, so the real player in the box can't drive it and the pocket "ghost" can walk the box by clicking (ActivateInWorld).

## History
No relevant commits found within budget (no git log on CardboardBoxSystem — budget exhausted). [HIST] acknowledged in Discord as still-open upstream bug on 2026-01-08.

## Upstream comparison
Mods: "Баг от визов" — originates in WizDen cardboard box feature; upstream fix not verified — local evidence sufficient.

## Verdict
LIKELY_CURRENT_BUG

## Confidence
Medium

## Root cause
HYPOTHESIS (Medium): desynchronizer polymorphs the user into the `DesynchronizedPocket` mob (polymorph.yml:253-266) while the original occupant relationship with the box persists; `CardboardBoxSystem` assigns movement/control to the inserted entity without checking whether it is a desynchronized/ghost entity (CardboardBoxSystem.cs:33-45), and the later real occupant gets no priority — hence the invisible "ghost" drives the box.

## Proposed fix (do NOT implement!)
In `CardboardBoxSystem.OnEntInserted/OnInteracted` (and movement relay), reject controllers that are polymorph/desync jaunt mobs (check `GhostComponent`/jaunt/desync marker, e.g. `DesynchronizedPocket` prototype tag) and make real actors take priority; optionally eject the desynced entity before taking control. Files: Content.Server/CardboardBox/CardboardBoxSystem.cs, Content.Shared/CardboardBox.

## Regression risk / Validation plan / Links-Related
Risk: low; ensure normal box stealth still works. Validate: repro from thread (desync inside box → click box) must not grant movement; second player climbing in must get control. Related: B15 shadowkin jaunt batch (pocket dimension).

## Severity & complexity
Severity: S3 MINOR (prank/stealth abuse potential, tagged Незначительный by mods) / Complexity: S
