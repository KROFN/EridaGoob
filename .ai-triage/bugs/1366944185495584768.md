# Агост, т-лучевой сканнер — пропажа всех предметов агоста?

## Metadata
Thread ID: 1366944185495584768 / Date: 2025-04-30T01:08Z / Author: Ykika (mlefur) / Tags: unknown_tag_1365427630660128861 / Archive state: archived / Messages: 10 / Attachments: 2 (PNG ×2)

## Discord evidence
- [SRC] Root message body ПУСТОЙ — весь отчёт состоит из заголовка и 2 скриншотов. Заявка из заголовка: при использовании Т-лучевого сканнера в агосте пропадают ВСЕ предметы агоста [?].
- [MEDIA] attachments/1366944185495584768/1366944186041106462_image.png, .../1366944186560942120_image.png — UNVIEWABLE_MEDIA (инспекция изображений запрещена регламентом этого воркера); содержимое скриншотов не изучено.
- [SRC] Yosif: «Вместо т лучевого, вообще за админа используй меню на кнопку B и переключить пол» → совет обходного пути через «Панель песочницы» (открывается по B), кнопка «Переключить пол» = ToggleSubfloorButton. «Сразу всё показывать будет».
- [SRC] Yosif: «полезна кнопка переключить спавны, например гост роль дракончика удалить» → «Показать спавны» (ShowMarkersButton) той же панели.
- [SRC] Ykika: «Так бай Зе Вей баг есть баг. Лучше сообщить чем не сообщать» — автор настаивает, что баг реален.

## Search vocabulary
User terms: т-лучевой сканнер, агост, предметы агоста / Domain terms: T-ray scanner, TrayScanner, subfloor, aghost / Prototype-entity candidates: TrayScanner, AdminGhost [?] / Code symbols: TrayScannerComponent, TrayScannerUserComponent, TrayScanRevealComponent, SubFloorHideComponent

## Repository investigation
FILE: Content.Shared/SubFloor/TrayScannerComponent.cs
SYMBOL: TrayScannerComponent
WHY RELEVANT: единственный «т-лучевой» сканнер в кодовой базе — T-ray scanner подсистемы SubFloor.
CURRENT BEHAVIOR: toggle-действие сканнера управляет подсветкой скрытых сущностей (трубы/кабели) через SubFloorHide; реализация: Content.Server/SubFloor/TrayScannerSystem.cs (сервер) и Content.Client/SubFloor/TrayScannerSystem.cs (клиент).

FILE: Content.Shared/SubFloor/{SubFloorHideComponent.cs, TrayScanRevealComponent.cs, TrayScannerUserComponent.cs, SharedTrayScannerSystem.cs}
SYMBOL: SubFloorHideComponent / TrayScanRevealComponent / TrayScannerUserComponent
WHY RELEVANT: механика видимости подпола — единственный кандидат на «визуальную пропажу» сущностей при включённом сканнере.
CURRENT BEHAVIOR: клиентское скрытие/раскрытие сущностей; код-путь, удаляющий инвентарь, из этой подсистемы не следует.

[?] FILE: Resources/Prototypes/** — поиск `id: AdminGhost|AGhost` и `-i adminghost` дал 0 результатов: прототип сущности агоста не найден по этим именам (переименован или лежит вне ожидаемых путей). Механизм «пропажи всех предметов агоста» из кода не выведен.

## History
Не выполнено: бюджет поиска исчерпан до `git log` по SubFloor-файлам. Упоминаний фикса в найденных файлах нет.

## Upstream comparison
Not investigated — local evidence insufficient (TrayScanner-подсистема стандартная; отличия форка не установлены).

## Verdict
CANNOT_VERIFY

## Confidence
Low

## Root cause
HYPOTHESIS: отчёт целиком в изображениях (root message пуст [SRC]), изображения не инспектированы. В подсистеме TrayScanner код-путь, удаляющий/теряющий предметы агоста, не обнаружен; правдоподобна путаница «скрыл/показал сущности» (визуальная «пропажа») либо баг выдачи предметов агосту. Требуется репро.

## Proposed fix (do NOT implement!)
Нет — сначала репродукция: aghost → toggle T-ray scanner → проверить hands/inventory; сверить со скриншотами треда; при подтверждении проверять Content.Server/SubFloor/TrayScannerSystem.cs на модификацию инвентаря.

## Regression risk / Validation plan / Links-Related threads
Risk: —. Validation: воспроизвести toggle сканнера в агосте, логировать удаление предметов. Related: 1370101954591719424 (та же «Панель песочницы» агоста).

## Severity & complexity (if actionable)
Severity: S3 MINOR (админ-QoL, подтверждение отсутствует) / Complexity: M (нужна репро-сессия)
