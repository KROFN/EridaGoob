# Исчезновение принтера документов.

## Metadata
Thread ID: 1382297503306743808 / Date: 2025-06-11 / Author: Звезда лисы (jickson900) / Tags: none / Archived: yes / Messages: 4 / Attachments: 0

## Discord evidence
- Root: "Когда я собираю принтер документов в ручную (не используя упаковщик). После того как он закончен и я использую отвёртку - он исчезает." Happened twice, specifically the document printer.
- Reply "о.": "Он должен быть в пакете, скорее всего попросту не добавлена такая возможность" — flatpack is the intended flow; hand assembly support likely incomplete.
- Author re-bumped 2025-09-12: "Я напоминаю про этот баг, так как он до сих пор есть" — still reproducing ~3 months later [UP].
- Expected: screwdriver on finished hand-built printer does nothing harmful / opens deconstruction with material drops; actual: entity vanishes.

## Search vocabulary
User terms: принтер документов→document printer (Corvax doc-printer), собрать вручную→hand construction, отвёртка→screwdriver, упаковщик→flatpacker/packaging machine.
Domain terms: ConstructionGraph, ConstructionComponent, Deconstruct event, Machine frame/board, MaterialStorage, flatpack.

## Repository investigation
FILE: Resources/Locale/ru-RU/_Corvax/paper/doc-printer.ftl
SYMBOL: ru locale keys for the Corvax doc printer
WHY RELEVANT: confirms the doc printer is Corvax-origin content present in this fork [SRC].
CURRENT BEHAVIOR: entity exists; its prototype/construction graph file was NOT located — wide rg over Resources/Prototypes and Content* for "printerdoc|documentprinter" returned no hits and larger searches timed out (bin/obj-heavy tree) [?]. Prototype file naming does not contain "printer" (cf. earlier glob: only textures/audio/locale match).

FILE: (unlocated) Resources/Prototypes/... construction graph for the hand-assembled doc printer
SYMBOL: ConstructionGraph final node / deconstruct edges
WHY RELEVANT: hypothesized root cause location.
CURRENT BEHAVIOR: unknown; standard SS14 construction pipeline (ConstructionSystem, Deconstruct event, screwdriver InteractUsing) deletes/rewinds an entity per its graph — a final node without a deconstruct edge or without material drop configuration makes a screwdriver click on the finished machine destroy it silently [INFER].

FILE: Content.Goobstation (packaging rework)
SYMBOL: packaging/flatpacker rework merged 2025-05-07 (0ddb58f4e0 "Goobstation.Maths / Reworked Packaging to Support it (#2560)")
WHY RELEVANT: the "упаковщик" flow the reporter contrasts hand assembly with; flatpack is the supported route per the reply.

## History
- No relevant commit found for the doc printer construction (searches for prototype/graph id failed; history check on the located file therefore impossible) [?].
- Report reaffirmed as live on 2025-09-12 [UP]; no fixing commit identified since.

## Upstream comparison
Not investigated — prototype/graph not located in local tree (search cost exceeded budget); Corvax origin suggests checking Corvax upstream for a deconstruct edge on the printer graph.

## Verdict
CANNOT_VERIFY

## Confidence
Low

## Root cause
HYPOTHESIS (Low): the hand-assembly construction graph for the document printer lacks a proper deconstruction edge/material drop for its final node, so a screwdriver click after completion is handled as an (empty) deconstruction that deletes the machine without dropping anything. The prototype and graph file could not be located within the search budget (rg timeouts; no filename hits for "printer"), so the code path is unproven. Supporting facts: bug reproduced twice by reporter, still live 2025-09-12, other machines unaffected per report wording, and the reply indicating flatpack is the intended packaging path [INFER].

## Proposed fix (do NOT implement!)
First locate: rg -n "DocumentPrinter|doc-printer|PrinterDoc" across Resources/Prototypes with bin/obj excluded, and the ConstructionGraph referenced by the entity's Construction component. Then add a deconstruct edge (or remove the screwdriver deconstruct step) to the final node, mirroring sibling machines (e.g., uniform printer) so deconstruction drops components/materials instead of deleting.

## Regression risk / Validation plan / Links-Related threads
N/A until verified. Validation: hand-build the printer via its graph, screwdriver it — expect no deletion, parts/materials returned; also verify flatpacker output unchanged. Related: B24 craft threads (batch context), Goob packaging rework 0ddb58f4e0.

## Severity & complexity (if actionable)
Severity: S3 MINOR / Complexity: M
