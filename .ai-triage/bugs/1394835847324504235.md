# На манекен нельзя напялить очки, маску и шапку (mannequin: cannot equip glasses/mask/hat)

## Metadata
Thread ID: 1394835847324504235 / Date: 2025-07-16 / Author: Svaxe (.antoscha) / Tags: none / Archive state: archived / Messages: 2 / Attachments: 1 (image.png)

## Discord evidence
[SRC] Root: «Проклятие Эриды !!!» + screenshot — cannot put glasses, mask and hat onto a mannequin.
[HIST] Reply (NickCatXD1): he removed cap's clothes from a mannequin (cap hat, glasses, gas mask, hardsuit) and «не мог большинство вернуть обратно на место» — could not put most items back.
[MEDIA] attachments/1394835847324504235/1394835848117223464_image.png — not examined (NO image reads per task constraint); presumed BUG_EVIDENCE.

## Search vocabulary
User terms: манекен → mannequin; очки → eyes slot; маска → mask slot; шапка/фуражка → head slot.
Domain terms: inventoryTemplate, StripableInventoryBase, Strippable BUI, inventory slots, sprite layer maps.
Code symbols: Mannequin (entity), `mannequin` (inventoryTemplate), StripableInventoryBase, InventoryBase.

## Repository investigation
FILE: Resources/Prototypes/Entities/Structures/Decoration/mannequin.yml
SYMBOL: entity `Mannequin`, parent `[BaseStructureDynamic, StripableInventoryBase]`
WHY RELEVANT: the entity itself. Sprite layers ARE defined for all slots: `jumpsuit`, `outerClothing`, `eyes`, `neck`, `back`, `mask`, `head` (lines 14–24), sprite _Erida/Structures/Decoration/mannequin.rsi.
CURRENT BEHAVIOR: rendering layers and slots exist — the failure is in the equipping interaction path, not in YAML slot/layer definitions.

FILE: Resources/Prototypes/InventoryTemplates/mannequin_inventory_template.yml
SYMBOL: inventoryTemplate `mannequin`
WHY RELEVANT: defines slots jumpsuit, outerClothing, neck, mask, eyes, head, suitstorage, back with correct slotFlags (MASK, EYES, HEAD present).
CURRENT BEHAVIOR: mask/eyes/head slots exist with correct flags; canEquip via generic InventorySystem should accept them.

FILE: Resources/Prototypes/InventoryTemplates/inventorybase.yml
SYMBOL: abstract entity `StripableInventoryBase` = `Inventory` + `InventorySlots` + `Strippable` + UserInterface `StrippingUiKey.Key: StrippableBoundUserInterface`
WHY RELEVANT: the mannequin's entire interaction surface is the generic strip UI + generic inventory verbs; there is NO dedicated mannequin dressing code in the fork.
CURRENT BEHAVIOR: [SRC] `rg -l -i 'mannequin' --glob '*.cs'` over Content.Server/Shared/Client/Goobstation.* returns rc=1 (zero matches) — no MannequinSystem exists; dressing relies solely on generic systems (strip UI / equip verbs), which evidently do not (fully) support putting items into eyes/mask/head of a structure.

## History
[HIST] git log -- Resources/Prototypes/Entities/Structures/Decoration/mannequin.yml: 1f9241fca2 "hello (#10)" (fork init), fdff2668f4/8ae9915535 (REUSE), 06b00b9795 SPDX — no functional changes since fork start; nothing after report date. [?]

## Upstream comparison
WizDen upstream has no mannequin entity; prototype layout matches Delta-V's mannequin (DV may carry dedicated dressing code that this fork did not port). [?] Not verified — local evidence sufficient for classification.

## Verdict
LIKELY_CURRENT_BUG — real, corroborated by two independent reports; exact failing check not located.

## Confidence
Low (root cause) / bug existence is high but per protocol confidence is reported for the verdict: Low–Medium.

## Root cause
HYPOTHESIS: the fork inherits the mannequin as a pure StripableInventoryBase structure without the dedicated dressing system/verb its upstream (DV) has; the generic equip/strip-BUI paths either disallow or silently fail for EYES/MASK/HEAD on a structure without a body/mind, while jumpsuit/outerClothing can still be (re)dressed. Key facts proven: no mannequin C# code exists (rg rc=1); slots + layers exist in YAML; interaction surface = Strippable BUI only. [INFER]

## Proposed fix (do NOT implement!)
Port/author a small MannequinSystem (or extend Strippable equip path): an "equip onto mannequin" DragDrop/verb that calls `InventorySystem.TryEquip` for the targeted slot, with `SharedAudioSystem`/popups feedback; register in Content.Shared/_DV or Content.Server. Files: new Content.Server/.../Mannequin/MannequinSystem.cs (+ shared events); verify CanEquip allows structures. Why: replaces reliance on strip-only UI.

## Regression risk / Validation plan / Links-Related threads
Low. Validation: construct mannequin; equip/unequip each slot type (hat, glasses, mask, jumpsuit, hardsuit); verify sprite layers update; strip UI roundtrip. Related: T-B30 thread 1394835847324504235 reply mirrors removal-only behavior.

## Severity & complexity
Severity: S3 MINOR / Complexity: M (new system or generic-path surgery)

## Verdict JSON
See .ai-triage/data/verdicts/batch_B30.json
