# В дополнительных слотах Х.А.Р.М.П.А.К.А. нет отображения (energy shield / AKMS invisible in extra slots)

## Metadata
Thread ID: 1449455266885144617 / Date: 2025-12-13 / Author: the Hope of the shattered Ones / Tags: Визден-Баг, unknown_tag / Archive state: archived / Messages: 2 / Attachments: 2 (mp4 videos)

## Discord evidence
[SRC] Root: «если взять энергощит, АКМС в дополнительные слоты - то их банально не будет видно(как и многое другое, кстати. Вырежьте эту багованную фигню блин)».
[SRC] Author's follow-up: «но это скорее всего проблема визардов, так что хз» (suspects WizDen origin).
[MEDIA] attachments/1449455266885144617/1449455267786789039_2025-12-13_20-33-52.mp4, 1449455268621717535_2025-12-13_20-35-14.mp4 — videos NOT examined (NO image reads per task constraint); presumed BUG_EVIDENCE; would confirm which item/slot UI "Х.А.Р.М.П.А.К.А." refers to.

## Search vocabulary
User terms: энергощит → energy shield; АКМС → AKMS rifle; дополнительные слоты → extra/additional slots; нет отображения → not visible/invisible.
Domain terms: storage slots, inventory slots, held-item visualizer, slot sprite layers.
Code symbols: WeaponRifleAKMS, EnergyShield, HeldVisualizer (not found), extraSlots (not found).

## Repository investigation
FILE: Resources/Prototypes/_Goobstation/Catalog/selectable_sets.yml
SYMBOL: references to WeaponRifleAKMS / EnergyShield
WHY RELEVANT: [SRC] both reported items exist in the fork as Goobstation content (also Resources/Prototypes/_Goobstation/Entities/Markers/Spawners/Random/evil.yml).
CURRENT BEHAVIOR: items themselves exist; invisibility is a slot/rendering issue, not missing content.

FILE: repo-wide rg
SYMBOL: `rg -n 'HeldVisualizer'` over Content.* / Content.Goobstation.* — rc empty (no such system); `rg -i 'extraSlots|ExtraSlot|additionalSlot'` — no hits; `rg -i 'harmpaka|hamkaka|армпака|хармкака|армкака'` over Resources — no hits.
WHY RELEVANT: the batch-suggested mechanism (InventoryTemplates + HeldVisualizer) could not be confirmed; the item "Х.А.Р.М.П.А.К.А." and its "extra slots" feature were NOT identified in code or locale within budget.
CURRENT BEHAVIOR: unknown slot system — possibly a custom Erida/Goob bag/armor with non-standard slots whose items get no sprite layer, or a localized name not present in the repo's locale files.

## History
[HIST] No relevant commits located (targeted searches exhausted budget before history pass). [?]

## Upstream comparison
Not investigated — the feature itself could not be identified locally. [?]

## Verdict
CANNOT_VERIFY — the reported slot system/item "ХАРМПАКА" cannot be located in the repo (0 hits for all candidate spellings), and video evidence was out of scope; no root cause can be proven without identifying the system.

## Confidence
Low

## Root cause
HYPOTHESIS: items placed into the non-standard "extra slots" of the unidentified ХАРМПАКА item lack a sprite/visualizer mapping (no held/slot layer for those slots), so they render invisible; plausibility high (report + videos) but system unlocated. [INFER]

## Proposed fix (do NOT implement!)
First step is identification: inspect the two mp4s (ffprobe/frames) or ask the reporter for the item's English id; then find the slot owner prototype (search by visible item names), and add the missing sprite-layer/HeldVisualizer mapping for those slots. Why: root cause cannot be targeted until the slot system is identified.

## Regression risk / Validation plan / Links-Related threads
n/a until identified. Validation: place energy shield + AKMS into the reported slots on a live client; verify visibility for both carrier and observers. Related: none in B30.

## Severity & complexity
Severity: S3 MINOR (visual only, but user reports "much more" affected) / Complexity: M (unknown system)

## Verdict JSON
See .ai-triage/data/verdicts/batch_B30.json
