# Спавн в ЦК (пассажир/юнит иногда появляется в кабинетах ЦК)

## Metadata
Thread ID: 1366802698430578818 / Date: 2025-04-29T15:45Z / Author: 𝔗𝔥𝔞𝔲𝔪𝔦𝔢𝔩 𝔚𝔞𝔯𝔩𝔬𝔯𝔡 (dark_revenge) / Tags: нет / Archive: archived / Messages: 3 / Attachments: 2 (image.png ×2 — BUG_EVIDENCE, не читались: лимит батча)

## Discord evidence
- «Иногда в кабинетах ЦК может появиться пассажир или юнит»; далее автор: «да, вот такова моя судьба за юнита» — сам автор играл «за юнита» и заспавнился в ЦК [MEDIA: 2 скриншота, не анализировались].
- Воспроизведение: спорадическое; зашёл в раунд как passenger/юнит → точка спавна оказалась в офисах CentComm.
- Ожидаемо: спавн лейтджойна на станции. Фактически: спавн на карте ЦК.

## Search vocabulary
User terms: ЦК, пассажир, юнит / Domain terms: CentralCommand, latejoin, SpawnPoint, SpawnPointType, visitor, ghost role, silicon unit / Prototype candidates: SpawnPointPassenger, VisitorSpawner, RandomHumanoidVisitor* / Code symbols: SpawnPointSystem, ContainerSpawnPointSystem, ArrivalsSystem, RuleGridsSystem.

## Repository investigation
FILE: Content.Server/Spawners/EntitySystems/SpawnPointSystem.cs
SYMBOL: выбор точки спавна (строки 37–59)
WHY RELEVANT: единственное место выбора позиций для LateJoin/Job-спавнов.
CURRENT BEHAVIOR: запрос `EntityQueryEnumerator<SpawnPointComponent, TransformComponent>` идёт по ВСЕМ сущностям мира БЕЗ фильтра по карте/гриду станции (фильтр только по SpawnType/Job). Если `possiblePositions` пусто — fallback (строки 50–59): берётся ПЕРВАЯ попавшаяся SpawnPointComponent «где угодно», с Log.Error «Unable to pick a valid spawn point, picking random spawner as a backup». Т.е. при отсутствии подходящих маркеров на станции игрок может заспавниться на чужом гриде (ЦК/шаттл/ERT).

FILE: Content.Server/Spawners/EntitySystems/ContainerSpawnPointSystem.cs
SYMBOL: строки 58–64 (та же пара проверок без фильтра карты)
WHY RELEVANT: аналогичный путь для спавна в контейнерах (silicon/«юнит»-спавны) [INFER].
CURRENT BEHAVIOR: тот же паттерн без фильтра по станции.

FILE: Content.Server/Shuttles/Systems/ArrivalsSystem.cs (строки 187, 365, 414)
SYMBOL: фильтры SpawnPointType.LateJoin по arrivalsUid / MapID
WHY RELEVANT: показывает, что в arrivals фильтрация по гриду/карте ЕСТЬ, а в базовом спавне — нет (сравнение).

FILE: Resources/Maps/centcomm.yml
SYMBOL: proto-сущности (650 шт.)
WHY RELEVANT: карта ЦК.
CURRENT BEHAVIOR: `rg -i 'SpawnPoint|VisitorSpawner|passenger'` → ПУСТО; из спавнеров только DecalSpawner*, MaintenanceWeaponSpawner, RandomCommandCorpseSpawner. Маркеров игроков на ЦК сейчас нет; `git log -S'SpawnPoint' -- centcomm.yml` → никогда не было [HIST]. Значит, сам маркер — не на centcomm.yml; задействованы чужие гриды, либо текущая сборка уже не воспроизводит.

FILE: Content.Server/GameTicking/Rules/RuleGridsSystem.cs (строки 54–77)
SYMBOL: OnSelectLocation / AntagSelectLocationEvent
WHY RELEVANT: Goob-комментарий: «Map check was removed because some rules with shuttle … was broken after shuttle FTL» — в форке сознательно удаляли фильтры карт для спавнов; паттерн склонен к таким утечкам [INFER].

## History
centcomm.yml после репорта (2025-04-29): e327eec5bd «Cent com update (#3853)», 0909b97dbe «CentCom Mapping Update (#5703)», 681bae550d «remove fun from cc (#5847)» и др. — карта ЦК активно правилась, но SpawnPoint-маркеров в ней нет ни до, ни после [HIST]. Релевантных фиксов системы спавна не найдено.

## Upstream comparison
Wizden SS14: SpawnPointSystem содержит тот же fallback-код (унаследовано); т.е. это не локальный регресс, а давний дефект выбора спавна, обостряющийся на картах с неполными маркерами [INFER]. Не копал глубже — достаточно локальных данных.

## Verdict
LIKELY_CURRENT_BUG — кодовый путь подтверждён (спавн без фильтра карты + fallback «любой спавнер мира»), точный триггер эпизода (какая сущность SpawnPointComponent лежала рядом с кабинетами ЦК в тот раунд: гриды ERT/шаттлов у ЦК/чужой грид) не доказан из-за отсутствия логов.

## Confidence
Medium

## Root cause
HYPOTHESIS (Medium): в раунде, когда для job/лейтджойна «пассажир/юнит» на станции не нашлось валидных SpawnPointComponent (период переделки карт — см. T-B24a тред 1419457062882250874), срабатывал fallback в Content.Server/Spawners/EntitySystems/SpawnPointSystem.cs (строки 50–59) и игрок заспавнился на первой попавшейся точке спавна другого грида (ЦК/шаттл у ЦК). Для «юнита» параллельно применим ContainerSpawnPointSystem (тот же паттерн без фильтра карты).

## Proposed fix (do NOT implement!)
- SpawnPointSystem.cs / ContainerSpawnPointSystem.cs: добавить фильтр по MapID/grid станции (аналогично ArrivalsSystem.cs:365), fallback ограничить гридами станции args.Station; при пустом наборе — телепорт на arrivals/Observer-точку вместо произвольного спавнера.
- Добавить assert/лог с указанием грида выбранной точки.

## Regression risk / Validation plan / Links-Related
Риск: поломать ERT/нукfu-спавны на отдельных гридах (RuleGrids) — фильтровать только в InRound-ветке. Валидация: запустить раунд на карте без Passenger-маркеров → убедиться, что спавн не уходит на чужие гриды; grep лога «Unable to pick a valid spawn point». Related: batch B24a 1419457062882250874 (переработка карт — источник «пустых» наборов маркеров).

## Severity & complexity
Severity: S2 MAJOR / Complexity: M

## Verdict JSON
См. .ai-triage/data/verdicts/batch_B24a.json
