# как смотреть (Train map has no SB cameras)

## Metadata
Thread ID: 1514984863562727596 / Date: 2026-06-12T13:28Z / Author: мерзкий жиж (merzkii_zhizh) / Tags: Маппинг / Archive state: archived / Messages: 9 / Attachments: 1 (PNG screenshot)

## Discord evidence
- [SRC] Root: "на трейне вообще нету камер эсбэ" — on the Train map there are no security (СБ) cameras at all (SB camera monitor shows nothing).
- [SRC] Reporter, 2026-06-12: "и пока не забыл, на аспиде вообще нету камер снабжения" — Aspid map also lacks supply cameras (same class of issue).
- [SRC] mangust2211 (mapper): "камеры нужно привязывать к серверу" / "каждую камеру, например камера СБ, нужно присоединить к серверу камер СБ" — each camera must be linked to the (security) camera server; announces taking over the work ("Я займусь сервисом...", "Ты сб Инж и карго").
- [MEDIA] attachments/1514984863562727596/1514984863802065117_2026-06-12_16-27-27.png — screenshot of empty camera monitor; not renderable in triage context; tentative BUG_EVIDENCE.

## Search vocabulary
User terms: трейн, камеры эсбэ, сервер камер, аспид / Domain terms: SurveillanceCamera, surveillance camera server/router, camera subnet/network, camera monitor / Prototype-entity candidates: train.yml map / Code symbols: SurveillanceCameraComponent, SurveillanceCameraRouterComponent, camera monitor networks

## Repository investigation
FILE: Resources/Maps/_Goobstation/train.yml [SRC]
SYMBOL: SurveillanceCamera (156 occurrences verified by grep count)
WHY RELEVANT: camera entities DO exist on the Train map — the complaint is that the SB camera monitor lists none, which matches cameras existing physically but not being assigned/linked to the security surveillance camera server/subnet.
FILE: Resources/Prototypes/Maps/train.yml [SRC] — map prototype for Train.
CURRENT BEHAVIOR: [INFER] camera entities present but not networked to the SB camera server → monitor shows an empty camera list; the in-thread mapper explicitly confirms the linkage requirement and claims the linking work as a content task. [?] Per-camera network fields in train.yml not individually inspected (map file is huge; budget).

## History
[HIST] Not checked (budget). In-thread commitment ("Исправим"-style) from mapper mangust2211 indicates a content fix was underway at thread time (2026-06-13).

## Upstream comparison
[UP] Not investigated — fork map content issue; upstream camera linking design is the same, so no upstream divergence expected.

## Verdict
CONFIG_OR_CONTENT_ISSUE (cameras exist but are not linked to the SB camera server; change is mapping/content work, already claimed by the mapper in-thread)

## Confidence
High

## Root cause
CONFIRMED (mapper statement + map content): Train map SurveillanceCamera entities are not linked/assigned to the security surveillance camera server, so the SB camera monitor shows no cameras. Same pattern reported for Aspid supply cameras.

## Proposed fix (do NOT implement!)
- In Resources/Maps/_Goobstation/train.yml (and the Aspid map), assign each SB camera's network/server linkage to the SB surveillance camera server (add routers where missing) per SurveillanceCamera system requirements; repeat for supply cameras on Aspid.

## Regression risk / Validation plan / Links-Related threads
Risk: none (map content). Validation: open SB camera monitor on Train — cameras must be listed and viewable; Aspid supply monitor likewise. Related: 1416386325006778440 (same failure class: per-map device content gaps).

## Severity & complexity (if actionable)
Severity: S3 MINOR / Complexity: S

## Verdict JSON (also write to batch file)
Covered by .ai-triage/data/verdicts/batch_B18.json
