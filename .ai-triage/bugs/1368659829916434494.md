# Противогазы и маски

## Metadata
Thread ID: 1368659829916434494 / Date: 2025-05-04 / Author: Toros (toros4) / Tags: none / Archived: yes / Messages: 6 / Attachments: 0

## Discord evidence
- Root: pull a mask partially down ("приспустить"), then fully remove and re-equip → mask has NO sprite but keeps function; works with any mask; fixed by toggling the mask once more.
- Author refinement: "Работает только с масками, которые не имеют спрайта при спущенном варианте" — only masks without a down-state sprite.
- Tested on грейтайд противогаз, neck gaiter (гетра), breathing mask; TREST notes it affects non-official (greyprot?) servers too.
- Expected: re-equipped mask shows worn sprite; actual: invisible on body while internals/protection still work.

## Search vocabulary
User terms: маска→mask (MaskComponent), приспустить→toggle/pull down, противогаз→gas mask, гетра→neck gaiter.
Domain terms: ToggleAction, IsToggled, EquippedPrefix, ClothingComponent, got-unequipped, RSI down state.

## Repository investigation
FILE: Content.Shared/Clothing/EntitySystems/MaskSystem.cs
SYMBOL: OnMaskToggled (line ~58 SetToggled(!IsToggled)), OnGotUnequipped (lines 65-68 → SetToggled(uid,false)), SetToggled (lines ~95-116): mask.Comp.IsToggled = toggled; var prefix = mask.Comp.IsToggled ? mask.Comp.EquippedPrefix : null; _clothing.SetEquippedPrefix(mask, prefix)
WHY RELEVANT: toggling rewrites the equipped prefix to the mask's down-prefix; masks whose RSI lacks that state render as nothing (matches "masks without a down sprite"). Re-equip visuals are driven by the stale prefix state — the reported no-sprite-but-functional state — until the next toggle rewrites the prefix.
CURRENT BEHAVIOR: upstream fix e9266d9cdf "Fix visual bug with masks appearing to be pulled down after re-equipping (#40332)" touches this exact system and is present in HEAD (arrived via 68b7977bee "2 july upstream (second attempt) (#4041)").

FILE: Content.Shared/Clothing/Components/MaskComponent.cs
SYMBOL: MaskComponent.IsToggled / EquippedPrefix / ToggleActionEntity
WHY RELEVANT: state persisted across unequip/equip is the bug vector.

FILE: Content.Shared/Clothing/EntitySystems/ToggleClothingPrefixSystem.cs
SYMBOL: prefix-driven clothing visuals (see also TODO in MaskSystem line 104: "Generalize toggling & clothing prefixes. See also FoldableClothingComponent")
WHY RELEVANT: same prefix mechanism family the upstream fix corrected.

## History
- e9266d9cdf Fix visual bug with masks appearing to be pulled down after re-equipping (#40332) — in HEAD via 68b7977bee 2025-07 2 july upstream sync (#4041); upstream SS14 PR #40332 post-dates the 2025-05-04 report.
- Earlier related: 90b99a3d91 Fix debug asserts when unequipping items (#38274), 5f4853f335 Predicted internals (#33800).
- No commits after e9266d9cdf reintroduce the bug (system untouched by later SPDX-nuke only: 06b00b9795).

## Upstream comparison
SS14 upstream: fixed by PR #40332 (title matches the report verbatim). Erida: received through the 2 july upstream sync. Goob: inherits same code path.

## Verdict
ALREADY_FIXED_IN_CURRENT_FORK

## Confidence
High

## Root cause
CONFIRMED: MaskSystem.SetToggled rewrote EquippedPrefix while the mask was worn; on unequip/equip the visual state was rebuilt from the stale toggled prefix, so masks lacking the toggled RSI state showed no worn sprite while components (internals/protection) kept working; re-toggling refreshed the prefix — exactly the reporter's steps and workaround. Fixed upstream in SS14 #40332 (e9266d9cdf), merged into this fork via 68b7977bee.

## Proposed fix (do NOT implement!)
None — already merged. Keep e9266d9cdf during future upstream syncs (the SPDX-nuke commit 06b00b9795 touches the same file; watch for merge conflicts there).

## Regression risk / Validation plan / Links-Related threads
Low. Validation: gas mask without down-state sprite → toggle down, remove, re-equip → worn sprite must render; toggle again → down sprite renders; internals stay functional in all states. Related: B03/B05 sprite batches (visual layering reports).

## Severity & complexity (if actionable)
Severity: S4 COSMETIC / Complexity: XS
