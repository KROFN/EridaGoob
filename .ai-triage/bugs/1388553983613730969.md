# Медицинские юниты не могут начать операцию.

## Metadata
Thread ID: 1388553983613730969 / Date: 2025-06-28 / Author: Shady (.besave) / Tags: На рассмотрении / Archive state: archived / Messages: 1 / Attachments: 0

## Discord evidence
- «Медицинский юнит» при попытке начать операцию любым способом сталкивается с тем, что синтетикам запрещено открывать некоторые интерфейсы, как это могут делать органики. Все условия соблюдены (пациент на столе, без одежды, анестезия) — прооперировать невозможно.
- Это авторская ИНТЕРПРЕТАЦИЯ причины («запрещено открывать интерфейсы») — фактический симптом: операцию начать нельзя, меню/интерфейс хирургии недоступен.
- Media: none.

## Search vocabulary
User terms: медицинский юнит, синтетик, интерфейсы, начать операцию
Domain terms: medical cyborg/unit, silicon, surgery BUI, UI open attempt
Prototype-entity candidates: SurgeryBui, SurgeryUIKey
Code symbols: AttemptStartSurgery, _ui.OpenUi, SurgeryTargetComponent

## Repository investigation
FILE: Content.Shared/_Shitmed/Surgery/SharedSurgerySystem.Start.cs
SYMBOL: AttemptStartSurgery (27–40)
WHY RELEVANT: `_ui.OpenUi(target, SurgeryUIKey.Key, user)` — вызов открытия UI на пациенте.
CURRENT BEHAVIOR: в текущем коде НЕТ никаких проверок типа «синтетикам запрещено» — ни силикон-фильтра, ни ActivatableUIOpenAttemptEvent на этом пути (проверено grep BoundUIOpenAttempt/ActivatableUIOpenAttempt по всему Content — хирургия не участвует).

FILE: Content.Client/_Shitmed/Medical/Surgery/SurgeryBui.cs
SYMBOL: RefreshUI (270–320)
WHY RELEVANT: клиентский ранний return, если `_player.LocalEntity` не имеет `SurgeryTargetComponent` c `CanOperate` (275–277).
CURRENT BEHAVIOR: у киборга-хирурга SurgeryTargetComponent на шасси отсутствует (компонент ставится пациентам/трупам) → кнопки шагов не активируются; субъективно «интерфейс не работает».

FILE: Content.Shared/_Shitmed/Surgery/SurgeryTargetComponent.cs
SYMBOL: CanOperate (default true)
WHY RELEVANT: единственный флаг «можно оперировать», нигде не выставляется в false в коде/прототипах для силиконов.
CURRENT BEHAVIOR: подтверждает отсутствие намеренного запрета.

## History
- Отчёт 2025-06-28. До него: «fucking predict surgery» (#4040, 2025-09-07) — ПОСЛЕ отчёта переписан старт/предикция хирургии; значит описанный баг мог быть устранён или изменён переработкой.
- git -S'Silicon' по Content.*_Shitmed: коммитов, вводивших/снимавших запрет силиконам, не найдено.

## Upstream comparison
Goob/Shitmed: не найдено апстрим-решения; не исследовалось глубже — локальной картины достаточно для CANNOT_VERIFY.

## Verdict
CANNOT_VERIFY — заявленный механизм («синтетикам запрещено открывать интерфейсы») в текущем коде не существует; симптом совпадает с тредом 1369257553430577222 (кластер silicon-surgery-ui). Доказать конкретный код-путь на дату отчёта невозможно (код переписан после).

## Confidence
Low

## Root cause
HYPOTHESIS (Low): тот же клиентский гейт SurgeryBui.RefreshUI:276 (SurgeryTargetComponent у пользователя-силикона отсутствует) либо серверный отказ открытия SurgeryBui для силикона, не восстановимый по текущему коду.

## Proposed fix (do NOT implement!)
См. 1369257553430577222: воспроизвести на текущем билде; чинить гейт RefreshUI (проверять Owner/пациента), добавить явные серверные проверки с понятными попапами вместо немого отказа.

## Regression risk / Validation plan / Links-Related threads
Risk: низкий. Validation: мед-юнит проводит операцию end-to-end. Related: 1369257553430577222 (первичный), 1372947789419516014.

## Severity & complexity
Severity: S2 MAJOR / Complexity: M
